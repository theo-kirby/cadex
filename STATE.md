# cadex — State

> Generated by `tools/hypergraph.py render` from the state-graph export.
> Do not hand-edit — run the hypergraph-reconcile skill instead.

Reconciled through `tidy-banner-0293`, `shy-glade-0050`, `empty-light-4558`, `neat-tower-5715`, `careful-key-9041`, `weathered-falcon-4350`, `deep-branch-6721`, `sleepy-shade-1485`, `shady-lodge-6077`, `wild-prairie-9912`, `quiet-creek-7756`, `idle-arrow-2946`, `candid-creek-2410`, `pale-river-6583`, `ancient-crest-4588`, `humble-shore-1680` at 2026-09-06T23:43:51+00:00.

## Frontier

- [open] **25T horns and servo pigtails come from manufacturer STEP sources** (`red-loom-6298`) — Open charter criterion: **25T horns and servo pigtails come from manufacturer STEP sources**, with the provenance recorded the way `docs/…
- [open] **A fresh machine runs the walk** (`ancient-sun-3952`) — Open charter criterion: **A fresh machine runs the walk** after one documented install script, headlessly, with no step that needs a pers…
- [open] **A research report renders headlessly** (`late-sky-8010`) — Open charter criterion: **A research report renders headlessly** from a project's `PROGRESS.md` numbers into a document with graphs a per…
- [open] **A variant study exists** (`easy-bloom-3674`) — Open charter criterion: **A variant study exists**: several variants of one joint generated by one script, tested the same way, ranked in…
- [open] **Compound mechanisms exist as parametric library values** (`idle-tower-1624`) — Open charter criterion: **Compound mechanisms exist as parametric library values** built from catalog parts: a rack and pinion and a plan…
- [open] **Inherited-tree reduction, and the unscheduled replacements** (`round-glacier-2865`) — The standing work of shrinking both inherited trees in place, under the two-commit removal protocol: dependency audit → a **disable commi…
- [open] **L3 motors and mechanisms families exist** (`rising-banner-4325`) — Open charter criterion: **L3 motors and mechanisms families exist** over `CadexCatalog`, same test shape as the boards family, and the pa…
- [open] **One mechanism in the repo came from a paper or a real product** (`spring-shore-5413`) — Open charter criterion: **One mechanism in the repo came from a paper or a real product**: the source is cited in the project's `DECISION…
- [open] **Parts library — hardware catalog and remaining families** (`brave-stone-9609`) — Catalogued hardware is available as the **lib script namespace** over `CadexCatalog`, composed as parametric BREP values with source-qual…
- [open] **The RL training loop and mg-legs** (`late-pond-2851`) — **The training loop itself is proven, locally, end to end** — what remains blocked is gait scale. The whole arc (agent-authored mechanism…
- [open] **The `hide_render` shell bug from `docs/IDEAS.md` is fixed** (`wild-comet-8096`) — Open charter criterion: **The `hide_render` shell bug from `docs/IDEAS.md` is fixed** with a test that fails on the old behaviour. [rec: …
- [open] **The agent can see its work without a screen** (`damp-moon-9297`) — Open charter criterion: **The agent can see its work without a screen.** One CLI call each, with outputs landing in the project directory…
- [open] **The catalog is broad enough for a robot prompt** (`ancient-tide-5930`) — Open charter criterion: **The catalog is broad enough for a robot prompt**: at least five servos, ten actuators, a bearings family, and M…
- [open] **The fork's delta against upstream is smaller than at the start of this run** (`green-sea-3991`) — Open charter criterion: **The fork's delta against upstream is smaller than at the start of this run**, measured by the delta manifest AG…
- [open] **The robot prompt works unattended** (`fond-clover-3062`) — Open charter criterion: **The robot prompt works unattended**: from a prompt naming the servos, bearings, and hardware, the loop produces…
- [open] **Two Phase 13b engine-side removals landed** (`windy-pebble-4630`) — Open charter criterion: **Two Phase 13b engine-side removals landed** under the two-commit protocol, DECISIONS entries included. [rec: em…
- [open] **mg-legs tips at the declared shove band, backward first** (`salty-fjord-6965`) — Open charter criterion: **mg-legs tips at the declared shove band, backward first**, with the numbers in the project's `PROGRESS.md`. [re…

## Architecture

- **cadex — state** (`nimble-pine-0740`)
  - [working] **The engine — cadexd and the xscript pipeline** (`forest-wind-0342`) — The engine is a FreeCAD fork at the repository root, stripped to one AI-native modelling engine. A release build produces `FreeCADCmd` an…
    - [working] **The wiring and electronic-assembly surface** (`clear-banner-8342`) — Procedural wire routing and the electronic-assembly surface, marked **experimental** in `docs/ARCHITECTURE.md` and carrying stated limits…
    - [open] **Parts library — hardware catalog and remaining families** (`brave-stone-9609`) — Catalogued hardware is available as the **lib script namespace** over `CadexCatalog`, composed as parametric BREP values with source-qual…
  - [working] **The shell — the Blender fork and mesh_agent** (`shy-crane-2573`) — The shell is a Blender fork under `shell/` carrying `mesh_agent` as application code under `shell/scripts/startup/` [rec: curious-sail-83…
    - [working] **File lifecycle — open, save, Save-As, and .blend vs .cadex** (`simple-willow-8989`) — The file-handling path inherited from Blender — opening, saving, Save-As, creating a new file, how the `.cadex` project directory relates…
  - [working] **The headless CLI** (`chilly-union-8972`) — `cli/` plus the `./cadex` shim is a second **front end** and the third client of the cadexd protocol: no Blender, no display, no shell co…
  - [working] **Dynamics and control on MuJoCo** (`salty-isle-4063`) — Dynamics and control is ordinary product surface on the one branch, not a vertical on a fork [rec: open-key-6334]. It is operations on th…
    - [open] **The RL training loop and mg-legs** (`late-pond-2851`) — **The training loop itself is proven, locally, end to end** — what remains blocked is gait scale. The whole arc (agent-authored mechanism…
    - [working] **The robot lifecycle walk — design to policy to iterate, agent-driven and headless** (`calm-peak-5247`) — Declared by the audit under the working name `robot-lifecycle-walk`; this node is that target [rec: sweet-light-3396].
  - [working] **Organic modelling and the CAD/mesh interface** (`idle-lantern-9094`) — Making the shapes a person actually asks for — a body, a limb, a skin over a mechanism — buildable by the agent and shapeable by the user…
  - [working] **Orientation, build and the working environment** (`early-arbor-7123`) — **Read this before touching anything.** The author's answer to "what would waste a fresh agent's whole day" was immediate and was not abo…
  - [open] **Inherited-tree reduction, and the unscheduled replacements** (`round-glacier-2865`) — The standing work of shrinking both inherited trees in place, under the two-commit removal protocol: dependency audit → a **disable commi…
  - [working] **Structural analysis, topology optimisation and shape search** (`kind-marsh-2645`) — Stress-testing a part, finding where material can be removed safely, and
  - [working] **Compliance and licensing** (`easy-wind-9848`) — The repository's compliance and licensing posture after the ADR-171 audit (PR #13, version 0.0.7) [rec: wild-sea-9905]:
  - [working] **The walk exists and is tested headlessly** (`crisp-reef-5607`) — Charter criterion: **The walk exists and is tested headlessly.** One documented entry point takes a mechanism from design → assembly → MJ…
  - [working] **Three modes, one shape** (`witty-spark-2613`) — Charter criterion: **Three modes, one shape.** The walk runs headless (exercised), with the GUI attached (documented, not exercised while…
  - [open] **A fresh machine runs the walk** (`ancient-sun-3952`) — Open charter criterion: **A fresh machine runs the walk** after one documented install script, headlessly, with no step that needs a pers…
  - [open] **The catalog is broad enough for a robot prompt** (`ancient-tide-5930`) — Open charter criterion: **The catalog is broad enough for a robot prompt**: at least five servos, ten actuators, a bearings family, and M…
  - [working] **Phase 8 `src/Gui` delete commit landed** (`civic-sand-2641`) — **The Phase 8 `src/Gui` directory-delete criterion is met (ADR-214).** After separate metatype and disable prerequisites, thirteen GUI di…
  - [open] **The agent can see its work without a screen** (`damp-moon-9297`) — Open charter criterion: **The agent can see its work without a screen.** One CLI call each, with outputs landing in the project directory…
  - [open] **A variant study exists** (`easy-bloom-3674`) — Open charter criterion: **A variant study exists**: several variants of one joint generated by one script, tested the same way, ranked in…
  - [open] **The robot prompt works unattended** (`fond-clover-3062`) — Open charter criterion: **The robot prompt works unattended**: from a prompt naming the servos, bearings, and hardware, the loop produces…
  - [open] **The fork's delta against upstream is smaller than at the start of this run** (`green-sea-3991`) — Open charter criterion: **The fork's delta against upstream is smaller than at the start of this run**, measured by the delta manifest AG…
  - [open] **Compound mechanisms exist as parametric library values** (`idle-tower-1624`) — Open charter criterion: **Compound mechanisms exist as parametric library values** built from catalog parts: a rack and pinion and a plan…
  - [open] **A research report renders headlessly** (`late-sky-8010`) — Open charter criterion: **A research report renders headlessly** from a project's `PROGRESS.md` numbers into a document with graphs a per…
  - [working] **L2 boards family with real-kernel and packaged verification** (`ready-falcon-6286`) — L2 boards exist over `CadexCatalog` through `lib.board`: ESP32-DevKitC V4 (WROOM-32E), Pi Zero 2 W and Adafruit 815 PCA9685 revision C. S…
  - [open] **25T horns and servo pigtails come from manufacturer STEP sources** (`red-loom-6298`) — Open charter criterion: **25T horns and servo pigtails come from manufacturer STEP sources**, with the provenance recorded the way `docs/…
  - [open] **L3 motors and mechanisms families exist** (`rising-banner-4325`) — Open charter criterion: **L3 motors and mechanisms families exist** over `CadexCatalog`, same test shape as the boards family, and the pa…
  - [open] **One mechanism in the repo came from a paper or a real product** (`spring-shore-5413`) — Open charter criterion: **One mechanism in the repo came from a paper or a real product**: the source is cited in the project's `DECISION…
  - [working] **The walk holds on a second mechanism** (`swift-dusk-2951`) — The unchanged `cadex walk` entry point trains, declares and verifies an ideal vertical slider/force-motor carriage beside a fresh revolut…
  - [open] **The `hide_render` shell bug from `docs/IDEAS.md` is fixed** (`wild-comet-8096`) — Open charter criterion: **The `hide_render` shell bug from `docs/IDEAS.md` is fixed** with a test that fails on the old behaviour. [rec: …
  - [open] **Two Phase 13b engine-side removals landed** (`windy-pebble-4630`) — Open charter criterion: **Two Phase 13b engine-side removals landed** under the two-commit protocol, DECISIONS entries included. [rec: em…
  - [open] **mg-legs tips at the declared shove band, backward first** (`salty-fjord-6965`) — Open charter criterion: **mg-legs tips at the declared shove band, backward first**, with the numbers in the project's `PROGRESS.md`. [re…
