---
node_id: 0d6b1e3a-81a5-57b7-9765-4edb485eda20
slug: silver-ledge-4640
title: D3. Viewport and videos match the neural-whoop reference
created_at: '2026-09-13T21:25:09+00:00'
parents:
- round-sun-8398
summary: ''
---
Status: working

## Current

**D3's evidence list is complete pending the owner's tick (ADR-331, ADR-332).** *The environment half* [rec: keen-water-3378]: `cli/cadex_cli/review_static/environment.js` exports one `PALETTE` — the reference's dark tile and scene values — and no `setTheme`; the light "prototype map" palette is deleted, not switched off, and every new video records the style `cadex-prototype-dark-v1`. On the operator URL serving `ot5-lark-copy85` with `lark98-final` re-rendered in the dark look: viewport and capture byte-identical at the same pose and camera; the decoded first frame within 1.10/255 (mean absolute RGB) of the viewport; the page's `--bg` `#141414` is the viewport's sky; the reference's own unmodified dark `scene.js`/`environment.js` over the same Lark solids at the same four cameras match in mean luminance to 0.1 (64.1 vs 64.0 whole, 30.3 sky, 32.5 floor). Stage checks at close 0.7×, framed, the video's pose, wide 3× and a real pointer orbit: the floor runs to four times the fog's far distance (asserted), the grid subdivision follows the framing (0.2 m close, 0.5 m at the video's standoff, none wide), the model stays drawn through the orbit, and a contact shadow lies beside the feet. `docs/probes/ot6/look/` holds `compare.py`, `look.json`, the quantised frames and `side-by-side.png` beside one frame each of the shipped reference `orbit`/`swing`/`flip` clips (reference commit `31caeb28…`, read-only) and the written assessment: floor/grid, horizon/fog, palette, lighting/shadows, materials and antialiasing match; materials differ by design (the eight identity colours, kept per the spec). *The camera half* (ADR-332, commit `0a6cabdf`, recorded retroactively because iteration 7 wrote no record and its suite was cut off at 74 %) [rec: brave-wave-1488]: `review_scene.js` exports `follow(track, options)` — one standoff at which the subject's standing height fills a declared **0.22** of the frame height, a Hann-smoothed anchor (half-window 4 frames), the subject 0.06 below centre, an `l·tanh(d/l)` drift limiter at 0.26, frame *i* a pure function of the track — `setClock(seconds)` drawing the reference's timer pill bottom-left inside the WebGL frame, and `modelPixels()`. `cadex_cli.video` builds the subject track from the sampled solved poses, calls `follow`, refuses a rig whose drift leaves its budget, sets camera and clock per frame, and records `framing` (declared and measured), `overlay` and the first camera into the video. `lark98-final` re-rendered on the persistent Lark copy (served without a restart) measures 0.2198–0.2201 apparent size; the viewport is within 1.16–1.24/255 of the decoded frames at 0, 4 and 8 s and byte-identical to the capture page; the floor outruns the fog at close/follow/wide framings; `docs/probes/ot6/look/follow.py`, `follow.json`, eight quantised frames and `follow-side-by-side.png` sit beside the reference's shipped clips with the assessment in the second half of the README. Tests: the dark-only and look-receipt tests and the follow receipt in `cli/tests/test_review_design.py`, `test_video.py` (the dark style, the rig on a synthetic walk and a whip, the overlay's pixel footprint); full CLI suite on the tree containing `0a6cabdf` and `fe181a85` **536 passed, 1 skipped** (the private-address test, which wants `CADEX_REVIEW_HOST`), 521 s. `docs/REVIEW-DESIGN.md` §4, §8, §9 and §10, `docs/CLI.md` and `docs/PROVENANCE.md` carry both halves.

Charter criterion: **D3. Viewport and videos match the neural-whoop reference.** Dark only: the reference's near-black grid mat with PROTOTYPE / 1 METER labels and subject-scaled pitch, fog, contact shadows, antialiasing, a camera tracking the subject at a declared framing fraction, and the timer overlay, from one environment module shared by viewport and capture. Evidence: reference frames (`neural-whoop/render-examples`) beside Cadex viewport screenshots and decoded video frames at equivalent framing, with an explicit written assessment of floor/grid, horizon/fog, palette, lighting/shadows, materials, framing and camera; the same pose/camera compared viewport-to-video; close and wide framing and orbit tested for stage edges, lost shadows or bad scale; the light palette removed from the code; existing playback, download, polling and headless tests still green. Declared target `gap-d3-viewport-videos-match-neural`; the human owns the checkbox edit [rec: brisk-ledge-9638].

Reconcile judgement: `working`, held the way ot5's ticked criteria were — every evidence item the charter lists has a committed artifact or a test, and only the checkbox edit is owner-reserved. Limit carried from the record: Lark barely moves, so the rig's smoothing and drift limiter are proven on the fixture's synthetic walk and whip, not on a travelling clip; the first travelling mechanism (D6–D8) is its first real exercise [rec: brave-wave-1488]. The ot5 receipts pinning `cadex-prototype-light-v1` are untouched and describe recordings that still exist; that look's history is on `fair-wolf-4645` [rec: keen-water-3378].

## Negative knowledge

None yet.

## Provenance

- brisk-ledge-9638 — the ot6 directive (ADR-328) declared this criterion as gap `gap-d3-viewport-videos-match-neural`
- keen-water-3378 — ADR-331: one dark `PALETTE`, no `setTheme`; viewport/capture byte-identical and the decoded video within 1.10/255 on the operator URL; the reference's own modules match to 0.1 luminance; stage checks at five framings; `docs/probes/ot6/look` with the written assessment; CLI 521 passed / 1 skipped
- brave-wave-1488 — ADR-332 (commit `0a6cabdf`, recorded retroactively): `follow` at the declared 0.22 framing with Hann smoothing and drift limiter, `setClock` timer pill, `modelPixels`; lark98-final at 0.2198–0.2201, viewport within 1.16–1.24/255 of the decoded frames; the follow receipts in `docs/probes/ot6/look`; CLI 536 passed / 1 skipped; D3's evidence list complete pending the owner's tick
