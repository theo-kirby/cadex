---
node_id: 101dd77a-16b8-5a0f-bc81-b3491f8691a7
slug: scarlet-ocean-2920
title: 'Bet: retain named inventory in historical walk reviews'
created_at: '2026-09-08T16:26:12+00:00'
parents:
- keen-field-4379
summary: ''
---
## What

Close both completed CPU dispatches and select one finite direction: retain the accepted inventory in each historical walk review (missions 1/2/6). First rehearse and document the current latest-report boundary; then replace the lossy inventory projection with existing inspection data and pin historical identity in the existing lifecycle regression.

## Why

The explicit CPU rehearsal completed in 14.6 s at 1.54 GB with all four reviews, and shared CPU fixtures passed 223 CLI tests without an outer backend override. No additional CPU or recovery unit remains [rec: floral-arrow-7365] [rec: keen-field-4379]. The eligible criterion frontier is empty; standing reduction and parked catalog/RL nodes do not authorize Later work [rec: dry-grove-2638].

Three candidates: (1) retain historical inventory identity in the existing walk review, selected because named parts are an explicit mission-6 review obligation and the current projection discards already available evidence; (2) another CPU/recovery matrix, rejected because the selected regressions now cover those boundaries; (3) catalog or fleet expansion, rejected because the human has not promoted Later criteria. This opens only candidate 1.

Read-only source inspection finds command_walk constructs inventory with available, component_count, catalogued_count and path only; write_review copies that reduced block. write_inventory overwrites docs/inventory.md, including its revision and named component rows. Thus an old run's JSON links to a latest-project report without retaining its own named inventory. Existing preservation assertions hash run directories and assets, not the linked mutable document. This is a source-derived limitation, not measured data loss: project Git retains earlier reports, and current docs accurately describe the present count-only schema. The improvement is self-contained historical review, not new catalog discovery, occurrence inference or an immutable-report subsystem. Preserve explicit uncatalogued identities.

Signals: 15 iterations, 4.8h elapsed, 43.2h left, nine iterations without frontier movement; Claude seven_day 62%, five_hour 100%, Codex seven_day 74%. Two finite model-free units fit. Budget creates neither an extra campaign nor a clock gate. The first unit performs the ladder's empty-short rehearsal at this specific review boundary and lands a concise documentation clarification, rather than another bookkeeping-only actor dispatch [rec: careful-union-7585].

## Method

Unit 1 runs one public existing-script toy CPU walk in an isolated project, verifies all reviews and records resource bounds. Then accept a minimal named-component change through the normal script command and run the ordinary inventory call without more training. Inspect the saved old review, the new latest report and Git's old report. Replace redundant inventory guide prose with a concise explanation of latest report versus per-run evidence; align the scaffold by reference and run the CLI gate. Do not claim a runtime defect if the contract behaves as written.

Unit 2 replaces the count-only projection with a bounded selection of existing inventory fields sufficient for revision, named components, source outputs and catalog identities, retaining existing summary fields for consumers and clearly labeling the latest-report link. Reuse write_review's existing serialization; no new engine op, dependency, archival directory or catalog inference. Extend an existing regression to make the inventory differ between reviews and assert the old saved rows and revision remain unchanged while latest docs advance. Replace duplicated summary/prose rather than adding a second report implementation; the whole direction must remove more lines than it adds. Update CLI guide and scaffold, ADR removal notes and ROADMAP together. Run the complete CLI gate with the requested CPU fixture, without an outer override; applicable engine/payload gates only if that boundary is actually touched. If unit 1 contradicts the source finding, replan instead of executing speculative repair.

Every training invocation remains at most 15 minutes and 3 GB, one iteration/four environments for the rehearsal. No model call, GUI, remote dispatch or generated artifact commit. Fold only the three plan horizons, retain historical negative knowledge and provenance, advance the plan mark, sync/check and commit. No charter, existing record, state or STATE.md edits.

## Result

One new direction and two bounded dispatches selected. CPU consistency is delivered evidence. This pass claims no implemented inventory fix and no retired charter gap; toy results still prove neither policy improvement nor motion safety. Replan when this direction resolves; no automatic adjacent persistence campaign follows.

## Repo

- repo: git@github.com:theo-kirby/cadex.git
- branch: ouroboros/ot4
- commit: ebdc22524ac13a4e90376068d4543ac312ae3a7f

## State Impact

- target: plan/young-crane-9546 — replace spent CPU units with bounded historical inventory review work
- target: plan/strong-birch-7412 — select self-contained inventory review from three candidates
- target: plan/late-valley-7350 — fold delivered CPU evidence and bound inventory maintenance
