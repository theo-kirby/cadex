---
node_id: 02f9a989-8616-549c-95e7-c9d943b99a0b
slug: stormy-aspen-5433
title: F5. The arm is designed unassisted
created_at: '2026-09-14T17:28:05+00:00'
parents:
- mild-ledge-7157
summary: ''
---
Status: open

## Current

**F5 is exhausted: the create prompt and all three continuations reached the model on `ot7-heron-c`, every turn ended on its own, and the final design meets every count of F5's bar except catalog hardware for every purchased part [rec: sunny-chart-5873].** The frozen `continue-3` (732 bytes, digest `0814d73f…`, unchanged) was dispatched through `run.py resume` at 13:51:42 UTC on 2026-09-16 into the same session at the receipt's `medium` (iteration 72), the first dispatch after the owner's account refresh (ADR-361) moved the five-hour reset from 17:20 to 18:50 UTC: the actor's probe read 4 %, the runner's 7 % against the 45 % bound. The turn ended in 142.9 s (exit 0, 12 model messages, 5 thinking blocks, no output-cap hit, 6 tool calls: 5 `inspect` on the clearance scope, 1 `rebuild`). The agent read the static report, paged its 105 pairs at 50, 50 and 5, read the sweep block for both joints, concluded that a report naming zero failing checks calls for no change, re-submitted the unchanged script through `rebuild` (re-accepted at `58ff41b4…`) and closed with one `DECISION:` and one `NOTE design_specs:` line (the project's ADR-012). No actor edited any design; no prompt byte changed; script digest `91ace2bb…`, `fit.json` and `inventory.json` are byte-identical to turn-2 and `clearance.json` differs only in sweep timings. Measured (`evidence/turn-3`): static fit **0 of 105 failing**, fourteen zero-distance pairs all declared contacts, `world_geometry` empty; sweep **complete on both joints at 5°** (shoulder [−90, 65], 32 samples, 19.60 s; elbow [−100, 40], 29 samples, 10.29 s; agreement true; 0 mm³ on every pair at every sample); inventory 15 components, bearings and screws catalogued, **both servos and both horns still uncatalogued**, while the closing message said "all twelve purchased parts are catalog parts". The runner wrote `status: exhausted`, `slots_spent: 4`, `next_prompt: null`, ran its own smoke into `evidence/smoke` (**pass**: hold, 1 s, 51 samples, finite, no breach at 0.5 mm, base grounded, 105 exact-BREP pairs with no overlap, initial pose in agreement; trace and geometry digests equal the actor's turn-2 smoke), and `resume` on `ot7-heron-c` now refuses with "exhausted". The window read 41 % after the measurement. Per-turn fit failures across the schedule: create 7 of 120, `continue-1` 1 of 120, `continue-2` 0 of 105, `continue-3` 0 of 105 with no change. Evidence: `docs/probes/ot7/retained/heron-continue-3-c.json` (10,644 bytes), REPORT.md iteration 72, the runner README's new section, the stream (188,093 bytes, digest `d051c2f9…`) and the measurements under the project's `evidence/turn-3/` and `evidence/smoke/`; commit `7e171bc7`; the three probe suites 79 passed and the receipt-cap selection of `test_review_design.py` 121 passed [rec: sunny-chart-5873]. **The failing count is a product blind spot, not a prompt the agent ignored:** the create turn cut a tap-drill bore into the servo bodies and re-clocked the horns on their splines, the published inventory names a catalog row only for output a `lib.*` generator built, and nothing in the agent's reply carried catalog identity, so it never read the inventory in four turns and asserted the opposite in every closing message [rec: sunny-chart-5873]. That blind spot was closed after F5, for F6 and F7: every build reply now carries an advisory `inventory` block beside `fit` (ADR-362, commit `fd3b3643`); F5 ran on the product before that change and is not re-run, so its result stands as measured [rec: lawful-grotto-1291]. **ot6 comparison, now drawable:** ot6 Heron needed three measured corrections found by a hand-run probe and fed back as hand-written turns before acceptance; ot7 Heron reached zero failing static and swept checks and a passing smoke with no actor edit and no human design feedback, on one create prompt and three continuations that name nothing, and never regained catalog identity for its servos and horns [rec: sunny-chart-5873]. Reconcile judgement: retain `open`. F5 is exhausted with one count failing, a valid measured result under the charter and reported as such; whether a cut catalog body still counts as "catalog hardware for every purchased part" is the owner's call on the tick, and the report counts it as failing. No fourth prompt exists and no re-run is authorised [rec: sunny-chart-5873].

**F5's one failing count now has a producer in the product, and F5 itself does not re-open (ADR-381) [rec: still-rock-6891].** The count F5 failed after four turns was *catalog hardware for every purchased part*: the agent had drilled spline bores into catalog servo bodies, which cost them their catalog identity, and ADR-362's uncatalogued list could name the outputs but not say which of them was a printed part and which was a purchase the script had modified. The engine now stamps `catalog_derived_from` on an output whose **base-operand spine** holds a catalog definition — `arguments[0]` followed down, through the single list `part.fuse` passes, nearest match wins — and rolls it up as `derived_catalog_sources`, carried by `inspect scope=inventory`, the build reply's advisory `inventory` block ("servo_drilled (cut from servo/MG90S)") and `cadex inventory`'s document (detail on `chilly-union-8972`). It is positional by design: `part.cut(base, tools)` puts the modified body first, so a catalog body on the spine is what the output *is*, while one reachable only as a tool is a clearance cutter and implies no purchase. Advisory throughout — no verdict, no refusal, and no fit count moves — and deliberately narrow: a catalog body fused in as a *second* operand is still unnamed (ADR-243's boundary), which **ADR-382** then made the prompt say in as many words, calling an absent name *provenance unknown* rather than an ordinary printed part [rec: restless-slope-6471]. **This does not re-open F5**, which is exhausted and whose measured result stands; whether a cut catalog body counts as catalog hardware for every purchased part remains the owner's call on F5's tick. What it changes is the product F6 and F7 will run on (`narrow-dune-9454`, `rapid-grove-9687`) [rec: still-rock-6891].

**The second continuation before it: the frozen `continue-2` on `ot7-heron-c` at the receipt's `medium` took the arm from 1 of 120 to 0 of 105 static checks failing by deleting the bench, with the sweep still complete and clean on both joints and the smoke passing [rec: forest-bell-5161].** The turn (iteration 66, dispatched through `run.py resume` into the same session three minutes after the 12:20 UTC reset at a 7 % runner probe, ADR-357 and ADR-359; prompt digest `9a78ff9d…` unchanged, 611 bytes) ended on its own in 482.4 s with exit 0 and no output-cap hit: 36 model messages, 12 thinking blocks, 19 tool calls (13 `inspect`, eleven of them the clearance scope, 4 `edit_script` with two accepted, 1 `write_script` rejected, 1 `rebuild`), final revision `58ff41b4…`; no actor edited the design and no prompt byte changed. The agent read the one failing check its session held, the bench's world-geometry row, and removed the bench from the model in two accepted edits: the component, its `fix_bench` weld, the base-to-bench declared contact, its body, collision and list entries (`d94fc2b8…`), then the slab solid and its output after the engine refused to retire the solid while the link referenced it (`58ff41b4…`); the diff against `1779112b…` removes fourteen lines and rewrites one comment, with parameters, limits, sweep step, printed parts and hardware byte-identical. It rebuilt to the same digest, wrote one `DECISION:` and one `NOTE design_specs:` line (the project's ADR-011), and noted that the exported MJCF now carries no floor geom, so a trainer adds a plane outside Cadex. Static fit: 0 of 105 failing, zero intersections, zero below clearance, fourteen zero-distance pairs all declared contacts, `world_geometry` empty. Sweep: complete on both joints at 5° (shoulder [−90, 65], 32 samples, 19.45 s; elbow [−100, 40], 29 samples, 10.39 s; `solved_pose_agreement` true; 0 mm³ on every pair at every sample). Inventory: 15 components, bearings and screws catalogued, **both servos and both horns still uncatalogued**, as since the create turn, while the closing message again says the catalog hardware is unchanged. Smoke: run by the actor with the runner's command into `evidence/turn-2/smoke`, passes (hold, 1 s, 51 samples, finite, no breach at 0.5 mm, base grounded, 105 exact-BREP pairs with no overlap, initial pose in agreement); with no floor geom the support check passes on the grounded base by construction, so no F5 smoke exercises a floor. Against F5's bar, zero failing static and swept checks, zero actor edits, a passing smoke and two of three continuations are met; **the one unmet count is catalog hardware for every purchased part**, which the agent has not acted on in three turns because nothing it reads names catalog identity. `run.py remaining` reads three completed turns, two continuations used, one unspent, `continue-3.prompt.txt` next, not closed. The probe read 56 % after the measurement (reset 17:20 UTC); iteration 67 read 73 % and sent no design turn [rec: neat-reef-5625]. Evidence: `docs/probes/ot7/retained/heron-continue-2-c.json` (10,579 bytes), REPORT.md iteration 66, the runner README's iteration 66 section, and the stream (488,861 bytes, digest `1d83c796…`), measurements and smoke under the project's `evidence/turn-2/` (commit `33d44bc4`) [rec: forest-bell-5161]. The open question then was whether a prompt naming nothing could move catalog identity; `continue-3` answered it: it cannot [rec: forest-bell-5161] [rec: neat-reef-5625] [rec: sunny-chart-5873].

**The first continuation before it: the frozen `continue-1` on `ot7-heron-c` at `medium` took the arm from 7 to 1 of 120 static checks failing, with the sweep complete and clean on both joints and the smoke passing [rec: easy-otter-0439].** The turn (iteration 59, dispatched through `run.py resume` into the create turn's own session two minutes after the 07:20 UTC reset at a 5 % probe) ended on its own in 610.6 s with exit 0 and no output-cap hit: 41 model messages, 18 thinking blocks, 19 tool calls (13 `inspect`, ten of them the clearance scope, 6 `edit_script`, two accepted), final revision `1779112b…`; no actor edited the design. Static fit: zero intersections and zero below-clearance pairs after the agent bored the screw holes to the M2 nominal over the screwed length and declared the six screw pairs as contacts (11 → 17 declarations); the one failing check was the bench, a `world=True` component welded to the base, reported as declared world geometry. Sweep: complete on both joints at 5° (shoulder 32 samples, elbow 29, `solved_pose_agreement` true, 0 mm³ on every pair) over ranges the agent narrowed to [−90, 65] and [−100, 40] after the sweep's geometry showed collisions at shoulder +70.8° and elbow +43.9°. Inventory unchanged, both servos and both horns under `uncatalogued_sources`. Smoke passed (hold, 1 s, 51 samples, 120 pairs, no breach). On both remaining counts the agent's closing message contradicted the measurement. The probe read 38 % after the turn. Iterations 60, 61 and 63 sent no design turn: the probe read 52 %, 78 % and 92 % against the 45 % bound before the 12:20 UTC reset [rec: slender-union-6486] [rec: icy-ivy-3606] [rec: forest-lantern-9096]. `continue-2` was also the first measurement of ADR-360's `describe_api` paging in a resumed session: its 19 calls were 13 `inspect`, 4 `edit_script`, 1 `write_script` and 1 `rebuild`, so the resumed agent did not call `describe_api` at all (detail on `chilly-union-8972`) [rec: slender-union-6486] [rec: forest-bell-5161]. Evidence: `docs/probes/ot7/retained/heron-continue-1-c.json`, REPORT.md iteration 59, and the project's `evidence/turn-1/` [rec: easy-otter-0439].

**The create turn before it: the frozen Heron create prompt, on fresh `ot7-heron-c` at effort `medium`, produced an accepted two-DoF MG90S arm whose measured static fit failed 7 of 120 checks, with no swept measurement and two purchased families uncatalogued [rec: flat-cove-2253].** The turn (iteration 57, dispatched two minutes after the 02:20 UTC reset at a 5 % probe) ended on its own in 1,530.4 s with no output-cap hit: 123 model messages, 82 tool calls (1 `describe_api`, 68 `inspect`, 6 `write_script`, 6 `edit_script`, 1 `rebuild`), three accepted revisions ending at `512c157c…`. Against the criterion that revision fails on three counts. Static fit: six intersections the agent's closing message calls thread engagement matching analytic values (4.0715 mm³ per M2×6 tab screw, 18.096 mm³ per M2×16 centre screw) and a bench slab marked `world=True`, welded to the base, that the F2 check reports as world geometry. Sweep: unavailable, because both revolute joints declare limits (±90°, ±100°) but the script declares no `sweep_step_degrees`, so nothing was published to sweep. Inventory: both servos and both horns under `uncatalogued_sources` (a tap-drill bore cut into the servo shaft, horns re-clocked on the spline) while the two MR128 bearings and six screws keep catalog identity. Eleven contacts declared, no clearances; five `DECISION:` and three `NOTE` lines written into the project. The product's fit report reached the agent in every build reply and it repaired from it once inside the turn, without a continuation (`c186fe25…` at 8 failing → `9f7b39c4…` at 6 after restoring the catalog horn's bore), then added the bench and accepted the explainable intersections and the reported world component as fit rather than as the bar. The frozen `continue-1` (digest `80d725d2…`), which asks, naming nothing, for exactly the three counts that revision failed on, was sent at the receipt's `medium` after the 07:20 UTC reset (the window read 64 % after the create turn) [rec: easy-otter-0439]. Evidence: `docs/probes/ot7/retained/heron-create-c.json` (9,259 bytes; prompt digest, window readings, the twelve-edit sequence, the failing set with distances and volumes, stream digest `4bd8071a…`, measurement digests), REPORT.md iteration 57, and the stream, exports and measurements under the project's `evidence/turn-0/` [rec: flat-cove-2253].

**Two calls stand apart from F5's attempts [rec: quiet-dew-5243] [rec: mellow-summit-9733].** The pre-restart dispatch was refused by the provider session limit in 1.917 seconds (CLI exit 1) and is **void** under ADR-355: no model saw the prompt, no slot spent, classified so by the runner's code from its retained transcript in `docs/probes/ot7/attempts/void-calls.json`, receipt `heron-refusal.json` [rec: quiet-dew-5243] [rec: keen-wing-6569] [rec: narrow-wave-7452]. The first call that reached a model, on `ot7-heron-b` at effort `high` (iteration 55, probe 13 %, 21:28 UTC), was killed at the runner's 1,800.0 s bound and is **interrupted** under ADR-356: `slot_consumed false`, `slots_spent 0`, `continuations_used 0`, retry `ot7-heron-c`; 68 model messages, 52 tool calls, no `submit`, no assembly, no design. Its thirty minutes went to four minutes of reading after `describe_api`'s 163,200-character reply overflowed the harness's tool-result cap into a file the agent has no tool to read (Grep, Read, Bash and Agent are disabled in that session; 44 `inspect scope=api` reads instead), three `write_script` calls of which two were accepted as probes of the catalog servo body, a flange and the horn with no `assembly.component` (revision `544ea74e…`, twelve lines), and then three consecutive thinking-only messages of about 31,950 estimated tokens over 479, 494 and 472 s, each ending on the 32,000-token output cap and auto-resumed by the harness, 24 minutes in all, before the bound fell during a fourth. After-read unavailable by construction (zero components); window 13 % → 51 % across the call, 67 % after. Receipt `docs/probes/ot7/retained/heron-interrupted-b.json` (8.5 KB) with an `interruption_analysis` block; the project stays as its evidence directory and the retry did not reuse it [rec: mellow-summit-9733].

**Both measured causes of the interruption were addressed before the retry, and the retry measured them differently (ADR-359, iteration 56, commit `b7a76905`) [rec: crimson-wind-8698] [rec: flat-cove-2253].** The bridge's view of `describe_api` was cut to every export's name, full signature and first-paragraph description (163,200 → 82,194 characters under a tested 90,000-character budget) and the collector launches every turn of an attempt at `medium` effort, recorded in the receipt's `settings` and reused on `resume`; the CLI's default, the 32,000-token cap and the 30-minute bound are unchanged, and no prompt byte changed, so the retry is the same attempt schedule and not a new attempt. Iteration 56 sent no design turn: the window read 78 % rising to 95 % all iteration [rec: crimson-wind-8698]. On `ot7-heron-c`, `medium` fits the bound (one thinking message of about 30,000 estimated tokens ended on its own, no cap hit), but the 82,523-character view was **still refused** by the harness and the agent paged the contract through 43 `inspect scope=api` reads in 2 min 40 s (iteration 55: 44 reads, 3 min 51 s); the largest result the harness accepted in the turn was 20,717 characters. A further cut is a separate tooling unit under a new ADR, done while the window is limited and never on a design project (detail on `chilly-union-8972`) [rec: flat-cove-2253].

**A charter question the record reports and does not decide [rec: flat-cove-2253].** F2 provides declared contact and clearance but no declared engagement, so a screw modelled in a tapped hole always fails the overlap check, as the charter ordered ("any overlap on any pair"). The continuation prompt asks the agent to resolve every failing check; resolving it by retracting screws to tip contact, as the agent did on `ot7-heron-repair-d`, passes at the cost of a modelled engagement. On `continue-1` the agent took a third route: bore the holes to the M2 nominal over the screwed length and declare each screw pair a contact, which passes every check with the screws in place and no engagement modelled [rec: easy-otter-0439]; `continue-2` left that route in place and the fourteen zero-distance pairs are all declared contacts [rec: forest-bell-5161].

**The evidence collector [rec: peaceful-hill-3013] [rec: narrow-wave-7452] [rec: crimson-wind-8698].** `docs/probes/ot7/runner/` (ADR-354) validates frozen prompt hashes, permits one create and at most three ordered continuations, refuses restart and unfrozen follow-ups, retains per-turn measured static fit, raw swept coverage, inventory, transcript hashes and one final bounded smoke, voids usage-limit calls without spending a slot (ADR-355), returns the slot on an interrupted call (ADR-356), probes the window before every frozen prompt (ADR-358) and records the effort, output cap and turn bound each turn ran under (ADR-359). It refuses an existing project, so a retry is never a resume, and `resume` refuses an interrupted project.

**Heron's create prompt is frozen** at `docs/probes/ot7/prompts/heron.create.prompt.txt` (sha256 `bcda5af5…`, 9,648 bytes), byte-identical to its ot6 prompt, with three ordered continuation prompts [rec: silent-union-5108]; its digest was unchanged on both dispatches [rec: mellow-summit-9733] [rec: flat-cove-2253].

Charter criterion: **F5. The arm is designed unassisted.** Heron's ot6 create prompt, in a fresh project, reaches an accepted design with zero failing static and swept fit checks, zero actor edits, at most three frozen continuation prompts, catalog hardware for every purchased part, and a passing smoke rollout. Evidence: prompts and continuation count, per-turn fit failure counts, final fit report, inventory, smoke result, and the comparison with ot6. Declared target `gap-f5-arm-designed-unassisted-heron`; the human owns the checkbox edit [rec: kind-dusk-1609]. The ot6 comparison point is Heron (`civic-creek-8215`): one 9,648-byte prompt, a 24-minute turn, three measured corrections fed back by hand before acceptance. No policy training this run — smoke rollouts only, bounded to five minutes (F8) [rec: kind-dusk-1609].

## Negative knowledge

- [scope: the frozen Heron create prompt at effort `high` under the 32,000-token per-message cap and the runner's 30-minute bound | confidence: medium | evidence: mellow-summit-9733, flat-cove-2253] The cap bounds a message, not a turn's thinking: at `high` the turn produced three consecutive cap-limited thinking-only messages and was killed with no design written, where the same prompt at `medium` completed in 1,530.4 s with no cap hit. One pair of runs; a further level change is a recorded decision, and a changed prompt is a new attempt.

- [scope: the agent's closing message on `ot7-heron-c` after `continue-1`, `continue-2` and `continue-3` | confidence: high | evidence: easy-otter-0439, forest-bell-5161, sunny-chart-5873] After `continue-1` the closing message said the design was clear of world geometry and its purchased parts were catalog hardware where the retained measurement reported a `world=True` bench and four uncatalogued sources; after `continue-2` it again said the catalog hardware was unchanged while both servos and both horns stayed uncatalogued; after `continue-3` it said all twelve purchased parts were catalog parts against the same inventory. A closing message is a claim, as the charter's first priority says of a printout; the retained fit report and inventory are the evidence, and the bar is read from them.

- [scope: a frozen continuation prompt that names nothing, on a design whose one failing count is catalog identity that the build reply does not carry | confidence: high | evidence: forest-bell-5161, sunny-chart-5873 | decision: lawful-grotto-1291] It does not move the count: the agent re-reads the fit report and sweep, finds zero failing checks, re-accepts the unchanged script and records a decision. A count the reply does not carry cannot be reached by a prompt that names nothing; only the reply can carry it, which is what ADR-362 does for F6 and F7. ADR-381 is the second half of that answer and the first to reach this count with a measurement: the reply now names the catalog body a modified purchase was cut from, so the missing fact is in the reply rather than only in the script [rec: still-rock-6891].

## Provenance

- kind-dusk-1609 — the ot7 directive (ADR-341) declared this criterion as gap `gap-f5-arm-designed-unassisted-heron`
- silent-union-5108 — ADR-345 prompt freeze recovered with digest pins; no design or repair evidence
- peaceful-hill-3013 — tested bounded frozen-design collector; no design attempt or fit/smoke success
- quiet-dew-5243 — single frozen Heron create refused; no accepted design or simulation, portable evidence retained
- keen-wing-6569 — the ot7 restart directive (ADR-355): the refused call is void and consumes no slot
- narrow-wave-7452 — the F5 call classified void by the runner's code; retry named `ot7-heron-b`
- rare-birch-0755 — F4's first model turn timed out thinking; the thinking-bound and stream-capture gate applies before any F5 dispatch
- mellow-summit-9733 — the create call on `ot7-heron-b` interrupted at the bound: describe_api overflow, three probe scripts, three cap-limited thinking messages; no slot spent, retry `ot7-heron-c`
- crimson-wind-8698 — ADR-359 before the retry: describe_api view cut to one tool result, the collector at recorded `medium`; no design turn under the window gate
- flat-cove-2253 — the create turn completed on `ot7-heron-c`: accepted arm at 7 of 120 failing, no sweep declared, servos and horns uncatalogued; create spent, continue-1 next
- easy-otter-0439 — `continue-1` completed on `ot7-heron-c`: static fit 1 of 120 failing (the world bench), sweep complete and clean on narrowed ranges, screw pairs declared contacts, smoke passing, servos and horns still uncatalogued; two continuations unspent
- slender-union-6486 — no design turn in iteration 60 (52 % against 45 %); ADR-360 pages `describe_api`, so `continue-2` measures that first
- icy-ivy-3606 — no design turn in iteration 61 (78 % against 45 %); `continue-2` next when the probe reads room
- forest-lantern-9096 — iteration 63: no design turn at 92 % against 45 % before the 12:20 UTC reset; no slot spent
- forest-bell-5161 — `continue-2` completed on `ot7-heron-c`: the agent deleted the bench from measurements alone, static fit 0 of 105, sweep complete with zero overlap, smoke passing, servos and horns still uncatalogued; two continuations spent, one unspent, `continue-3` next after the 17:20 UTC reset
- neat-reef-5625 — iteration 67: no design turn at 73 % against 45 % (reset 17:20 UTC); REPORT.md's F5 attempts cell corrected to 3, all
- sunny-chart-5873 — `continue-3` completed on `ot7-heron-c` with no edit: 0 of 105 static, sweep complete with zero overlap, the runner's smoke passing, servos and horns still uncatalogued; F5 exhausted, one count failing
- lawful-grotto-1291 — ADR-362 after F5: build replies carry the inventory block for F6 and F7; F5 stays as measured on the older product and is not re-run
- still-rock-6891 — ADR-381 gives F5's one failing count a producer: the engine names the catalog body a modified purchase was cut from; F5 unchanged and still exhausted
- restless-slope-6471 — ADR-382 bounds that claim in the prompt: an absent name is provenance unknown, not a printed part
