Create a NEW parametric two-legged walking mechanism called Plover in this empty project. This is a fresh design: do not import, link, copy or consult anything from any other project, mechanism, checkpoint, policy or earlier conversation. Do not train anything in this turn. Complete this single design turn without asking questions.

The bar this design is measured against: it must be a machine somebody could build. Every purchased part comes from the catalog (`lib.*`; inspect the catalog families and specs first and use their datasheet numbers as datums), every other solid is a modelled printable part with a named purpose, and no solid is a bare box or cylinder standing in for something. Nothing in the world is part of the design: no floor, slab, wall, bench or stage. The floor is the environment's plane at z = 0 (world +X forward, +Y left, +Z up); the biped stands on it with the flat soles of both feet on z = 0, and nothing is grounded.

The machine, from the catalog: four `lib.servo("mg90s")` micro servos, two per leg, hip pitch and knee pitch, every joint axis along world Y so each leg swings in the XZ plane. All four joints use the SAME printed joint module, built from the servo's spec numbers:
- The PARENT of a joint (the pelvis for a hip, the thigh for a knee) has two printed cheeks straddling the child. The servo sits in a rectangular window cut through the OUTBOARD cheek (window sized from body_width_mm and body_length_mm about shaft_offset_from_front_mm, with a declared clearance window_clear per side, e.g. 0.3 mm), its mounting tabs seated on the cheek's outer face, and it is held by two catalog M2 socket screws (`lib.bolt("m2", 6.0)`) through the tab holes (positions from the spec's mount_holes) into tap-drilled holes in the cheek (`lib.tap_drill("m2")`). The servo's spline points inboard, through the window, towards the child.
- The CHILD (the thigh for a hip, the shin for a knee) is a printed block between the cheeks. On its outboard face a form-fit slot receives the servo's catalog single-arm horn (`lib.servo("mg90s").horn("single_arm")`, seated on the spline, hub and arm pocket with a declared horn_clear per side) and the horn is clamped by a catalog M2 centre screw (`lib.bolt("m2", 16.0)`) down the spline axis into the spline, whose engagement you state analytically. On its inboard face the child carries a printed cylindrical stub that turns in a catalog `lib.bearing("mr128")` (8 mm bore, 12 mm OD, 3.5 mm wide) pressed into a bore through the INBOARD cheek, with a declared stub_clear radial clearance. A declared side_gap separates the child block from the inboard cheek's inner face; on the outboard side the child face sits at the spline top so the horn hub clears the cheek.
- The PELVIS is one printed part: a top plate with an open electronics bay (a pocket declared by parameters for a small driver board and a battery, not modelled as solids; state the bay size in the design specs), and two hip clevises, one per leg, at a declared hip_spacing (about 56 mm between the leg planes). It is the free base: declare it as the FIRST component in script order and first in the assembly's component list, ground nothing, and give it no fixed joint to the world.
- Each THIGH is one printed part: the hip child block with its horn slot and stub, a beam of declared length thigh_len (about 80 mm axis to axis), and the two knee cheeks at its far end. Each SHIN is one printed part: the knee child block with its horn slot and stub, a beam of declared length shin_len (about 80 mm from the knee axis to the sole), and an integral flat sole of declared foot_len by foot_w (about 70 x 34 mm) whose underside is the lowest face of the design. At the standing pose (all four joints at 0 degrees) both legs are straight down (-Z) under the pelvis and both soles lie flat on z = 0; joint limits are declared parameters (hip_limit about 60 degrees each way, knee bending one way only from 0 to knee_limit, about 90 degrees).
- Use `lib.*` parts only for what the catalog has: four servos, four single-arm horns, four MR128 bearings, eight tab screws and four centre screws. The pelvis, both thighs and both shins are printed.

Declare parameters with sensible defaults in millimetres and kg/m3 and use them everywhere: thigh_len, shin_len, hip_spacing, block_w, block_d, cheek_t, window_clear, horn_clear, stub_clear, side_gap, foot_len, foot_w, foot_t, pelvis_w, pelvis_d, pelvis_t, bay_w, bay_d, bay_h, printed_density (PLA ~1240), servo_armature, servo_friction, hip_limit, knee_limit, plus the task parameters below. Also declare a numeric parameter policy_on with default 0, following the project's iterate convention, so that a trained policy can later be switched on by `cadex walk` without editing geometry; while policy_on is 0 nothing about the model changes.

Dynamics, declared in the script for MuJoCo, following the project's own guidance:
- Every purchased part is fixed to the component that carries it with a `fixed` joint (servo, tab screws and bearing to the parent; horn and centre screw to the child). Each hip and knee is a `revolute` about the lateral axis with the declared limits, with both connectors of every joint given the identical posed world frame so the assembly solves with zero residual at the standing pose.
- Masses from geometry: `assembly.body` for every solid, printed parts at printed_density, catalog parts at the density their spec gives (the servo's effective_density_kg_m3, steel 7850 for screws and bearings, nylon 1150 for the horn).
- Collision proxies, declared per body, never the tessellation: boxes for the pelvis plate and each clevis cheek, for each link's block, beam and cheeks, for each sole and for each servo case, each with a declared friction; screws, horns and bearings carry mass only. Record in a comment how each proxy relates to its solid. At t = 0 only the two soles touch the floor and no two proxies of the design touch each other.
- Actuators: one `servo.actuator(joint, control_deg="0", command_limits_degrees=[...])` per joint bounded by the joint's declared limits, so each is a position servo bounded by the MG90S's real stall torque, and `assembly.joint_dynamics` on each joint with damping derived from the spec's stall torque and no-load speed, plus a small armature and friction.
- Observations: all four joint positions and velocities, the pelvis's position, orientation and angular velocity, the centre of mass and its velocity, and each actuator's force.
- A standing task (`assembly.task`): reward an alive bonus, a penalty proportional to the pelvis's tilt from upright, a small term for forward progress of the centre of mass along +X, and a small control cost on the actuator forces; end the episode when the biped has fallen, declared as the pelvis frame height dropping below a declared fraction of its standing height (a parameter fall_frac, default 0.7), and say in a comment which channel the termination reads. Episode length episode_steps (default 400) at control_hz (default 50), rollout_seed for the declared seed set 0-9, and a reset variation on the pelvis of a small rigid tilt (0-2 degrees) with a small lift (4-10 mm) so the task cannot be won by bracing.
- Export the assembly and the MJCF/training task as publishable outputs so that `cadex train` finds exactly one training task; publish every solid as an output too, named so the pelvis, each thigh, each shin, each servo, horn, bearing and screw are recognisable by name and side (e.g. pelvis, thigh_l, shin_r, servo_hip_l, horn_knee_r, bearing_hip_r, tabscrew_knee_l_0, centrescrew_hip_r).

Verify before accepting, through the measured fit checks your tools report for the published result, never through the script's printout alone: the solver reports solved with zero residual and the pelvis as the free base; each servo sits in its window with the declared clearance (nearest distance between servo and cheek equals window_clear and common volume is 0); each horn sits in its slot at horn_clear with common volume 0 against the child; each stub sits in its bearing bore at stub_clear; each bearing is pressed in its cheek bore (distance 0, common volume 0); each screw's only intersection is its thread engagement (tab screws in the tap-drilled cheek holes, centre screws in the spline), and you state the expected engagement volumes analytically; there are no other intersections and no unmeasured pairs; both soles are on z = 0 and nothing is grounded; the dynamics export reports the environment's floor and lists exactly four revolute joints. Fix and re-accept until these hold.

Build and accept the model. Then finish with the documentation the project asks for: a `DECISION:` line each for the joint module (why servo-on-parent, horn-on-child, bearing opposite), the free base and the sole-on-floor convention, the standing task shape and the actuator choice, and `NOTE design_specs:` lines stating the standing height, the hip spacing, the sole size, the total mass split into printed and purchased with the unmodelled board and battery named, the actuator torque limit, the fall threshold, episode length, control rate and the success measure (upright survival, pelvis tilt excursion and displacement over seeds 0-9). Report the parameters you declared and the outputs you published.
