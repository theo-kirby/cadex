Create a NEW parametric two-degree-of-freedom servo arm called Heron in this empty project. This is a fresh design: do not import, link, copy or consult anything from any other project, mechanism, checkpoint, policy or earlier conversation. Do not train anything in this turn. Complete this single design turn without asking questions.

The bar this design is measured against: it must be a machine somebody could build. Every purchased part comes from the catalog (`lib.*`; inspect the catalog families and specs first and use their datasheet numbers as datums), every other solid is a modelled printable part with a named purpose, and no solid is a bare box or cylinder standing in for something. Nothing in the world is part of the design: no floor, slab, wall, bench or stage. The bench is the environment's plane at z = 0 (world +X forward, +Y left, +Z up); the arm's printed base stands on it with its foot at z = 0 and is the one grounded component.

The machine, from the catalog: two `lib.servo("mg90s")` micro servos, one per joint, both joint axes along world Y so the arm works in the XZ plane. Shoulder pitch at the base, elbow pitch at the end of the upper arm. Both joints use the SAME printed joint module, built from the servo's spec numbers:
- The PARENT of a joint (the base for the shoulder, the upper arm for the elbow) has two printed cheeks straddling the child. The servo sits in a rectangular window cut through the OUTBOARD cheek (window sized from body_width_mm and body_length_mm about shaft_offset_from_front_mm, with a declared clearance window_clear per side, e.g. 0.3 mm), its mounting tabs seated on the cheek's outer face, and it is held by two catalog M2 socket screws (`lib.bolt("m2", 6.0)`) through the tab holes (positions from the spec's mount_holes) into tap-drilled holes in the cheek (`lib.tap_drill("m2")`). The servo's spline points inboard, through the window, towards the child.
- The CHILD (the upper arm for the shoulder, the forearm for the elbow) is a printed block between the cheeks. On its outboard face a form-fit slot receives the servo's catalog single-arm horn (`lib.servo("mg90s").horn("single_arm")`, seated on the spline, hub and arm pocket with a declared horn_clear per side) and the horn is clamped by a catalog M2 centre screw (`lib.bolt("m2", 16.0)`) down the spline axis into the spline, whose engagement you state analytically. On its inboard face the child carries a printed cylindrical stub that turns in a catalog `lib.bearing("mr128")` (8 mm bore, 12 mm OD, 3.5 mm wide) pressed into a bore through the INBOARD cheek, with a declared stub_clear radial clearance. A declared side_gap separates the child block from the inboard cheek's inner face; on the outboard side the child face sits at the spline top so the horn hub clears the cheek.
- The BASE is one printed part: a foot plate on the bench (with four M3 clearance holes for bench screws, `lib.clearance_hole("m3")`; the bench screws are not modelled because the bench is not part of the design), a pedestal, and the two shoulder cheeks at a declared shoulder height z_shoulder (about 40 mm). It is the grounded component (`grounded=True`).
- The UPPER ARM is one printed part: the shoulder child block with its horn slot and stub, a beam of declared length upper_len (about 80 mm axis to axis), and the two elbow cheeks at its far end. The FOREARM is one printed part: the elbow child block with its horn slot and stub, a beam of declared length fore_len (about 70 mm from the elbow axis to the tip) ending in a rounded printed tip. At the rest pose (both joints at 0 degrees) the upper arm points straight up (+Z) from the shoulder and the forearm points forward (+X) from the elbow; joint limits are declared parameters (shoulder_limit about 90 degrees each way, elbow_limit about 100 degrees each way).
- Use `lib.*` parts only for what the catalog has: two servos, two single-arm horns, two MR128 bearings, four tab screws and two centre screws. The base, upper arm and forearm are printed.

Declare parameters with sensible defaults in millimetres and kg/m3 and use them everywhere: upper_len, fore_len, z_shoulder, block_w, block_d, cheek_t, window_clear, horn_clear, stub_clear, side_gap, foot_len, foot_w, foot_t, printed_density (PLA ~1240), servo_armature, servo_friction, shoulder_limit, elbow_limit, plus the task parameters below. Also declare a numeric parameter policy_on with default 0, following the project's iterate convention, so that a trained policy can later be switched on by `cadex walk` without editing geometry; while policy_on is 0 nothing about the model changes.

Dynamics, declared in the script for MuJoCo, following the project's own guidance:
- Every purchased part is fixed to the component that carries it with a `fixed` joint (servo, tab screws and bearing to the parent; horn and centre screw to the child). Each joint is a `revolute` about the lateral axis with the declared limits, with both connectors of every joint given the identical posed world frame so the assembly solves with zero residual at the rest pose. Place the FOREARM component's frame at the tip point (so its `component_position` observation reads the tip), and give the elbow joint's forearm-side connector the offset from the tip back to the elbow axis.
- Masses from geometry: `assembly.body` for every solid, printed parts at printed_density, catalog parts at the density their spec gives (the servo's effective_density_kg_m3, steel 7850 for screws and bearings, nylon 1150 for the horn).
- Collision proxies, declared per body, never the tessellation: boxes for the base's foot, pedestal and cheeks, for each link's block, beam and cheeks, and for each servo case; screws, horns and bearings carry mass only. Record in a comment how each proxy relates to its solid. At t = 0 no two proxies of the design touch each other and nothing but the base touches the environment.
- Actuators: one `servo.actuator(joint, control_deg="0", command_limits_degrees=[-limit, limit])` per joint, so each is a position servo bounded by the MG90S's real stall torque, and `assembly.joint_dynamics` on each joint with damping derived from the spec's stall torque and no-load speed, plus a small armature and friction.
- Observations: both joint positions and velocities, the forearm's position (the tip), the forearm's linear velocity, and each actuator's force.
- A reach task (`assembly.task`): the tip must reach and hold a declared target point (parameters target_x, default about 100 mm, and target_z, default about 60 mm; target_y = 0 in the arm's plane) from the rest pose. Reward: a term that rises as the tip nears the target (e.g. `exp(-(sqrt((tip_x - target_x)^2 + (tip_z - target_z)^2))/reach_scale)` with reach_scale a declared parameter, about 30 mm), a small control cost on the actuator forces, and a small penalty on tip speed so holding is rewarded over oscillating. Terminate the episode when the tip drops below a declared tip_floor (default 5 mm above the bench), and say in a comment which channel the termination reads. Episode length episode_steps (default 200) at control_hz (default 50), rollout_seed for the declared seed set 0-9. A grounded mechanism has no free base to vary at reset, so give the task its per-seed variation with an `assembly.disturbance` on the forearm: a horizontal push of a declared band (push_n, e.g. 0.2 to 0.6 N) in the arm's plane (azimuth_degrees=[0, 0] or [180, 180], drawn between them by declaring two events or one band; state which) at a drawn time in the middle of the episode for a declared duration (0.2 s). State plainly that the target does not vary between episodes.
- Export the assembly and the MJCF/training task as publishable outputs so that `cadex train` finds exactly one training task; publish every solid as an output too, named so the base, upper arm, forearm, servo, horn, bearing and each screw are recognisable by name (e.g. base, upper_arm, forearm, servo_shoulder, horn_elbow, bearing_shoulder, tabscrew_shoulder_0, centrescrew_elbow).

Verify before accepting, through inspect facts and the script's own stdout: the solver reports solved with zero residual; each servo sits in its window with the declared clearance (nearest distance between servo and cheek equals window_clear and common volume is 0); each horn sits in its slot at horn_clear with common volume 0 against the child; each stub sits in its bearing bore at stub_clear; each bearing is pressed in its cheek bore (distance 0, common volume 0); each screw's only intersection is its thread engagement (tab screws in the tap-drilled cheek holes, centre screws in the spline), and you state the expected engagement volumes analytically; there are no other intersections and no unmeasured pairs; the only grounded component is the base; the dynamics export reports the environment's floor and lists exactly two revolute joints. Fix and re-accept until these hold.

Build and accept the model. Then finish with the documentation the project asks for: a `DECISION:` line each for the joint module (why servo-on-parent, horn-on-child, bearing opposite), the tip-frame convention, the reach task shape and the grounded base, and `NOTE design_specs:` lines stating the reach (upper_len + fore_len), the shoulder height, the target point, the total mass split into printed and purchased, the actuator torque limit, the tip floor, episode length, control rate and the success measure (tip-to-target distance at episode end and over the final second, over seeds 0-9, and whether it stays within a declared reach_tol of 10 mm). Report the parameters you declared and the outputs you published.
