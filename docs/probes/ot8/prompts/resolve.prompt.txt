This project already holds an accepted design of yours. Its fit measurements pass and the project's own bounded smoke does not. Before you edit anything, read the measured evidence your tools return for the accepted revision: the fit report at the rest pose and through every joint's declared range, the inventory of what the design is made of and where each part came from, the exported model and the task the design declares, and the smoke result together with the rule that ended it. Decide from those measurements alone which of two things the smoke is reporting: a defect in the design, or the declared behaviour of the machine this design is. If it is a defect in the design, resolve it without changing what the design is: every purchased part stays an unmodified catalog part, every other part stays a modelled printable part, nothing from the world enters the design, nothing becomes fixed to the world that the design declares free, no support appears that the machine does not have, no declared limit or task rule is weakened, and the smoke stays exactly as long as the design declares it. Submit the whole corrected script through normal acceptance and re-accept. If instead the measurements show the machine behaving as it declares, and the behaviour the task asks for would need active control this design cannot provide, then change nothing at all: write a DECISION: line and NOTE design_specs: lines naming exactly which measurements show that, what the missing control contract would have to be, and what it would read and command. Do not train. Do not write a controller. Do not ask questions.
