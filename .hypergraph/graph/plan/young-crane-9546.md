---
node_id: c1e64944-a027-59bb-80ed-a530f6fc6dad
slug: young-crane-9546
title: short
created_at: '2026-09-06T19:21:31+00:00'
parents:
- fond-ember-4937
summary: ''
---
Status: open

## Current

1. **Qualify and finish the existing `cadex walk` on one fresh repository-owned toy (mission 2; crisp-reef-5607).** Inspect the implementation now tracked in d34c3cab; fix forward only what prevents a documented headless entry point from building design/assembly, exporting MJCF/task, training, automatically declaring the policy, verifying, rolling out and reviewing. Reuse existing legs. Supply a repo-owned starting mechanism without a paid model turn; land outputs beneath the project and exercise `docs/gear-ratios.md` and `docs/sensors.md` with project-local numeric review. Commit source/docs/numbers, never policies, checkpoints or traces. Run dedicated entry-point coverage and the full CLI suite against this implementation; run engine or packaged gates if that zone is touched. Training uses its venv, CPU, at most 15 minutes and 3 GB per run. Update lifecycle docs/scaffold together and record the exact remaining leg if incomplete; the old 117-test audit does not cover this command. One qualification/fix commit, at most one full build. [rec: lone-wood-3732] [rec: fond-mesa-1562]
2. **Script the walk's remote-training handoff (mission 2; witty-spark-2613), after unit 1 lands or is recorded blocked.** Reuse `training/remote_train.sh` and the walk's bundle, policy return and verification legs; provide one documented export/dispatch/return contract with identical project-relative artifacts and offline tests for command construction and returned-policy handling. Do not dispatch, access the GPU checkout or provision anything. Preserve training's offboard boundary and record scripted versus executed evidence separately. [rec: lone-wood-3732] [rec: empty-wolf-3962]
3. **Document the GUI-attached walk against the same project contract (mission 2; witty-spark-2613).** Describe ownership/locking, when the headless caller releases the project and how the shell observes accepted changes, using actual client behavior as evidence. Use the same steps, domain documents and artifacts as the proven headless entry point; correct any mismatch in the doc, and record a separate blocker if runtime work is necessary. No GUI launch; documentation is the authorized evidence for this mode. [rec: lone-wood-3732] [rec: empty-wolf-3962]

## Negative knowledge

None yet.

## Provenance

- lone-wood-3732 — qualify current implementation and sequence lifecycle legs
- empty-wolf-3962 — fold pending short seed under charter constraints
