# .hypergraph/config.yml — written by `hypergraph adopt --init`.
project: cadex

# The release that last installed this project's skills, AGENTS.md
# block and workflows — refreshed by `hypergraph upgrade`.
hypergraph_version: 0.0.13

record_root:
  node_id: 9cd90f99-81e3-5d17-9c44-06634151caf4
  slug: odd-banner-6071

state_root:
  node_id: 76f41cb1-395d-5a67-b207-edd243158413
  slug: nimble-pine-0740

cache_dir: .hypergraph/cache
state_md: STATE.md
graph_dir: .hypergraph/graph

# Adoption epoch (SPEC: Adoption epochs). Record nodes created strictly
# before this marker are legacy history, exempt from I2 in `check`.
epoch:
  marker: winter-rain-7897

# Named views (SPEC: Views): extra derived graphs over the record graph,
# each reconciled independently. Appended by `hypergraph views add`.
views:
  plan:
    root:
      node_id: 27ca0eeb-32fc-5f3a-9fc4-a58509761fa4
      slug: fond-ember-4937
    md: PLAN.md
