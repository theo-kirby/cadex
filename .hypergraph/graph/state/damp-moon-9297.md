---
node_id: 531acaa2-823d-5218-88ca-dc1043987a63
slug: damp-moon-9297
title: The agent can see its work without a screen
created_at: '2026-09-06T19:18:34+00:00'
parents:
- nimble-pine-0740
summary: ''
---
Status: working

## Current

Charter criterion: **The agent can see its work without a screen.** One CLI call each, with outputs landing in the project directory: render from named angles, section view through a named plane, list the parts of an assembly with catalog ids, and a clearance and intersection check that names the offending pairs. The lifecycle walk's review step uses them [rec: empty-wolf-3962]. The nt3 operator directive re-seeds the same criterion, unticked, as the second half of this run's frontier ("the lifecycle walk and the eyes it reviews itself with"); its ladder puts the four calls on the medium rung, one CLI call at a time, each wired into the walk's review step as it lands [rec: modest-summit-8554]. Declared target: `gap-agent-can-see-work-without`.
**Reopened for ot4 (2026-09-08) and closed the same day.** The ot4 operator directive re-seeded this criterion unticked as the second half of this run's frontier [rec: humble-forest-6896]. Nothing below was withdrawn: all four calls exist, are wired into the walk's review step, and are pinned by real-engine tests. What had changed was the machine — `sb1x`, Ubuntu 24.04, RTX 5090, 32 cores — on which none of the four had been exercised, because the run's first walk exited 3 at the train leg and never reached its review [rec: open-hollow-2140].

**All four eyes now run on this machine, in one walk.** The clean ot4 walk (execution tracked on `crisp-reef-5607`) used every one of them at its review step, landing under the project directory: **render** front/top/right/iso over 13,432 triangles in 3.4 s, **section** on the world XZ plane at Y = 3.125 mm, **inventory** at 10 components with 7 catalogued, and the **clearance bounds check** reading **pass** over 90 comparisons across 45 pairs, with 13 pairs inside the 0.1 mm advisory band. The generated `docs/{clearance,inventory}.md` were committed by the walk's own child commands [rec: wandering-jasper-6102].

Reconcile judgement: `open` → `working`. The reopening rested on the operator's re-seed rather than on a defect, and the honest reading at the time was that the surface was delivered and awaiting re-verification on this run's machine by the walk's own review step. That is exactly what has now happened, so the condition the previous pass set is discharged [rec: humble-forest-6896] [rec: wandering-jasper-6102].

**Delivery and integration, compacted (the record graph keeps the per-ADR detail).** The inventory is `inspect scope="inventory"` joining accepted assembly components to source outputs, catalog identities, solved placements and source facts, with `cadex inventory` writing `docs/inventory.md` with no AI and no rebuild (ADR-236) [rec: fair-rose-5950]; the CLI reader recursively expands nested previews and pages mappings, lists and strings, after a critic rejection on catalog totals and oversized rows [rec: lawful-ivy-4474]; and the walk commits the report beside a `review.json` inventory block [rec: happy-dune-3481]. **Clearance** is `inspect scope="clearance"` reporting every pair's exact minimum distance and common solid volume at the initial solved pose, with finite nonnegative read-time thresholds yielding intersection / below-clearance / clear / unknown and legacy or unsolved measurements never implying clear — no `OP_ARG_SPECS` or shell change, and warm rebuild medians moved +0.84% / -0.61%, inside the declared stop conditions (ADR-237) [rec: falling-birch-1503]; the walk writes `docs/clearance.md`, a `review.json` summary and a `PROGRESS.md` counts row at 0.1 mm / 1e-6 mm³, where findings do not fail the walk but inspection errors do (ADR-238) [rec: clear-ash-2884]. **Rendering** went to a CPU projection over bounded accepted display snapshots after the background Blender route was measured refusing outright [rec: zesty-aspen-6846]: `cadex render` writes front/top/right/iso SVGs wrapping a lossless 512px raster plus `summary.json` under `review/render/`, with no graphics dependency and no engine, protocol, payload or shell change (ADR-239) [rec: silver-key-8483], integrated into the walk under `review/render/<accepted-revision>/` from one snapshot taken before inventory and clearance can invalidate the buffers [rec: square-path-1173] [rec: modest-sun-6068]. **Sections** are `cadex section --plane XY|XZ|YZ --offset-mm N`, closed per-object contours with even-odd fill preserving cavities, reporting unsupported on plane contacts or open/branched segments and empty outside the cut (ADR-240) [rec: placid-lily-5624], wired into the walk at the fixed world XZ plane at Y = 3.125 mm from the same accepted snapshot as the named views [rec: copper-timber-8947]. A bounded arm rehearsal reproduced the committed inventory, clearance and verified rollout along the way [rec: brave-delta-4193].

**Rehearsals and cold revisits, compacted.** Separate fresh arm and carriage complete-review rehearsals through the unchanged public entry point (one CPU training iteration, four environments, seed 0, timeout 600) had all four named views and the interior XZ section visually inspected, with accepted identity matching rollout and review and retained artifacts matching clean committed bytes; arm section areas 360/640 mm², carriage 360/400 mm²; the named base–swing pair contact at 0 mm / 0 mm³ and base–slide clear at 34 mm, both with zero unknowns; each gate 195 passed, zero skipped [rec: first-branch-9614] [rec: quiet-vine-3426]. A cold revisit through four separate public processes retained accepted identity and object geometry with all five SVGs byte-identical to the committed baseline [rec: fair-cedar-7455], and the refreshed ordinary installed-engine walk extended the same evidence to the local bundle in a clean five-commit scratch project [rec: strong-raven-3067]. Engine-selection guidance was bounded to its verified check, prose only, leaving review evidence untouched [rec: tiny-haven-0347] [rec: keen-pebble-3574]. Rewards do not rank useful control and the carriage falls under gravity; GUI remains documented-only and remote scripted-only [rec: first-branch-9614] [rec: quiet-vine-3426].

**The first walk on catalog parts verified three of the four eyes and caught the fourth lying (2026-09-08).** A pan-tilt head on two catalog MG90S servos went through the walk on the installed bundle. All four named views read correctly as the mechanism; the XZ section at Y = 3.125 mm gave five closed `ok` contours whose bounds agree with the exported STL vertex bounds and the MJCF inertial positions; and the inventory carried catalog ids for the first time — 5 components, 2 catalogued, `servo/mg90s` ×2, three uncatalogued hand-modelled outputs named. But `clearance` reported three intersecting pairs of which two were impossible (servo_pan/servo_tilt sharing exactly one whole servo volume, 8240.943 mm³, while the section, STL and MJCF put them ~26 mm apart) and one was inflated ten-fold (base/servo_pan 1112.640 mm³ against a true ≈111.3 mm³ sink). Hand-modelled parts measured correctly; only `lib.*`-placed bodies were wrong, and no real-kernel clearance test used a transformed component [rec: empty-banner-7438].

**Pair clearance now measures a component where the assembly puts it (ADR-241, commits `cf3b77a6` and `0a1a1fbe`).** The cause was read in the kernel: an `App::Link` *replaces* the linked object's placement with its own instead of composing them, and every `lib.*` part carries its transform on the shape because `lib._place` uses one `part.transform`. `_measure_clearance` read `component.Shape` and so measured catalog bodies in their canonical origin frame. The fix is one helper, `_component_world_shape`, taking the linked shape with `component.Placement * shape.Placement` — the composition the MJCF export already uses, so review now agrees with the physics; a container source keeps `component.Shape` and an unreadable source falls through to the old error path. A real-kernel `FreeCADCmd` regression over four `App::Link` components **fails on the pre-fix source** (shape-placed pair 0.0 mm / ≈1000 mm³) and passes after it (40.0 mm / 0 mm³; the both-frames body at 150–160 mm; a real 2 mm overlap at its true 200 mm³). Engine gate 2076 passed / 52 skipped; CLI gate 195 passed, zero skips. On the pan-tilt assembly, rebuilt through `cadex params` and re-reported with `cadex clearance` against the dev tree: servo_pan/servo_tilt clear at 25.9 mm, base/servo_tilt clear at 57.9 mm, base/servo_pan intersecting at 111.264 mm³ — and three real contacts the wrong frame had hidden now appear (pan servo in the yoke 18.857 mm³, tilt servo in the yoke arm 75.430 mm³ and in the head plate 73.500 mm³), so the error had suppressed true findings as well as inventing false ones [rec: light-timber-5868].

**The swept clearance check measures a component where the assembly puts it (ADR-242, commit `79d40404`).** `_clearance_at_frame` read the same `App::Link.Shape` and so swept every `lib.*`-placed body in its authored frame. The fix hoists the composition out of the frame loop: `_clearance_prepare` builds one re-placeable copy per component before the sweep and the loop writes only `component.Placement * shape.Placement` per frame (a `TopLoc_Location`, not geometry); `_component_world_shape` and the swept path share a new `_linked_source_shape`, and a container source is still read live as ADR-241 decided. **Measured before deciding**, on identical world-authored geometry with both modules in one process, three runs each: 0.429 / 0.422 → 0.374 / 0.378 ms/frame where box rejection dominates (2000 frames, 286 queries) and 2.999 / 3.015 → 3.043 / 3.044 ms/frame where a distance query dominates (300 frames, 900 queries), noise ≈3 % and ≈1 % — faster in the first case because one composed copy replaces one `Link.Shape` build per pair, and inside the noise in the second. The cost ADR-241 feared is not there once the copy is hoisted. A real-kernel regression (`test_swept_clearance.py`) swings a shape-placed arm in 5° steps past a world-authored post: 36.674 mm at rest and 0.0 mm across frames 16–20 after the fix, where the pre-fix source reports **33.0 mm at rest and an empty breach list** — a straight-through collision read as clear air. Engine 2077 passed / 52 skipped; CLI 195, no skips; engine rebuilt, installed and staged; packaged lifecycle gate 15 and both clearance suites 14 against the fresh payload. A first run failed 34 tests because the helper shadowed the existing `_component_local_shape` used by the MJCF export; renamed before anything was committed [rec: southern-otter-5999].

**Both fixes now ship in the ordinary installed bundle, and its clearance is proved right on a catalog-placed assembly (2026-09-08).** `pixi run build-shell` plus the install script refreshed the `.app`, with `cadex_assembly_worker.py` byte-identical across source, staged payload and bundle (sha256 `6a0e9ff8…`; manifest and binary unchanged from the previous refresh, so only Python moved), and the packaged lifecycle gate passed 15 against that root. A fourth pan-tilt walk against the bundle ran clean (406.33 s, 1.750 GB, four legs exit 0 — tracked on `crisp-reef-5607`), but its design turn **fused** both `lib.servo` bodies into hand-authored solids (3 components, 0 catalogued), so none of its three clearance rows involved a `lib.*`-placed part and the structural check on it is a **null result, not a pass**. The evidence came instead from driving `clearance`, `inventory`, `section` and `render` through the same installed bundle against the earlier pan-tilt project (5 components, 2 catalogued): a checker that reads each pair's world bounds from three surfaces independent of the clearance path — section summary, render summary and the exported STL's parsed vertex bounds — asserts that a disjoint pair carries no common volume and no distance below its axis gap, and an overlapping pair no volume above its AABB overlap. **20 pairwise comparisons, 0 failures**, seven of the ten rows touching a catalog part: base/servo_pan 111.264 mm³ against a 158.6 mm³ ceiling, base/servo_tilt 57.9 mm clear, servo_pan/servo_tilt 25.9 mm clear. **Negative control:** the same checker on the three pre-fix rows from the first catalog walk reports 10 failures, so it has power over exactly the defect it was written for [rec: morning-summit-7848].

**Reconcile judgement: retain `working`; the three named blockers are closed.** The previous pass held the criterion off its tick on three counts — the swept check's frame, the stale installed bundle, and no walk against a bundle carrying the fix. ADR-242 closes the first, the refresh closes the second, and the fourth walk plus the lib-placed cross-check close the third, with the honest note that the walk itself could not exercise the catalog path because the design turn is nondeterministic. All four review calls now measure a catalog-placed component where the assembly puts it, through the shipped artifact, against independent surfaces, with a working negative control. Neither record declares a status flip and the planner's bet explicitly retires no charter gap, so the tick stays a charter decision rather than a maintainer one; what this node can say is that it knows of no remaining defect on the criterion. Still not claimed: sections are tessellated cuts, `cadex clearance` is initial-pose (the swept path shipped in the bundle but was exercised only by its regression), toy-policy scope, documented-only GUI and scripted-only remote modes, and a repeatable way to make a design turn produce catalog components [rec: southern-otter-5999] [rec: morning-summit-7848].

**Fused-part identity remains unsupported in cold reports; bounded dependency discovery is complete.** The accepted pan-tilt has 14 outputs, zero catalog stamps and neither MG90S as a separate output; an output-only scan cannot recover the fused servos [rec: odd-ridge-9607]. Three fresh real-kernel scratch acceptances distinguish direct sources, transformed fuse inputs, cut tools and discarded values. The combined assembly retains four components and two placed servo instances; isolated cut-only and discarded-only cases each retain one component and zero placed catalog counts. Syntactic dependency paths therefore remain separate from placed counts [rec: early-gate-3510].

Joining a fresh evaluation's identity map to the unchanged historical pan-tilt definitions finds servo/mg90s at `base_solid`'s `$.arguments[0][2]` and `yoke_solid`'s `$.arguments[0][4]`. The cold accepted report alone cannot supply those labels. Equal-definition registry overwrite and an identical unused-call control show that matches cannot recover occurrence provenance; unknown operation roles remain unknown, and traversal never tests material survival. Discovery stopped at these identity limits: no diagnostic, persisted metadata or automatic reaccept is authorized. Reconcile judgement: retain `working` on existing review evidence; this bounds a limitation, closes no additional criterion and does not measure fresh agent adherence to placement guidance [rec: early-gate-3510].

**Unsupported tally removed; placed inventory verified (ADR-243).** The call registry, worker field, inspection arithmetic, derived inventory/walk claims and tests of that discarded contract were removed. ADR-236 identity stamping remains. A real-kernel regression publishes one catalog bolt body and links it twice, retaining two catalogued instances, both source names and family/id without generated/unplaced fields. Historical reports' tally fields are ignored without reacceptance or digest migration. Full engine: 2078 passed / 52 skipped; CLI: 195 passed, no skips; fresh packaged lifecycle/inventory: 24 passed. Build/install and staging succeeded; the local 2.4 GB payload retains external-path warnings and is not a relocatable release. The application bundle was not refreshed for this correction [rec: windy-dune-3488].

**Reconcile judgement: retain `working`.** The correction preserves the established review criterion while removing an unsupported inference. It establishes neither fused catalog identity nor an agent's adherence to separate-component placement guidance; no additional charter criterion closes. Initial failures were resolved before the recorded final gates, including quarantine of one stale temporary worker bundle; the underlying cache invalidation issue was not fixed [rec: snowy-cove-0032] [rec: windy-dune-3488].

**Review guidance now pairs the script with placed inventory.** The design overlay and fresh scaffold teach separate purchased components and transformed clearance cutters, and ask for script review alongside inventory; neither construction calls nor inventory absence establish purchases or absence of fused catalog geometry. CLI 195 passed with no skips proves delivery only. Retain `working` on existing review evidence; at that point the fresh two-servo rehearsal and agent adherence remained unmeasured [rec: misty-tide-6394] — the leg rehearsal below measured both once.

**The two-servo leg's review, fresh on the qualified payload: all four eyes read a catalog-placed assembly and name real findings (2026-09-08).** The eligible leg walk (execution tracked on `crisp-reef-5607`) produced four front/top/right/iso views (7,776 triangles, 10 objects) that were inspected by extracting the embedded PNGs: straight hanging links, the two servo bodies on opposite sides of the link plates, lateral offsets and the foot hardware. The world XZ section at Y = 3.125 mm is available/ok: thigh, knee servo and foot intersect the plane and seven other objects correctly report empty; the visible cut shows the long thigh contour with the knee-shaft circle and a separate foot rectangle. **The placement guidance was observed working on this run:** the inventory reports 10 components, 6 catalogued (`servo/mg90s` ×2, `bolt/m3x10-socket` ×2, `nut/m3-nyloc` ×2), and a manual read of the script agrees — one servo, one bolt and one nut body each published once and placed twice, four independently modeled printed solids, the only other catalog call a scalar M3 clearance diameter, no catalog body feeding a printed-solid boolean and no discarded catalog body or cutter. That conclusion comes from reading the script, not from a zero tally, and it establishes no cold historical identity or occurrence-tracing guarantee. Clearance at 0.1 mm / 1e-6 mm³: **45 pairs, 11 offending, 0 unknown** — seven zero-volume contacts and four positive volumes: shin against each foot bolt 5.837 mm³ (a concrete design finding) and each bolt against its nyloc nut 9.278 mm³ (possibly simplified mating envelopes, not waived). Both servo/driven-link distances are 5 mm, in the views as in the numbers: the ideal revolute joints simulate, but the review does not show a physically attached drive interface. The morning-summit-7848 bound-agreement checker reads **PASS, 90 comparisons, 0 failures**, render and section for all 45 pairs, 39 of them involving catalog instances — the first non-null fresh check of the published catalog-placement path, as against the zero-catalog fourth pan-tilt walk. No STL comparison is claimed (the walk's STL exports are local-frame source solids, and no world-placed STL was found). The project carries inventory, clearance, review JSON, views and section in a clean five-commit history; the agent added no dedicated sensor or gear domain notes beyond the generated inventory/clearance documents, a limitation of this authoring run rather than of the convention. Reconcile judgement: retain `working`; the criterion gains its first fresh evidence on an assembly the design turn itself built from separately placed catalog parts, and the eyes named two findings a person would act on. Still not claimed: swept or animated clearance (initial-solved-pose views and tessellation cuts only), exact OCCT volume validation beyond AABB consistency, a mechanically qualified leg, or a repeatable way to make a design turn place catalog parts [rec: proud-beacon-8002].

**The eyes' findings were acted on and the eyes re-read the result (2026-09-08).** The same leg walked again in a durable project (execution on `crisp-reef-5607`) with the three findings above stated as design inputs. Four front/top/right/iso views and the world XZ section at Y = 3.125 mm were inspected by eye at the accepted revision `417d0fdf…` by extracting the embedded PNGs and rasterising the section: the hanging chain support→thigh→shin→pad, each servo case seated through its plate cutout with a boss meeting the case top, the two servos on opposite sides, and in the section the thigh hip boss as an annulus, the thigh bridge and **the knee servo spline inside the shin boss bore** — the attachment, in cut. Clearance at 0.1 mm / 1e-6 mm³: **45 pairs, 9 offending (was 11), 0 unknown, and every offending pair a zero-volume face contact** — the four positive volumes are gone (real Ø3.4 `lib.clearance_hole("m3")` through-holes with a countersink void; bolt grip held at 10 mm so the shank ends at the nyloc base plane), and both servo/driven-link distances are 6.1e-16 mm where they were 5 mm (a printed Ø9 boss with a Ø5.5 bore over the Ø4.9 spline, face-coincident with the case top). The nine remaining contacts are declared mating faces; a 0.0 mm contact is still below the criterion and still reported, a changed geometry with a stated intent rather than a waived check, and no print-fit or tolerance claim follows. Inventory: 10 components, 6 catalogued (`servo/mg90s` ×2, `bolt/m3x10-countersunk` ×2, `nut/m3-nyloc` ×2), the script tally agreeing that no catalog body enters a printed solid; the agent chose not to place a catalog horn and said so in a project ADR. The bound-agreement checker, re-implemented because the earlier one was ad hoc and not kept, reads **PASS, 90 comparisons, 0 failures** at a stated 1e-3 mm tolerance, worst discrepancy 3.05e-6 mm (float32 tessellation resolution at ~100 mm), 39 of 45 pairs involving a catalog instance — AABB consistency across three independent outputs, not validation of each OCCT volume. The limitation the previous paragraph noted (no dedicated domain notes) is closed on this run: the design turn wrote `docs/actuators.md` and `docs/sensors.md` under ADR-245. Reconcile judgement: retain `working`; this is the first time the review's named findings went round the loop and came back answered in geometry, which is what the criterion is for. Still not claimed: swept or animated clearance, exact solid sections, a mechanically qualified leg, or a repair turn on a live conversation — the findings were given to a fresh prompt, not fed back through `--resume` [rec: fond-star-1809].

**Latest provider-free review evidence survives scratch repair (2026-09-08).** The fresh hinged-arm walk persisted four visually inspected front/top/right/iso previews, two-component inventory (zero catalogued), and the documented base/swing contact: one pair checked, 0 mm distance, 0 mm³ common volume, zero unknowns. The XZ section at Y = 3.125 mm reports a closed contour for each object; SVG paths and metadata were inspected, without a separate rasterized section inspection. Render and section match rollout revision `63cd29194a373d23a3906370a5733fefa27c2812da4a3b7470dac4945bd5595c` and digest `31b1848c15d8fbf21aa73403f31cd23cba3aa174ddc88c8492444ef3f4137980` [rec: lucid-snow-0350]. Scratch commit `faeced2` leaves review JSON, all five SVGs, inventory/clearance documents and progress tracked and byte-identical while untracking generated artifacts. Reconcile judgement: retain `working` within existing initial-pose/tessellation limits; no review or walk rerun occurred, and first-visit scaffolding remains unexercised [rec: scarlet-journey-0203].

**The wider-carriage iterate has consistent initial-pose review evidence.** Four inspected front/top/right/iso renders show the carriage around its rail (60 triangles, Y bounds ±40 mm). The world XZ section at Y = 3.125 mm reports `ok`, with one closed base contour and two carriage contours. Inventory has two uncatalogued components and carriage volume 231950 mm³. Clearance is 0.5 mm, common volume 0, one pair, zero offending/unknown; bounds agreement passes two comparisons. Review outputs agree with the accepted revision/digest. Retain `working`: these are initial-pose and tessellation checks, not motion coverage; CLI gate: 223 passed without skips. [rec: mellow-quartz-8093]

**Review documentation agrees with its existing output.** The guide's obsolete denial of the walk clearance review row is removed; runtime and the accurate project scaffold are unchanged [rec: still-harvest-7242]. The condensed carriage rehearsal retains initial-pose and single-seed limitations and links immutable four-eye evidence instead of repeating it. Reconcile judgement: retain `working` within the existing review limits; documentation maintenance supplies no new review execution [rec: floral-canyon-9424].

**Successful recovery exercises all four review outputs.** The real-engine CPU regression continues after injected trainer exit 7 through a successful public walk retry, then checks inventory, clearance, render and section using the existing review helpers. The toy retains its one known clearance offender; the final CLI gate passes **223 tests, zero skips**. Reconcile judgement: retain `working` within the existing initial-pose/tessellation limits; this adds recovery-path review coverage, not motion, GUI or actual remote qualification. [rec: placid-ember-6741]

**Explicit-CPU rehearsal corroborates the existing review scope.** The public hinged-arm walk produced four nonempty named renders, an `ok` XZ section and two uncatalogued inventory components. Clearance checked the single base/swing pair: below 0.1 mm with zero common volume, zero unknowns and a passing bounds check. All four reviews passed; the CLI gate recorded 223 passed, no skips. Reconcile judgement: retain `working`; this is initial-pose review evidence, with no swept-motion, GUI or actual remote qualification [rec: floral-arrow-7365].

**The saved review keeps counts, not names, and the latest report is a moving target (ADR-254, 2026-09-08).** A public toy walk followed by a component rename measured the boundary between a run's own review and the project's `docs/inventory.md`: `write_review` copies only the reduced inventory block — `available`, `component_count`, `catalogued_count` and `path` — while `cadex inventory` overwrites the linked document, revision and named rows included. Measured on the hinged-arm walk (base/plate and swing/arm at revision `ca5336093456`), renaming the published `swing` key to `rocker` advanced the latest report to `efd11c898d0b` with the new names while the old `review.json` bytes stayed byte-identical, still counting two components and zero catalogued and still linking `docs/inventory.md`. The original report survives in the project's own Git at commit `1de6507`, so this is a projection limit rather than data loss, and `docs/CLI.md` and the project scaffold now state the latest-report-versus-per-run contract in place of repeated inventory prose. Runtime is unchanged and no defect is claimed; a named inventory snapshot inside the saved review remains unbuilt [rec: soft-crane-2369].

**A motion channel joins the four eyes: what moved, and by how much, with no screen (ADR-259, 2026-09-08).** Every one of the four calls the criterion names reads a single pose. `review.json` now also carries a `motion` block, read from the rollout trace the run already produced: per component the per-axis `position_range_mm`, the `max_displacement_mm` from the reference pose and the `max_rotation_deg` swing away from the reference orientation, plus `largest_translation` in millimetres and `largest_rotation` in degrees named separately. It **refuses to rank millimetres against degrees** and says so in a `ranking` field. The reference is the first solved frame; the `input` frame the solver was given is excluded and reported as excluded, so the count read back is 26 solved rather than the raw 27. The same two figures reach the `PROGRESS.md` row and the run notes, which is the other place a reader looks. Delivery is `cli/` and `docs/` only — no engine, protocol, payload or `shell/` diff — with four unit regressions over frames lifted verbatim from the two documented example rollouts (the rotating case, the translating case, an all-identity trace and the unavailable reasons) and the end-to-end walk test asserting the review file, the notes and the progress row; CLI gate 231 passed, 0 skipped. Reconcile judgement: retain `working`. This is the first review output on this criterion that is not initial-pose, and it closes the gap where a rollout that never moved and one that merely scored worse read identically — but it reads a trace, not swept geometry, so the swept-clearance limit above is unchanged, and it is not yet in the iterate comparison. Execution is tracked on `crisp-reef-5607` [rec: square-bay-3436].

**All four eyes, plus the two newer channels, on a third mechanism (2026-09-08).** The quill-lift walk (execution on `crisp-reef-5607`, mechanism on `swift-dusk-2951`) exercised every review call at its review step: **render** front/top/right/iso over 1,404 triangles in 0.49 s, **section** on the world XZ plane at Y = 3.125 mm reading `ok`, **inventory** 2 components / 0 catalogued, and **clearance** 1 offending pair. Two things it added that no previous walk could [rec: chilly-crest-2100]:

- **The ADR-256 documentation eye read a real design turn's own unprompted notes back, for the first time.** `expected [actuators, sensors]`, `notes [docs/actuators.md, docs/sensors.md]`, `missing []` — the row says `docs notes 2, none missing`. Both notes were written unprompted and both are substantive: the servo's 5 N/mm stiffness, its near-critical 0.03 N·s/mm damping with the arithmetic shown, a 20 N limit against a 0.27 N static weight and why the rotation coordinate is deliberately unactuated; and all six sensor channels with units, naming the one that drives the termination rule. Every earlier read of this eye was on a recipe walk, which runs no design turn and so must report every subject missing. [rec: chilly-crest-2100]
- **The ADR-259 motion block reported both channels on one component**: `travel_mm 20.28`, `travel_deg 0`, `quill_component`, 201 solved frames. The cylindrical joint offers a slide and a hinge, and the rollout used only the slide — gravity exerts no torque about a vertical axis, so an axially offset head mass does not spin the quill. The zero is a fact about the rollout, not a gap in the eye, and it is the case the two-channel design was written for. [rec: chilly-crest-2100]

**The clearance eye returned its first offending pair on an agent-authored design, and it was right to.** `housing`/`quill`, verdict `intersection`, 960 mm³ common volume, 0.0 mm distance — **deliberate**, and the project says so in its own ADR-005: the shaft is modelled inside a solid bore cylinder, the joint rather than contact constrains the quill, and the two collision groups are disjoint. So the eye read a known modelling choice back rather than catching a defect, and the walk's exit 0 is correct — ADR-238's rule that findings do not fail a walk, and the report says "reported", not "all pairs are clear". Worth recording as a correction: the 960 mm³ was first read as the head block passing 2 mm through the column, from the arithmetic alone; the project's own ADR gave the real reason and both documents were corrected before the commit — a reminder that a volume figure does not carry its cause [rec: chilly-crest-2100].

Reconcile judgement: retain `working`. This is the criterion's first evidence on a third mechanism and the documentation eye's first live read, and it is what the eye was built for. Unchanged and still not claimed: swept or animated clearance (initial-pose views and tessellation cuts only), exact OCCT volume validation, GUI and actual remote qualification, and a named inventory snapshot inside the saved review [rec: chilly-crest-2100].

## Negative knowledge

- [scope: early-gate-3510 scratch definition traversal and unchanged historical pan-tilt report | confidence: high | evidence: early-gate-3510] Fresh evaluation identity enables syntactic matches for measured fuse/transform/cut inputs, but cold pan-tilt stamps supply no identity. Equal definitions lose occurrence provenance; unknown operations are not traversed, so zero matches there proves no absence. Paths establish neither purchases nor surviving material.
- [scope: removed generator-minus-placement tally and audited declarative definitions | confidence: high | evidence: snowy-cove-0032, windy-dune-3488] Construction counts do not identify physical quantities, unplaced hardware or surviving boolean inputs. Dependency paths alone do not establish catalog provenance or material survival.
- [scope: temporary worker bundle encountered during the tally correction | confidence: high | evidence: windy-dune-3488] One content-addressed bundle held old worker bytes and caused import failures; quarantining it restored the final gates without changing cache code. Cache invalidation remains uninvestigated. Staging must finish before suites that inspect the payload run.

- [scope: accepted pan-tilt rerun artifact at revision `8ad39702…`, catalog-stamped published-output scan | confidence: high | evidence: odd-ridge-9607] Both fused MG90S are absent as separate outputs and all 14 outputs lack catalog stamps. Scanning those outputs cannot recover their identities; generator-call totals are not evidence of actually used parts.

- [scope: named-angle probe using the existing background Blender renderer after accepted hinged-arm hydration | confidence: high | evidence: zesty-aspen-6846] All four valid blueprint views and `render_views` explicitly refuse background mode. The override probe's exit 0 records returned errors, not a rendered PNG. CPU projection must snapshot revision-dependent display buffers before another rebuild invalidates their paths.

- [scope: inventory inspection replies exceeding the 1 KiB per-key budget | confidence: high | evidence: lawful-ivy-4474] Paging only `/components` is insufficient: catalog totals, uncatalogued sources, component rows and nested strings can also be previews. The former reader failed converting `/catalog_counts` to an integer and could lose oversized names; recursive expansion is required and regression-tested.

- [scope: `_measure_clearance` on an `App::Link` whose linked object carries its own placement — every `lib.*`-placed catalog part | confidence: high | evidence: empty-banner-7438, light-timber-5868] `App::Link` replaces the source's placement rather than composing it, and `Part.getShape(link)` does the same; only a link to an `App::Part` container composes. `SimpleNamespace`-stubbed tests cannot see this, which is why three earlier walks on hand-modelled parts never did. Fixed for the pair check by composing `component.Placement * shape.Placement`, pinned by a real-kernel regression that fails on the old source.
- [scope: `_clearance_at_frame`, the swept clearance inside the simulation trace, for `lib.*`-placed bodies | confidence: high | evidence: light-timber-5868, southern-otter-5999] Had the same link-shape frame defect and was left by ADR-241 pending a cost measurement. Closed by ADR-242: with the composed copy hoisted out of the frame loop the sweep is no slower (faster under box rejection), and the pre-fix source reported a straight-through collision as 33.0 mm of clearance. The measurement, not the guess, is what licensed the fix.
- [scope: a structural check stated as a conditional over `lib.*`-placed rows | confidence: high | evidence: morning-summit-7848] The design turn is nondeterministic: the same pan-tilt prompt that placed two catalog servos once fused them into hand-authored solids the next time (0 catalogued). A conditional with an empty antecedent is vacuously true, and reporting that as a pass is the same class of lie as the defect it checks for. Record it as a null result and get the evidence from an assembly that actually carries placed catalog parts.
- [scope: the two-servo leg's review at its initial solved pose | confidence: high | evidence: proud-beacon-8002, fond-star-1809] A 5 mm gap between each servo and the link its ideal revolute joint drives passes clearance and simulates, so a clean rollout and a clean clearance row do not prove a physical drive interface; the review names the gap and a person — or the next design turn, told the finding — must close it, which the sixth walk did with a printed shaft boss at 6.1e-16 mm. The bound-agreement checker asserts AABB consistency across three independent surfaces, a necessary check, not an independent validation of each OCCT volume; and the walk's STL exports are local-frame source solids, so they cannot serve as a world-placed third surface.
- [scope: the installed bundle between commit `cf3b77a6` and the 2026-09-08 refresh | confidence: high | evidence: light-timber-5868, morning-summit-7848] Was stale by one Python file and reported the pre-fix clearance numbers under `--engine <bundle>`. Closed by the refresh; the worker is now byte-identical across source, payload and bundle. A Python-only engine change still needs `build-shell` (which stages) plus the install script before the bundle sees it.

- [scope: reading a historical `review.json`'s inventory block as named evidence | confidence: high | evidence: soft-crane-2369] It carries counts and a path only, and that path points at a document later `cadex inventory` calls overwrite — so an old review names no component, and its link may describe a different revision than the run it belongs to. Source-read first, then measured by rename. The named rows are recoverable from the project's Git history, not from the review; other review blocks do carry names and revisions, so this is a limit of the inventory block specifically, not of saved reviews generally.

- [scope: an offending clearance pair on an agent-authored design | confidence: high | evidence: chilly-crest-2100] The volume is a measurement and not a diagnosis. The quill lift's first-ever offending pair on an agent-authored assembly (`housing`/`quill`, 960 mm³ at 0.0 mm) is intended — the shaft is modelled inside a solid bore, the joint constrains it and the collision groups are disjoint — and the project's own ADR says so. Read the project's `DECISIONS.md` before inferring a cause from the arithmetic; the first reading here (a head block 2 mm through the column) was wrong and had to be corrected in the documents.
- [scope: reporting how far a mechanism travelled in a rollout | confidence: high | evidence: square-bay-3436] A displacement-only travel figure is a *wrong* answer, not a partial one. Measured on the repository's own examples: the `hinged-arm` swing travels **0.0000 mm** and rotates **178.8334°**, while the `linear-carriage` slide travels **4739.3783 mm** and rotates **0°**. A millimetre-only row therefore calls a working revolute rig motionless and a free fall the run's biggest mover — both readings exactly inverted from what a reader wants. Report both channels or neither. And no scale reconciles them: any single ordering ranks a carriage falling 4,739 mm on an ideal unlimited guide above a swing arm doing its job, so travel is a fact about one rollout and never a score, neither across channels nor across projects.

## Provenance

- empty-wolf-3962 — operator-declared charter gap
- modest-summit-8554 — nt3 operator directive re-seeds the criterion unticked; the four calls sit on the medium rung
- fair-rose-5950 — ADR-236: the assembly inventory with catalog ids as `inspect scope="inventory"` plus `cadex inventory` writing docs/inventory.md; real-engine qualified; three calls and the review-step wiring remain
- lawful-ivy-4474 — fixes the inventory paging critic rejection with real inspection-pager regressions; remaining review calls and walk wiring stay open

- happy-dune-3481 — inventory report and review metadata committed by the walk; arm/carriage and CPU stand-in parity verified
- falling-birch-1503 — ADR-237: complete initial-pose clearance/intersection reporting; bounded recipe rebuild cost and packaged gates verified; remaining review work stays open
- clear-ash-2884 — ADR-238: clearance findings, explicit unknowns and comparable counts committed by the walk; full CLI gate and fresh public recipes pass
- zesty-aspen-6846 — ADR-239: background refusal measured; accepted-tessellation CPU route selected with four inspected SVG probes; product rendering and sections remain open
- brave-delta-4193 — fresh bounded arm walk reproduces committed inventory/clearance and verified rollout; CLI 161 passed without skips
- silver-key-8483 — bounded named-angle CPU render delivered and visually inspected; CLI 180 passed without skips; walk integration and sections remain open
- square-path-1173 — walk integrates committed revision-bearing previews; arm/carriage and CPU stand-in parity pass; sections remain unavailable
- modest-sun-6068 — separate fresh arm rehearsal verifies committed previews, preserved clearance and reward; full CLI gate 182 passed with zero skips
- placid-lily-5624 — named-plane cavity-preserving section command and built-engine qualification
- copper-timber-8947 — shared-snapshot walk sections and both-mechanism CPU parity; separate complete-review rehearsal remains open

- first-branch-9614 — fresh arm complete-review rehearsal, inspected artifacts and committed identity audit; CLI 195 passed without skips
- quiet-vine-3426 — fresh carriage completes the paired rehearsal with committed comparisons; evidence supports working status within initial-pose and toy-policy limits
- fair-cedar-7455 — cold public review matches committed baseline identity, geometry and SVG bytes with truthful contact counts; no persistence defect
- strong-raven-3067 — refreshed installed-engine walk produces inspected views, interior section, truthful inventory and named clearance with committed project bytes
- tiny-haven-0347 — resolver-only probe bounds the payload schema check; review evidence untouched
- keen-pebble-3574 — payload guidance corrected; installed-engine CLI gate 195 passed, zero skips; criterion preserved
- empty-banner-7438 — pan-tilt walk on catalog parts: views, section and inventory (first catalog ids) verified against STL bounds and MJCF inertials; clearance shown measuring lib.*-placed bodies in the origin frame, two false intersections and one 10x volume
- light-timber-5868 — ADR-241: pair clearance composes component and shape placements; real-kernel regression fails old / passes new; engine 2076 passed, CLI 195 no skips; pan-tilt numbers corrected and three hidden contacts revealed; swept check and installed bundle still carry the defect
- southern-otter-5999 — ADR-242: the swept check composes the frames once per component outside the loop; measured no slower (0.429→0.374 / 3.00→3.04 ms/frame); real-kernel regression 36.674 mm rest / 0.0 mm breach where pre-fix reads 33.0 mm and no breach; engine 2077/52, CLI 195, payload rebuilt and staged
- morning-summit-7848 — the refreshed .app carries both fixes (worker byte-identical, lifecycle gate 15); a fourth pan-tilt walk was a null result for the catalog path (servos fused, 0 catalogued); the lib-placed pan-tilt assembly through the same bundle passes 20/20 bound comparisons against section, render and STL, with the pre-fix rows failing 10
- odd-ridge-9607 — accepted pan-tilt outputs contain neither fused servo nor catalog stamp; output-only diagnostic infeasible; inherited tally unverified
- snowy-cove-0032 — audited tally semantics and bounded a separate dependency-path discovery experiment; no diagnostic qualified
- windy-dune-3488 — ADR-243 removes unsupported tally, preserves repeated placed catalog instances and passes full engine/CLI plus fresh packaged gates; bundle remains unrefreshed
- misty-tide-6394 — script-plus-inventory guidance delivered; no purchase inference, fused-identity claim or fresh compliance qualification
- early-gate-3510 — three real-kernel controls bound dependency roles without altering placed counts; historical pan-tilt cold identity and occurrence provenance remain unavailable; no implementation authorized
- proud-beacon-8002 — fresh two-servo leg review on the qualified payload: four views and the XZ section inspected, 6 of 10 components catalogued and separately placed by script tally, 45 pairs / 11 offending / 0 unknown naming shin/bolt interference and 5 mm servo/link gaps, 90/90 bound comparisons with 39 catalog pairs
- fond-star-1809 — the same leg re-reviewed after its findings were designed out: four views and the XZ section inspected at the accepted revision, 45 pairs / 9 offending all zero-volume face contacts / 0 unknown, 90/90 bound comparisons at 1e-3 mm with a 3.05e-6 mm worst discrepancy; the agent wrote the actuator and sensor notes
- lucid-snow-0350 — inspected named previews and section metadata, truthful inventory/contact finding, matching rollout identity
- scarlet-journey-0203 — scratch index repair preserves tracked review artifacts byte-for-byte without rerunning review
- humble-forest-6896 — the ot4 operator directive re-seeds the criterion unticked as the second half of this run's frontier
- open-hollow-2140 — the ot4 machine's first walk never reached its review leg, so none of the four calls had been exercised there; superseded by the clean walk below
- wandering-jasper-6102 — the clean ot4 walk's review step exercises all four eyes on this machine in one run: render front/top/right/iso over 13,432 triangles, section XZ at Y = 3.125 mm, inventory 10 components / 7 catalogued, clearance bounds check pass over 90 comparisons across 45 pairs
- mellow-quartz-8093 — The wider-carriage iterate has consistent initial-pose review evidence
- still-harvest-7242 — guide corrected to distinguish child progress rows from successful walk clearance review; CLI 223 passed, no skips
- floral-canyon-9424 — carriage iterate guide shortened while preserving command, comparison and review limits; CLI 223 passed, no skips
- placid-ember-6741 — successful CPU recovery pinned in the real-engine regression, including preserved history and all four review outputs; CLI 223 passed, zero skips
- floral-arrow-7365 — explicit-CPU rehearsal completes named views, XZ section, inventory and expected touching-pair clearance within initial-pose limits
- soft-crane-2369 — ADR-254: the saved review's inventory block is counts-only while the linked docs/inventory.md is overwritten; measured by rename after a public toy walk, guide and scaffold clarified, runtime unchanged
- square-bay-3436 — ADR-259: a motion block in review.json, and the same two figures in the PROGRESS row, report per-component travel in millimetres and degrees from the first solved frame, ranking neither; four regressions on the documented examples' real frames; CLI gate 231 passed, 0 skipped
- chilly-crest-2100 — all four eyes on a third mechanism (render 1,404 triangles / 0.49 s, section XZ ok, inventory 2 components 0 catalogued, clearance 1 offending pair); the ADR-256 documentation eye's first read of a real design turn's unprompted notes (docs notes 2, none missing) and the ADR-259 motion block's first two-channel report on a joint offering both (20.28 mm / 0°); the offending pair is deliberate per the project's own ADR-005
