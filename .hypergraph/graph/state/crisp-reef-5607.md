---
node_id: f9d8b739-9a02-5f30-89eb-3a1cb1a32b7a
slug: crisp-reef-5607
title: The walk exists and is tested headlessly
created_at: '2026-09-06T19:18:33+00:00'
parents:
- nimble-pine-0740
summary: ''
---
Status: working

## Current

Charter criterion: **The walk exists and is tested headlessly.** One documented entry point (a CLI prompt or a headless script) takes a mechanism from design → assembly → MJCF → task → toy-scale local CPU training → policy verify → rollout → review, **on this machine**, with no human step. Declared target `gap-walk-exists-tested-headlessly-one` [rec: empty-wolf-3962]. Re-seeded unticked by the nt3 directive [rec: modest-summit-8554] and again by the ot4 directive [rec: humble-forest-6896].

**Closed for ot4: the walk runs end to end on this run's machine.** The criterion says "on this machine", and ot4 moved to `sb1x` — Ubuntu 24.04, RTX 5090, 32 cores — where the first `cadex walk --prompt` **failed, exit 3, after 1:06:25**. Two legs needed a person and both are now fixed [rec: open-hollow-2140]:

1. **The model.** The design leg refused in 2.4 s: the default `claude-fable-5` is out of usage credit on this login. ADR-249 makes `--model` read `$CADEX_MODEL` before `DEFAULT_MODEL` (`chilly-union-8972`) [rec: open-hollow-2140].
2. **The training layer.** The design agent built the rig and then gated its whole training layer behind `policy_on=0`, because `assembly.mjcf` and `assembly.dynamics` never returned (four 300 s SIGXCPU worker kills), so `train` exited 3 with *the accepted revision exports no training task*. ADR-250 fixed it: the stall was **not** in the rig's dynamics declarations but in `import numpy` under `import mujoco`, where OpenBLAS reserves 4.4 GB of address space sized by the host's core count. The walk's own accepted script at `policy_on=1` went **300.0 s / exit 3 → 2.0 s** and its full dynamics layer accepts in 1.2 s; owned by `salty-isle-4063` and `forest-wind-0342` [rec: lawful-wolf-9205].

**The clean run.** With both fixes installed, `cadex walk --prompt` took a one-servo swing-arm rig from a sentence to a verified policy with **no human step past documented flags**: **exit 0, 17:43 wall clock, 2,640 MB peak RSS**, into a durable project outside this repository. Legs, all exit 0: design 1,014.2 s (one turn), train 38.3 s, declare 2.2 s, rollout 2.4 s; `walk_seconds` 1,063.1 through review. Training was local CPU inside the run's bound — 5 iterations × 16 envs, 8.7 s, 4,673 parameters, reward/step **-0.1254**, witness error 7.2e-09 against a 1e-4 tolerance — and the rollout the engine verified scored `total_reward` **-0.1765** over 4 legs. The review step used **all four headless eyes** (tracked on `damp-moon-9297`), and the project landed as a codebase: five commits, five `PROGRESS.md` rows, `docs/{actuators,clearance,inventory,sensors}.md`, all written by the walk's own child commands. `pixi run python -m pytest cli/tests` recorded **217 passed, 0 skipped**, with `test_walk.py`'s two real walks against the real engine and trainer not skipping on this machine [rec: wandering-jasper-6102].

Two decisions the run took without a person, both documented mechanisms rather than workarounds: `--model claude-opus-5` through `$CADEX_MODEL` after probing that `claude-fable-5` is still out of credit on this login, and toy scale at 5 × 16 rather than the 200 × 256 default, which is what the criterion asks for and what the 15-minute run bound allows. The `swing.cxpolicy` ordering error the bisect had seen did **not** fire in the walk: the train leg writes the asset before the policy leg names it, which is exactly the ordering the walk exists to get right [rec: wandering-jasper-6102].

Reconcile judgement: `open` → `working`. Every leg the criterion names — design → assembly → MJCF → task → toy-scale local CPU training → policy verify → rollout → review — ran unattended and exited 0 on this run's machine, which is the evidence the reopening asked for. Neither record declares the flip; both leave it to this pass, and `wandering-jasper-6102` states plainly that no leg still needs a person. Not claimed by that flip, and since resolved elsewhere in this same pass: `swift-dusk-2951`'s second mechanism, which the carriage below supplies [rec: rare-cliff-9595], and `witty-spark-2613`'s currency audit of the GUI-attached and remote documents [rec: early-quill-3654]. What was still unproven then is the repair turn on a live `--resume` conversation; it ran on 2026-09-08 and is recorded below [rec: humble-star-3313]. None of the three needed a code change this run has found [rec: wandering-jasper-6102] [rec: lawful-wolf-9205].

**Corroborated by a second unattended completion on this machine (2026-09-08).** The same documented entry point ran end to end a second time, on a mechanism it had not seen — a prismatic/force-motor vertical carriage — with **no build and no retry**: exit 0, 5:59.8, 1,721 MB peak, four legs and all four review eyes, `cli/tests` 217 passed 0 skipped. Prerequisites were checked rather than assumed: the tree was clean, and the installed runtime already carried ADR-250's OpenBLAS pin (`grep -c OPENBLAS_NUM_THREADS build/release/Mod/cadex/CadexScriptedRuntime.py` → 1), so no rebuild was needed. Reconcile judgement: retain `working` — corroboration that the two fixes hold across mechanisms on this box, adding nothing the criterion still needed. The mechanism evidence itself belongs to `swift-dusk-2951` [rec: rare-cliff-9595].

**Six clean prompt walks on the nt3 machine, on five topologies, with no mechanism-specific code** [rec: cool-fountain-2483] [rec: placid-sky-7374] [rec: empty-banner-7438] [rec: morning-summit-7848] [rec: proud-beacon-8002] [rec: fond-star-1809]:

| prompt walk | mechanism, joint, actuator | design | train | declare | rollout | wall | peak RSS | `total_reward` | witness err |
|---|---|---|---|---|---|---|---|---|---|
| nt3b [rec: cool-fountain-2483] | pendulum rig, revolute, position servo | 262.03 s | 14.50 s | 1.43 s | 1.76 s | 280.7 s | 1.23 GB | 425.997 | 4.48e-09 |
| nt3c [rec: placid-sky-7374] | vertical carriage, prismatic, bounded force motor | 172.59 s | 13.06 s | 1.25 s | 1.50 s | 189.4 s | 1.115 GB | 0.0035556 | 3.679e-09 |
| nt3 pan-tilt [rec: empty-banner-7438] | pan-tilt head, two revolute in series, two catalog MG90S position servos | 347.25 s | 17.49 s | 1.54 s | 1.74 s | 371.70 s | 1.419 GB | -2380.357 | 4.028e-09 |
| nt3 pan-tilt, refreshed bundle [rec: morning-summit-7848] | pan-tilt head, same prompt; two MG90S bodies fused into hand-authored solids this time | 374.52 s | 24.65 s | 1.52 s | 1.72 s | 406.33 s | 1.750 GB | -237.568 | 2.882e-09 |
| nt3 two-servo leg, qualified payload [rec: proud-beacon-8002] | hip-and-knee leg, two revolute in series, two catalog MG90S position servos placed separately | 539.06 s | 19.13 s | 1.82 s | 2.09 s | 565.72 s | 1.271 GB | -5144.792 | 3.758e-09 |
| nt3 two-servo leg, durable project, findings as inputs [rec: fond-star-1809] | same leg, same payload; prompt names the previous review's three findings; six catalog instances placed separately | 599.74 s | 20.03 s | 2.33 s | 2.69 s | 630.22 s | 1.116 GB | 0.853045 | not recorded |

All at 1 iteration × 4 environments (seeds 0, 1, 0, 0, 0 and 0), `--timeout 600`, under the charter's guards (0.2 s process-tree RSS sampling, 2.9 GB and 850 s cutoffs). `review.json` (`cadex-walk-review-v1`) landed in each project beside `ARCHITECTURE.md`, `DECISIONS.md` and `PROGRESS.md`. **No number here is a claim about learned control**: one PPO iteration is a smoke test of the loop, which is what the criterion asks for. Across nt3 the ratio was six clean of seven attempts, the one failure older than both walk fixes below. Design turns are nondeterministic and unprompted-capable: nt3c wrote its own inline-literal `assembly.policy(...)` call with `policy_on` and recorded three `DECISION:` lines as project ADRs [rec: placid-sky-7374]; the sixth walk left `docs/actuators.md` and `docs/sensors.md` behind — a torque-limited position-servo model from the MG90S 4.8 V datasheet, six sensor channels — which is the first live evidence for ADR-245 [rec: fond-star-1809].

**The entry point itself.** `cadex walk` is the documented headless entry point since ADR-199 (commit a4248b26; `docs/CLI.md` walk section, `docs/MUJOCO.md` §7c, ROADMAP tick): two real walks on the plate-and-arm toy from an existing script, `review.json` under `--out`, the scaffolded `.gitignore` keeping checkpoints, `train/` copies and `*-trace.json` out of the project's history while `assets/*.cxpolicy` stays committed; `cli/tests/test_walk.py` pins the digest edit, review reader, leg order and flags plus both real walks [rec: shy-cabin-0798]. Both `examples/lifecycle` recipes ran clean in nt3 (hinged-arm 15.3 s, 1.03 GB, -27.109384; linear-carriage 13.1 s, 0.98 GB, -24159.195), and commit `64b92b29` made `docs/CLI.md` state all three counts the digest edit refuses on [rec: misty-rain-9048].

**The two legs the first prompt run named are closed** — both were the walk's own defects, not the model's [rec: misty-rain-9048]:

1. **The digest edit no longer refuses an agent-authored script** (commit 7d089bff) [rec: cool-fountain-2483]. `CLI_OVERLAY` in `cli/cadex_cli/agent.py` teaches `weights=` and `sha256=` as inline literals at the call site, names constant-factoring as the mistake, and marks the call as the one exception to the parametric rule; `test_turn_loop.py` pins the contract text and `test_walk.py` refuses the constant-factored script on both keywords, then feeds the prompt's own example through `walk._replace_keyword` byte for byte. `declare_policy` is unchanged by decision.
2. **A failed design leg now names its cause** (commit dca63b13) [rec: ancient-wind-0117]. The `accepted is None` branch in `cli/cadex_cli/__main__.py` reports the last engine refusal (op, failure code, clipped message) or "the engine refused nothing" with the ops called, plus the agent's closing words, each clipped to 400 characters; `run_leg` already carries a child envelope's `error` into the walk envelope, so no `walk.py` change. Three stubbed-turn tests pin it. No automatic retry, no leg-order change, no `OP_ARG_SPECS` change.

**Deployment and persistence, qualified on the nt3 machine.** The installed-bundle route passes ordinarily: one stale-API refusal was resolved by supported staging, one shell build and one install with no source fix, after which a hinged-arm walk ran in 16.04 s at 1.055 GB, and the same-root packaged lifecycle (15), full CLI (195) and background shell gates all passed with no skips [rec: northern-hill-9362] [rec: strong-raven-3067]. Cold revisits preserve accepted revision, digest, policy assets and a byte-identical restored trace across six separate public CLI commands [rec: fair-cedar-7455]. Engine-selection guidance is now bounded to what it verifies — a nonempty manifest/module schema mismatch and nothing more (commit c84fd306) [rec: tiny-haven-0347] [rec: keen-pebble-3574]. `VISION.md` principles 5 and 3 were corrected to agree with the supported CPU walk, prose only [rec: polished-moss-9358] [rec: neat-summit-3586]. The worker snapshot boundary passed full engine (2085 passed, 52 skipped), built-engine CLI (195) and packaged lifecycle (15) gates with every staged worker member byte-matching the payload [rec: lawful-dune-3795].

**Two conveniences landed on top of the walk, both regression-verified only.** ADR-246 makes progress rows and project commit subjects use a project-relative output path (or the basename for external outputs); four regressions failed on the old formatter, the full CLI run recorded 200 passed with one stale absolute-path assertion, and the corrected real toy walk then passed alone [rec: twilight-snow-9071]. ADR-247 suppresses the unchanged-session `agent.json` timestamp rewrite while keeping truthful restore bookkeeping; three real-engine refusal regressions and two acceptance regressions cover unchanged identity, changed session/model and preserved user work, at 206 CLI tests with zero skips [rec: vast-ledge-4610] [rec: ready-otter-2700].

**Provider availability was the recurring nt3 blocker, and one thing it left unproven.** Two attempts stopped at the design leg on session-limit and usage-credit refusals with nothing accepted [rec: silent-mist-5233] [rec: sunny-walrus-5847], and one dispatch deferred rather than probe [rec: calm-beacon-7800]. What survived those is the gap ADR-247's offline tests do not close: **a repair turn on a live conversation** — a second design turn through `--resume` that iterates from the run's own review rather than from a prompt told the answers — had never run. The durable project existed to do it on, and that is where it ran — see the ADR-271 paragraph below [rec: fond-star-1809] [rec: humble-star-3313]. Provider-free downstream rehearsals continue to pass (hinged-arm 15.66 s, 0.987 GiB, witness 1.384e-09, rollout -27.109384) [rec: lucid-snow-0350], and a scratch project preinitialized outside the first-visit scaffolding had its index repaired forward — 33 generated paths ignored and untracked, all local bytes preserved, stored policy and review evidence still tracked [rec: scarlet-journey-0203].

**Walk engine/source evidence is now reported before the first leg (ADR-251).** JSON and stderr distinguish `match`, `different` and `unavailable` for top-level installed/payload Python bytes versus source, with full counts and at most ten names per list. Reporting changes neither selection nor refusal and proves no binary currency or loaded-module provenance. CLI gate: 223 passed without skips; all three new walk cases fail on the old source at the missing report key. Retain `working`: this qualifies reporting without independently repeating the full lifecycle. [rec: narrow-wing-0418]

**Failed retraining and successful recovery are regression-qualified after two successful toy walks.** Injected trainer exit 7 leaves the accepted sweep and prior successful artifacts/history intact [rec: dawn-sky-3891]. A public CPU retry from that retained sweep, using the last successful policy/task, a new policy name and fresh output directory, completed verification, rollout and all four reviews without a production fix [rec: dusty-vale-2809]. The real-engine regression now replaces its export-only ending with that successful retry, explicitly pins CPU, and passes the final full CLI gate: **223 passed, zero skips**. Reconcile judgement: retain `working`; the finite recovery sequence is covered, without demonstrating control improvement, live conversation repair, GUI or actual remote execution. [rec: placid-ember-6741]

**Explicit CPU verification is documented, shared, and re-walked (ADR-253).** The public hinged-arm walk selects `JAX_PLATFORMS=cpu` in the discovered CUDA-capable trainer venv; setup, CLI guidance and the project scaffold state the CPU contract, and the real train, iterate, walk/recovery and local dispatcher stand-in tests request one `cpu_training` fixture and assert CPU receipts [rec: floral-arrow-7365] [rec: keen-field-4379]. Walked again to carry the review-inventory measurement, it held: exit 0 in 14.6115 s at 1,540,816,896 bytes peak aggregate RSS, CPU trainer receipt 1.2591 s, reward/step -0.38019815, witness error 2.07e-9 against 1e-4, verified rollout `total_reward` -27.10938422, and all four review eyes (four nonempty named renders, an `ok` XZ section, two uncatalogued components, one touching base/swing pair at zero common volume). The follow-on component rename used the documented iterate switch rather than a retrain: at `policy_on=1` the script submission correctly refused a changed task digest, and accepted after `params --set policy_on=0`. `pixi run python -m pytest cli/tests`: **223 passed, 0 skipped**, no outer backend override [rec: soft-crane-2369]. Reconcile judgement: retain `working` — the finite CPU verification direction is complete and corroborated on this machine, with production backend defaults unchanged; the inventory boundary itself is tracked on `damp-moon-9297`, and nothing here is new evidence about control, live-conversation repair, GUI or actual remote dispatch.

**The review step gains a fifth eye: the domain-note convention is read back, and documented where a project's own agent reads it (ADR-256).** After the rollout, `cadex walk` parses the MJCF it actually trained on (`DIR/train/<name>-model.xml`) and turns each declared section into a note subject — an `<actuator>` section with children asks the project for `docs/actuators.md`, a `<sensor>` section for `docs/sensors.md`. `review.json` gains a `documentation` block (`notes`, `expected`, `missing`, and the model read, relative to `DIR`) and the `PROGRESS.md` row carries the same finding after its clearance half (`docs notes N, none missing` / `docs notes N, no actuators`). It **reports and never writes**: a missing note is a finding for the next design turn, the way an offending clearance pair is a finding rather than a walk failure (ADR-238), because a CLI-authored `docs/actuators.md` would be invented content pasted back into the next turn as if it were knowledge. Reading from the exported model rather than from the script is deliberate — the script is what was asked for, the model is what ran, so a declaration behind a switch that evaluated false asks for no note. On the real toy walk the mechanism declares both subjects, the project carries only `docs/sensors.md`, and `review.json`, the progress row and the run notes all name `actuators` as the one with no note [rec: eager-lake-5745]. The finding is then explained **in the project**, not only in this repo's `docs/CLI.md`: the generated `ARCHITECTURE.md` scaffold's "Domain docs" section names the declaration-to-note mapping, both landing places, the two row forms, and the never-fails/never-writes rule, worded from `docs/CLI.md` §2 and ADR-256 rather than reworded so the three descriptions of one behaviour do not drift; the scaffold test the critic named gained four assertions [rec: chilly-glacier-9294]. `cli/` and `docs/` only, no engine, protocol, payload or `shell/` diff; CLI gate **227 passed, 0 skipped** on both commits, real engine and real CPU trainer. Reconcile judgement: retain `working` — this adds a review eye and its in-project documentation without repeating the lifecycle, and proves nothing new about control, live-conversation repair, GUI or actual remote dispatch.

**The documented *example* path was reproduced here too, and it named two defects (2026-09-08).** Both repository-owned lifecycle examples went through the commands `examples/lifecycle/README.md` tells a reader to run, unchanged: hinged arm exit 0 at `walk_seconds` 14.40, linear carriage exit 0 at 13.10, no code change of any kind, diff documentation-only. That is a different claim from the prompt walks above — it tests the entry point *as documented*, on the machine of record, and it was last measured on a different machine before the ADR-256 eye existed. The mechanism numbers belong to `swift-dusk-2951` [rec: western-gate-9567].

Two defects it exposed, both fixed in the same unit [rec: western-gate-9567] [rec: blue-quill-9477]:

1. **The reproduction command could not run here.** The README hard-coded `--trainer-python "$PWD/.venv/bin/python"`, a path that does not exist on this machine. It is now flagless, relying on the CLI's documented venv discovery order — and the claim is bounded to what was measured: **one** carriage walk ran the flagless form (same `total_reward` -24159.195371510654, 13.19 s), while the two rows in the tables were produced with the interpreter named explicitly. Both `PROGRESS.md` reproduction sections, the README's reproduction table and ADR-257 now say which walk evidences which form; the numbers were never in question, because the same interpreter binary ran either way [rec: blue-quill-9477].
2. **The example projects did not keep the domain note their own review asks for.** The ADR-256 eye reported `0 domain note(s) for 2 declared subject(s)` on both reproduced projects — correct for a recipe walk, which runs no design turn, and the README now says so. But the example *directories* carried only `docs/sensors.md`, so the convention their own review declares was half kept; both gained `docs/actuators.md` written from the exported MJCF rather than from the prose. The fix was a documented convention, not a CLI change that would copy sibling files into a project [rec: western-gate-9567].

Reconcile judgement: retain `working`. A dead command in the documented reproduction path is a defect in the *documentation* of a criterion that says "one documented entry point", so fixing it strengthens the existing claim rather than adding a new one; nothing here is new evidence about control, live-conversation repair, GUI or actual remote dispatch [rec: western-gate-9567] [rec: blue-quill-9477].

**The walk's review now says whether the mechanism moved (ADR-259, commit `19549da9`, 2026-09-08).** Every review block above could report what a rollout scored and whether its parts collided, but not whether anything travelled — so a retrain that killed the motion and a retrain that merely scored worse read identically. `review.json` (`cadex-walk-review-v1`) gains a `motion` block beside `clearance`, carrying per component the per-axis `position_range_mm`, the `max_displacement_mm` from the reference pose and the `max_rotation_deg` swing `2·acos(|q₀·q|)` away from the reference orientation. It names `largest_translation` in millimetres and `largest_rotation` in degrees and **declines to rank one against the other**, saying so in a `ranking` field; the `PROGRESS.md` row and the run notes carry the same two figures (`motion N mm (component), N° (component) over N solved frame(s)`). Implementation is `motion_from_trace()` in `cli/cadex_cli/walk.py`, called from the existing `review_from_outputs()` so the trace is opened once, serialized by the existing `write_review()`, plus `_motion_cell()` in `__main__.py` — no new trace locator and no engine, protocol, payload or `shell/` diff. `pixi run python -m pytest cli/tests`: **231 passed, 0 skipped**, with no backend override, so the engine-needing half ran; the real-engine lifecycle test reports the hinged arm at `motion 0 mm (swing), 178.8° (swing) over 26 solved frame(s)` — the measured number, through the whole walk [rec: square-bay-3436].

Two things stated rather than left implicit. The reference is the **first solved frame** and only solved frames are counted: frame 0 carries `frame_kind: "input"` and `nominal_time_s: null` — the pose the solver was *given* — so counting it would report an input as an output, and the block publishes `frames_counted`, `frames_excluded` and `excluded_frame_kind` so its 26 is never mistaken for the raw 27. And one defect the change found rather than added: the first full run failed on an *existing* assertion (`docs notes 1, no actuators`) because `PROGRESS_NUMBERS_LIMIT` capped the progress row's numbers cell at 160 characters and the motion cell pushed the ADR-256 documentation finding off the end as `docs…`. Raising the cap to 320 is part of the same commit; without it this unit would have silently dropped a finding the walk already reported [rec: square-bay-3436].

Reconcile judgement: retain `working`. This adds a review channel and its regressions without repeating the lifecycle, and proves nothing new about control, live-conversation repair, GUI or actual remote dispatch; the review-surface half is tracked on `damp-moon-9297`. Its one open item — `motion` in the walk's review but not in the iterate comparison — is closed by ADR-260 below. One line of that record is **not** folded because this node already contradicts it: it closes by saying this criterion "still wants a clean end-to-end walk run recorded on this machine", which `wandering-jasper-6102` and `rare-cliff-9595` supplied and which this node has carried since [rec: square-bay-3436].

**The walk's `PROGRESS.md` row now carries a delta, and travel is spelled so it can (ADR-260, commit `2b0c1678`, 2026-09-08).** The measurement behind it is the useful half: **no walk row had ever carried a delta for any figure.** `_record_progress` in `cli/cadex_cli/__main__.py` branches on the command, and only the non-walk branch called `progress_numbers(..., previous=...)`; it now reads `previous_numbers()` once, before the row is appended, for both branches. Compounding it, ADR-259 spelled the travel `motion 0 mm (swing), 178.8° (swing)`, which `_NUMBER_RE` — built from `COMPARED_NUMBERS` as `<label> <number>` — cannot parse, so registering the labels alone would have read nothing back. `_motion_cell` now spells the two channels `travel_mm N on <component>, travel_deg N on <component>` through `compared_number`; `COMPARED_NUMBERS` gains `travel_mm` and `travel_deg`; `_compared` became the public `compared_number` and its per-label spelling the new `spelled_number` (one decimal for `total_reward`, four significant figures otherwise). The run note is the same cell with its prefix stripped, so there is one spelling to learn and the note carries the same delta; `review.json` is untouched and keeps `millimetres` and `degrees` under their own keys. The worked example is the linear carriage: baseline 103.298 mm travel at `total_reward` 3.296298, iterate 103.719 mm at 2.760187 — the travel held while the reward fell, which is the exact thing an iterate run exists to notice and which the walk could not say. Two assumptions written down rather than asked: **a delta is not a verdict** (ADR-259's refusal to rank millimetres against degrees stands), and one spelling for note and row rather than units in the note. Precision note: the change is measured against the row **as written** (four significant figures), not the raw float, because the row is what a later run can read — so the carriage delta is `+0.419`, not `+0.421`. Pinned by an offline round-trip in `test_project_docs.py` (a walk-shaped row written, both travel labels read back, the delta text not mistaken for the next row's value), by the toy walk asserting a project's **first** walk carries no delta, and by the extended real-engine iterate lifecycle test, which now requires the second walk's row to carry `(Δ ±N vs <digest> at N)` on both channels, untruncated, still ending in its ADR-256 documentation finding — the thing `PROGRESS_NUMBERS_LIMIT` protects. `pixi run python -m pytest cli/tests`: **233 passed, 0 failed**, including the real-engine CPU-training lifecycle test; `test_project_docs.py` 24 passed. `cli/` and `docs/` only. Reconcile judgement: retain `working` — this makes the review step's *iterate* half real for the first time without repeating the lifecycle [rec: northern-comet-5917].

**A third mechanism went end to end on this machine, and named the one leg that can still outrun a caller's budget (2026-09-08).** A *quill lift* rig — one **cylindrical** joint driven by a **position servo on the linear coordinate** — went from one sentence to a verified policy through the unchanged `cadex walk --prompt` with **no code change of any kind**: exit 0, **10:52.99**, 1,946 MB peak RSS, into `~/cadex-projects/ot4-quill`, a third durable project outside this repository. Legs all exit 0 (design 629.6 s in one turn, train 19.6 s, declare 0.9 s, rollout 1.0 s; `walk_seconds` 652.9 through review), same toy scale and seed as both earlier walks (5 × 16, seed 0, `--timeout 600`, `JAX_PLATFORMS=cpu`), reward/step -0.5796, witness error 2.9e-08 against 1e-4, verified rollout `total_reward` -74.79 over 200 steps and four terms. `CADEX_MODEL=claude-opus-5` again through the ADR-249 mechanism, probed before the run; `--trainer-python` was dropped and the documented venv discovery resolved `~/cadex-train-venv` on its own. Prerequisites were checked rather than assumed — clean tree, and the walk's own `engine_source_comparison` reported `status: match` over 56 files, so no build was run. Gate: `JAX_PLATFORMS=cpu pixi run python -m pytest cli/tests` **234 passed, 0 skipped**, `test_walk.py`'s real-engine real-trainer walks not skipping; the repository diff was two documents (`docs/ROADMAP.md`, `docs/CLI.md`) and nothing generated entered this repository. Three things this walk reported that the plan asked for: **both travel channels on the same component** (`travel_mm 20.28`, `travel_deg 0`, `quill_component`, 201 solved frames — the joint offers both and the rollout used one, because gravity exerts no torque about a vertical axis); **the delta did not render, and could not**, this being the project's first walk, so ADR-260's threading is exercised by its offline regressions rather than here; and the **documentation eye read a real design turn's own unprompted notes back for the first time** — `docs notes 2, none missing`, both substantive (the servo's 5 N/mm stiffness, near-critical 0.03 N·s/mm damping with the arithmetic, a 20 N limit against a 0.27 N static weight, why the rotation coordinate is deliberately unactuated; six sensor channels with units and which one drives termination). The mechanism numbers are on `swift-dusk-2951` and the review surface on `damp-moon-9297`. Reconcile judgement: retain `working` — a third unattended completion corroborates the criterion without adding a leg it did not already have. **What the record newly names against this node**: the walk still spends an **unbounded design turn** — 629.6 s here, 1,014.2 s on the swing arm — with no `--timeout` of its own on that leg, which is the one place the documented entry point can outrun a caller's budget [rec: chilly-crest-2100]. **That hole is closed below.**

**Every walk leg is now bounded in wall clock, and the bound kills the subtree (ADR-261, commits `dbb198e3` and `ea725ca6`, 2026-09-08).** The hole the third mechanism named was worse than it read: `run_leg` called `subprocess.run` with **no `timeout=` at all**, so *every* leg was unbounded, and the walk's own `--timeout` is forwarded only into the train leg's argv, where it is the trainer's internal limit — its help text and `docs/CLI.md` both read as though the walk were already bounded. `run_leg` now takes a `timeout` and `cadex walk` takes `--leg-timeout SECONDS` (default 3600, `0` for the old unbounded behaviour exactly) applying to **all six** legs — design, sweep, train, script, declare, rollout. A leg that runs out of time comes back as an ordinary failed leg (exit 124, reason in its envelope's `error`) and ends the walk through the existing `failed(...)` path at `EXIT_FAILURE`; it is not a new refusal semantic. `train_leg_timeout` gives the train leg `max(--leg-timeout, --timeout + 300 s)`, and the envelope carries `walk.leg_timeout_s` and `walk.train_leg_timeout_s`. An hour is ~3.5x the longest leg that has ever legitimately run here (629.6 s and 1,014.2 s design turns). `cli/**` only, LGPL zone; no engine, protocol, payload or `shell/` diff. `cli/tests/test_walk.py` 42 passed including the real-engine and real-trainer walks; full `cli/tests` **238 passed** [rec: honest-river-1137].

**Subtree termination now preserves the full cleanup grace.** The first fix captured the process-group id while the child was alive, made group SIGKILL unconditional and bounded the final pipe drain to 10 seconds; its stubborn-grandchild regression failed on the old implementation and the CLI suite passed 239 tests [rec: tidy-cove-8382]. That implementation still shortened descendants' grace when their direct parent exited quickly. A monotonic deadline now preserves the remaining grace before unconditional SIGKILL. A real descendant needing 0.3 seconds to write a cleanup marker fails against the previous code and passes with the full one-second test grace; the complete CLI suite passes **240 tests, no skips**, in 226.73 seconds [rec: lucky-creek-4303].

Reconcile judgement: retain `working`; the cleanup correction strengthens the existing bounded-walk claim without claiming a new end-to-end rehearsal or control-quality evidence [rec: lucky-creek-4303].

**Resumed-turn history is regression-qualified.** Two scripted model turns through the real engine and bridge preserve session/project identity, deliver current architecture, decisions, domain notes and between-visit sensor/progress edits, then accept changed geometry and append notes without losing prior text. Removing project-doc delivery makes the regression fail. This proves context delivery and persistence, not model understanding or live Claude resume; the provider-guided geometry/train/review iterate remains unmeasured. No runtime change; CLI gate: **245 passed, no skips** [rec: humble-sky-8445].

**The fixed-stroke quill seed measurement is complete.** The retained stroke-60 reference uses CPU 5 × 16 training, training seed 0 and rollout seed 7; its parameters, MJCF and full task match the earlier stroke-60 run, with independently verified objective/action identities [rec: floral-grotto-1463]. Exactly three further walks, training seeds 1–3 at the same inputs and rollout seed, pass all nine legs in 23.78–25.47 s each at less than 2 GB peak tree RSS. Prior artifacts and project history survive, with new generated outputs excluded throughout the new commits. Recorded CLI gate: **245 passed, no skips**. Retain `working`: this finite seed direction is exhausted; it supplies lifecycle repeatability evidence, not learned control or live provider understanding [rec: honest-banner-0821].

**The review leg's section artifact moves, and nothing else about the leg does (ADR-267, commit `749be272`, 2026-09-08).** The walk now asks its section eye for a *derived* cut rather than the literal world XZ plane at Y = 3.125 mm, so the artifact directory becomes `review/section/<accepted-revision>/XZ-<derived-offset>/` and `review.json` carries `offset_mm`, `offset_source: derived` and the ordered `offset_candidates_mm`. `docs/CLI.md`'s walk review contract, its shared-mode artifact table and the `cadex section` block, `docs/MUJOCO.md` §7c and the project-doc scaffold in `cli/cadex_cli/project_docs.py` are updated in the same commit, with the scaffold's two pins in `cli/tests/test_project_docs.py` moved with it. **The leg is otherwise unchanged**: its failure semantics, snapshot sharing and revision checks are exactly as before, `cadex section --plane/--offset-mm` remains the explicit surface (recording `offset_source: explicit`), and `test_walk.py` now asserts the walk's cut is the first derived candidate rather than 3.125. CLI gate **256 passed, 0 skipped, 0 failed** in 229.34 s; `cli/` and `docs/` only, no new flag, op, protocol change, engine or `shell/` diff. Reconcile judgement: retain `working` — the change was verified against committed run summaries and the new regressions, not by a fresh walk, so the next headless run is what shows the derived plane in a live `review.json`. The eye itself and its coverage numbers are on `damp-moon-9297` [rec: western-cliff-6876].

**The documented entry point ran twice more on this machine, token-free, and the second run carries the live derived section (2026-09-08).** Two `cadex walk` runs on `ot4-quill` with **no `--prompt`** — `runs/derived-section-41` and `runs/derived-section-41b`, identical argv apart from the output directory: seed 0, rollout seed 7, CPU 5 × 16, `--timeout 600`, `--leg-timeout 120`, under an external 0.2 s tree-RSS watchdog at 2.9 GB / 850 s that also hashed every prior run file before and after. Both **exit 0**, at **23.821580 s** and **23.830270 s**, peak tree RSS 1,980,588,032 and 1,994,387,456 bytes, with **156 and 177 prior run files byte-unchanged**. Every leg and all four review calls ran, and no tokens were spent — a `--prompt`-less walk is the cheap way to re-exercise the eyes on a real mechanism, and it is what showed ADR-267's derived section cutting live geometry for the first time. **It is coverage in objects cut, not a fifth seed and not a reward row**: nothing about the mechanism, the policy or the training changed between the two walks, and the rollout `total_reward` 175.487211145051 is identical to the seed-0 reference. The section finding itself, and the ADR-270 fix between the two runs, are on `damp-moon-9297`; the `offset_candidates` change is on `chilly-union-8972` [rec: copper-canyon-0231].

**The walk's leg list is now pinned to the code that spawns it (ADR-269, commit `46449386`, 2026-09-08).** `docs/CLI.md` §2 carries a seven-row table naming the six legs `cadex walk` spawns — `design`, `sweep`, `train`, `script`, `declare`, `rollout` — plus the review it runs in its own engine session, each with its child command and the artifacts it lands at their project-relative paths (`write_render`, `write_section`, `write_inventory`, `write_clearance`, `review.json`, the `PROGRESS.md` row, the project commit). A test in `cli/tests/test_project_docs.py` asserts the table's leg column equals the `run_leg("…")` names in `cli/cadex_cli/__main__.py` **in order**, with the in-process review last, so **a seventh leg fails the doc rather than quietly outdating it**. `cli/tests` 260 passed, 0 skipped; `cli/` and `docs/` only. The GUI-attached half of that table — what an open window changes — belongs to `witty-spark-2613`. Reconcile judgement: retain `working`; this makes an existing claim checkable and repeats no part of the lifecycle [rec: cool-eagle-6400].

**The repair turn on a live conversation has run, and it closed the finding it read (ADR-271, commit `4b5306a2`, 2026-09-08).** This node has carried "a repair turn on a live `--resume` conversation" as the one shape never exercised since `fond-star-1809`. It is exercised now, on `ot4-quill`: a real provider turn on `--resume` read the project's own `PROGRESS.md` and `DECISIONS.md`, took the `clearance offending 1 … 960 mm³` row that every walk had repeated, **contradicted the project's own ADR-005** about its cause, checked the arithmetic itself against the accepted values (head block y −32.2…−10.2 against column y −42.2…−30.2 = 2.0 × 30 × 16 = 960 mm³ exactly) and wrote project ADR-007, standing the bore axis off the column face by the head's full swept radius (25.50 mm) plus a declared `spin_clearance`, with a web tying the boss back in, under the invariant `stand_off = max(head_sweep_r + spin_clearance, housing_od/2 − embed)`. The walk that followed — `runs/live-iterate-43`, the reference command, no `--prompt` and no `--set`, seed 0, rollout seed 7, CPU 5 × 16, `--timeout 600`, `--leg-timeout 120` — came back **exit 0 in 23.618845950928517 s** (23.67 s external, peak RSS 1,945,824 KB), all three legs exit 0 (train 19.59, declare 0.97, rollout 1.07 s), and the review eye reported the finding **gone: clearance offending 1 → 0**, bounds check pass over 2 comparisons, both parts still cut by the derived section (the plane moved XZ −15.7 → 0.0 because the geometry did), inventory and motion otherwise as before. **The reward columns are not a comparison**: the exported task sha256 changed with the moved masses (`89e911f824d1…` → `42958f923436…`), so 175.487211145051 → 175.3771498711709 is two objectives, and the row's standing disclaimer applies with more force rather than less. The one code gap the run exposed is fixed in the same unit — the walk row compared its reward and travel figures against the previous walk but wrote the clearance finding count bare, so the number the iterate actually turned read as though there had never been a finding (delivery on `chilly-union-8972`). Gate `pixi run python -m pytest cli/tests` **262 passed** (was 261); no engine, protocol, payload or `shell/` diff [rec: humble-star-3313].

Two qualifications on that closure, both the record's own. The design turn had **already run and committed** at 22:57:54Z (`0741643`), in an earlier iteration killed during its train leg; this unit finished the walk that turn had started rather than spending a second provider call to re-ask a question already answered, on the stated judgement that the evidence the criterion wants is the closed loop — history read → geometry changed → review re-measured — not a second call. So the loop is closed, and **a single uninterrupted `walk --prompt … --resume` invocation, start through review, has still not been observed** [rec: humble-star-3313].

**That uninterrupted invocation was attempted next, and the provider refused (2026-09-08).** `claude usage` and a direct `claude -p` probe both returned *"You're out of usage credits."*, and the walk was run anyway so the refusal is observed through the product's own path: `runs/uninterrupted-44`, leg `design`, **exit 1**, the provider's sentence carried **verbatim** into `walk.legs[0].error`, walk exit 1, `review` empty, no further leg run. The walk reported it honestly, and what is missing is **credits, not code**. Left behind and named rather than fixed: `ot4-quill` carries two uncommitted files from the refused design leg — `script.json` re-staged (the ADR-247 bookkeeping this node already records) and, less benignly, `agent.json`'s `model` overwritten from `claude-opus-5` to the walk's default `claude-fable-5` **by a turn that never ran**. Reconcile judgement: retain `working`. The uninterrupted-invocation gap is named here rather than flipping any status, because no code defect was found and the walk's own reporting behaved exactly as ADR-261 and ADR-249 designed it to [rec: dry-rain-0489].

## Negative knowledge

- [scope: what the walk commits from a training leg | confidence: medium | evidence: shy-cabin-0798] Before ADR-199 the `train` leg's commit carried `job.cxpolicy`, `job.best.cxpolicy` and the store copy — three copies of one policy. The store's asset is the project (ADR-194); checkpoints and traces are not, and the `.gitignore` says so. Reversible per project, because the file is editable.
- [scope: `declare_policy` in `cli/cadex_cli/walk.py` against agent-authored scripts | confidence: high | evidence: misty-rain-9048, cool-fountain-2483] The rewrite handles inline `weights="…"` / `sha256="…"` literals only; a script that names them through module constants is refused at exit 3. The fix went into the authoring contract, not the rewrite: resolving constants would make the walk guess at a script it did not write. A test pins the refusal as intended behaviour.
- [scope: the first design-leg failure inside `cadex walk --prompt` on 2026-09-07 | confidence: medium | evidence: misty-rain-9048, ancient-wind-0117] One observed fast failure (3.76 s, after one `describe_api` call) with the child's reason discarded, and one identical retry that succeeded in 214 s. Its cause is unrecoverable: the envelope that would have named it landed afterwards. Not reproduced on purpose; the clean runs since [rec: placid-sky-7374] make it the outlier, not the pattern.
- [scope: what one prompt walk proves about a review surface | confidence: high | evidence: morning-summit-7848] The design turn is nondeterministic across runs of the same prompt: the pan-tilt prompt placed two catalog servos once and fused them into hand-authored solids the next time. A walk selected to exercise a catalog-part code path may not exercise it at all; check the inventory's catalogued count before reading its review numbers as evidence for anything catalog-shaped.
- [scope: comparing `total_reward` across prompt-walk projects | confidence: high | evidence: placid-sky-7374, empty-banner-7438, fond-star-1809] The reward expressions are different objectives in different units, so 425.997, 0.0035556 and -2380.357 compare runs of one project only, never designs against each other — the same caveat `examples/lifecycle/README.md` records for the two example mechanisms. The two leg walks make the same point on one mechanism: −5144.79 and 0.853 are the same leg under a negatively and a positively shaped objective.
- [scope: a walk project under the system temporary directory | confidence: high | evidence: lucid-pebble-4000, fond-star-1809] It is reaped between iterations, so its review findings cannot be written back into its own `DECISIONS.md`, and there is no conversation left to `--resume`. The findings survived only in this graph. An iterate unit must walk a durable project outside the checkout and outside `~/arch`.
- [scope: a design turn's own account of why an engine op refused | confidence: high | evidence: open-hollow-2140] Do not take it as a measurement. The ot4 rig's `docs/rejected.md` asserts that a single grounded box with one box collision exhausts the worker cap; a control in a fresh project builds that model in under a second. The turn writes what it inferred from the failure it saw, not what it isolated.
- [scope: walk evidence and the machine it ran on | confidence: high | evidence: open-hollow-2140, humble-forest-6896, lawful-wolf-9205] Six clean walks on one machine did not carry to the next one, and neither cause was in the walk. The design leg's model default was machine-specific (credit on one login, not another), and the dynamics stall was a native library sizing its allocations from the host's core count — so it needed a 32-core box to appear at all. "Tested headlessly" is a per-machine claim as long as the criterion words it that way, and the two machine-portability faults it caught were both worth catching.
- [scope: a design leg that fails in seconds carrying a provider's own sentence | confidence: high | evidence: dry-rain-0489] It is a credit refusal, not a walk defect, and the walk already reports it correctly — exit 1 on leg `design`, the sentence verbatim in `walk.legs[0].error`, no later leg run, `review` empty. But it is **not inert on the project**: the refused leg still re-stages `script.json` and still rewrites `agent.json`'s `model` to the walk's default, so a turn that never ran can change the project's recorded model out from under the next run. Check the project's `git status` after a refusal before reading the next run's metadata.

- [scope: a token-free `cadex walk` as evidence | confidence: high | evidence: copper-canyon-0231] A walk with no `--prompt` runs every leg and all four review calls for ~24 s and no tokens, which makes it the cheap way to re-exercise the eyes on a real mechanism — but it runs **no design turn**, so it is evidence about the review step and the leg plumbing and none at all about the design leg, the provider, or learned control. Its reward row is the reference's, unchanged by construction; reporting one as a new seed would be a false measurement.

- [scope: what the walk's documentation eye proves | confidence: high | evidence: eager-lake-5745, western-gate-9567] It reads the exported MJCF's declared sections, so it can only ask for notes a mechanism declares — an undeclared subject is invisible to it, and a note that exists is counted, never read for content. On a **recipe** walk it will report every declared subject missing, and that is correct rather than a defect: `script --set` installs the recipe alone and no design turn runs, so there is nothing to have written the note [rec: western-gate-9567]. **Correction (2026-09-08):** this entry previously read "no walk on this machine has yet produced a domain note from a real design turn's closing text", and that was false — `docs/actuators.md` and `docs/sensors.md` in each of `ot4-carriage`, `ot4-swing2` and `ot4-swing` were committed by a `cadex prompt:` commit, so ADR-245's write half is exercised here three times over. Only the ADR-256 read-back eye had never run on a real prompt walk [rec: sleepy-hollow-9498].
- [scope: reading a `PROGRESS.md` travel or reward delta | confidence: high | evidence: northern-comet-5917, chilly-crest-2100] The delta is computed against the previous row **as written** — four significant figures — not against the raw float, because the row is what a later run can read; the raw figure stays in `review.json`. And a project's **first** walk carries no delta on any channel by construction, so an absent delta on a fresh project is not evidence that ADR-260's threading failed: `ot4-quill`'s baseline walk reported none, correctly, and the threading is evidenced by the offline regressions instead.
- [scope: a zero in one of the two travel channels | confidence: high | evidence: chilly-crest-2100] It is a fact about that rollout, not a missing measurement. The quill lift's cylindrical joint offers both a slide and a hinge and the rollout used only the slide — `travel_mm 20.28`, `travel_deg 0` — because gravity exerts no torque about a vertical axis, so an axially offset head mass does not spin the quill. Read a zero channel against the physics before reading it as a defect in the eye.
- [scope: bounding a walk leg by signalling it | confidence: high | evidence: tidy-cove-8382, honest-river-1137] Signal the **group**, kill it **unconditionally**, and capture the pgid while the child is alive. Every leg spawns something the walk does not control (`agent.py` uses `Popen` with no timeout of its own), so a direct-child kill leaves the process that was actually hanging alive, still holding the inherited stdout — and any drain after the stop must itself be bounded or it blocks on that pipe forever. Waiting on the direct child to decide whether to escalate is not a grace period: a polite child dies at once and the wait succeeds, which is exactly the case the bound exists for. A bound that only *looks* like it holds is worse than none, because the run reads as protected.
- [scope: reading a `review/section/.../XZ-3.125/` path as the walk's current shape | confidence: high | evidence: western-cliff-6876] Since ADR-267 the walk's section artifact directory is `XZ-<derived-offset>`, and the offset differs per mechanism (quill −21.2 mm, swing-arm rig −9.2 mm, carriage 0 mm). The committed `docs/probes/` baselines naming `XZ-3.125` are historical measurements and were deliberately left as written, so a `XZ-3.125` path in this repository dates a run rather than describing the leg.
- [scope: running the walk after a Python-only engine change | confidence: high | evidence: narrow-wing-0418] The installed runtime remains a separate copy requiring rebuild/install after source edits. ADR-251 now reports top-level Python differences or unavailable evidence before the walk, superseding the missing-comparison warning. A match proves neither binary currency nor loaded-module provenance; reporting never rebuilds or refuses [rec: narrow-wing-0418].

## Provenance

- empty-wolf-3962 — operator-declared charter gap
- fond-mesa-1562 — fixed-toy evidence does not close the general entry point
- shy-cabin-0798 — ADR-199: cadex walk qualified on the toy through two real walks, review.json in the project, checkpoints and traces out of its history, 13 tests
- modest-summit-8554 — nt3 operator directive re-seeds the criterion unticked as that run's leading frontier
- misty-rain-9048 — the walk run end to end: clean on both example recipes with numbers, refused at the digest edit on an agent-designed project; docs/CLI.md names all three refusal counts
- cool-fountain-2483 — the authoring contract teaches weights=/sha256= as inline literals, two tests tie the prompt's example to the rewrite, and the walk from a prompt ran clean end to end (280.7 s, 1.23 GB, total_reward 425.997)
- ancient-wind-0117 — a turn with no accepted script reports its cause (last engine refusal or "refused nothing" plus closing words, 400-char clip); the walk carries it with no walk.py change; 142 CLI tests, no skips
- placid-sky-7374 — the second independent prompt walk (vertical carriage, prismatic, force motor, seed 1) ran clean end to end on an unchanged tree: 189.4 s, 1.115 GB, total_reward 0.0035556
- brave-delta-4193 — bounded documented arm walk refreshes CPU training, verified baseline rollout and committed review evidence; CLI 161 passed without skips
- fair-cedar-7455 — fresh CPU walk and six cold public commands preserve accepted identity, policy assets and restored trace; CLI 195 passed without skips
- lucky-canyon-6724 — preserve working CPU-walk evidence while tracking the unimplemented VISION rationale correction
- polished-moss-9358 — VISION principle 5 corrected to the offboard dependency boundary and supported CPU path; no runtime rerun
- neat-summit-3586 — VISION principle 3 policy-asset rationale corrected without changing the asset contract or runtime evidence
- northern-hill-9362 — installed-bundle baseline stops at design; prior working lifecycle evidence preserved
- strong-raven-3067 — refreshed installed engine accepts design and full bounded CPU walk; same-root lifecycle/CLI gates and matching background shell gate pass
- tiny-haven-0347 — resolver-only probe bounds the payload schema check; no walk change
- keen-pebble-3574 — payload guidance corrected to the verified check; installed-engine CLI gate 195 passed, zero skips
- empty-banner-7438 — third clean prompt walk on the installed bundle: pan-tilt head, first serial two-revolute topology, four legs exit 0 in 371.70 s at 1.419 GB, no leg needed a person or a guess
- morning-summit-7848 — fourth clean prompt walk, on the refreshed installed bundle carrying both clearance fixes: four legs exit 0 in 406.33 s at 1.750 GB, witness 2.882e-09; the design fused its servos, so the catalog path was not exercised by this walk
- silent-mist-5233 — two-servo design refused by provider quota; no acceptance, training or review
- lawful-dune-3795 — worker prerequisite qualified on fresh local payload; two-servo rehearsal still pending eligibility
- calm-beacon-7800 — pre-reset dispatch deferred without probing or invoking the walk; matching runtime hash retained, no new lifecycle evidence
- proud-beacon-8002 — fifth clean prompt walk: the eligible two-servo leg through the qualified payload, four legs exit 0 in 565.72 s at 1.271 GB, witness 3.758e-09, no person, no guess, no quota refusal
- lucid-pebble-4000 — ADR-245: a closing `NOTE <subject>:` line lands `docs/<subject>.md` and is read back next visit; 197 CLI tests, no skips; mechanism verified, behaviour not yet
- fond-star-1809 — sixth clean prompt walk, in a durable project with the previous findings as inputs: four legs plus review in 630.22 s at 1.116 GB; the design turn writes docs/actuators.md and docs/sensors.md; the repair turn on a live conversation is what remains
- twilight-snow-9071 — ADR-246 portable walk progress/commit labels; four regressions and corrected real toy iterate verified
- sunny-walrus-5847 — durable resumed repair refused for usage credits; resumption and live label verification unreached
- vast-ledge-4610 — controlled offline refusal separates truthful restore bookkeeping from redundant unchanged-session timestamps
- ready-otter-2700 — ADR-247 suppresses unchanged-session replacement; five refusal/success regressions and all 206 CLI tests pass; live repair remains unproven
- lucid-snow-0350 — one existing-stage provider-free hinged-arm walk passes; scratch preinitialization limits ignore coverage
- scarlet-journey-0203 — scratch index repaired forward, preserving local bytes, stored policy, review evidence and history; no rerun
- humble-forest-6896 — the ot4 operator directive re-seeds the criterion unticked on a new machine, the sb1x Linux GPU box
- open-hollow-2140 — the first prompt walk on the ot4 machine exits 3 at the train leg after 1:06:25: the model default is fixed as ADR-249, and the accepted design gates its training layer off because assembly.mjcf burns the 300 s worker CPU cap
- lawful-wolf-9205 — ADR-250 removes the second blocker: the 300 s stall is OpenBLAS address space, not a dynamics declaration; the walk's own script goes 300.0 s / exit 3 to 2.0 s
- wandering-jasper-6102 — one clean `cadex walk --prompt` end to end on this machine: exit 0 in 17:43 at 2,640 MB peak, all four legs and the four-eye review, local CPU training at 5x16 (reward/step -0.1254, witness 7.2e-09), verified rollout -0.1765, cli/tests 217 passed 0 skipped
- rare-cliff-9595 — a second unattended end-to-end walk on this machine, on an unseen mechanism, with no build and no retry: exit 0 in 5:59.8, cli/tests 217 passed 0 skipped; the stale-engine gap stated in docs/CLI.md
- narrow-wing-0418 — Walk engine/source evidence is now reported before the first leg (ADR-251)
- dawn-sky-3891 — failed fresh-directory retraining preserves prior policies and comparison history, retains the accepted sweep; CLI 223 passed, no skips
- dusty-vale-2809 — public CPU recovery preserves prior artifacts/history and compares against last successful training and rollout rows
- placid-ember-6741 — successful CPU recovery pinned in the real-engine regression, including preserved history and all four review outputs; CLI 223 passed, zero skips
- floral-arrow-7365 — public explicit-CPU toy walk verifies policy and all reviews; setup and scaffold CPU guidance corrected
- keen-field-4379 — shared opt-in CPU fixture and receipt assertions; CLI 223 passed without an outer backend override; finite CPU verification direction complete
- soft-crane-2369 — a public explicit-CPU hinged-arm walk (exit 0, 14.6 s, 1.54 GB, witness 2.07e-9, rollout -27.109384, all four reviews) carries the inventory-boundary measurement; CLI gate 223 passed, no skips
- eager-lake-5745 — ADR-256: the walk's review reads the domain-note convention back from the MJCF it trained on, reporting declared-versus-kept notes in review.json and the PROGRESS row, never written and never fatal; CLI gate 227 passed, 0 skipped
- chilly-glacier-9294 — the generated ARCHITECTURE.md scaffold documents that review where a project's own agent reads it, closing the critic rejection of eager-lake-5745; CLI gate 227 passed, 0 skipped
- western-gate-9567 — the documented example entry point reproduced on this machine, exposing and fixing a dead `--trainer-python` path in the README's reproduction command and the two example projects' missing `docs/actuators.md`
- blue-quill-9477 — bounds that fix's claim: one walk, not three, evidences the flagless documented command form
- sleepy-hollow-9498 — planner measurement that falsifies this node's "no note from a real design turn on this machine" entry: three ot4 projects carry notes committed by a `cadex prompt:` commit
- square-bay-3436 — ADR-259: review.json's motion block and the PROGRESS row report rollout travel per component in millimetres and degrees from the first solved frame, ranking neither; the 160-character numbers cap raised to 320 after it truncated an existing finding; CLI gate 231 passed, 0 skipped
- northern-comet-5917 — ADR-260: `_record_progress` threads `previous_numbers()` through the walk branch, which it never did, and travel is spelled `travel_mm`/`travel_deg` so `_NUMBER_RE` can read it back; the walk's row and run note now carry a delta on both channels; CLI gate 233 passed
- chilly-crest-2100 — a third mechanism (cylindrical joint, position servo on the linear coordinate) through the unchanged entry point on this machine with no code change: exit 0 in 10:52.99 at 1,946 MB, travel 20.28 mm / 0°, `docs notes 2, none missing` on a real design turn's unprompted notes, CLI gate 234 passed 0 skipped; names the unbounded design leg
- honest-river-1137 — ADR-261: `--leg-timeout` bounds all six walk legs in wall clock (default 3600 s, 0 for none), a timed-out leg fails the walk at exit 1 with the leg at 124, and the train leg gets `max(--leg-timeout, --timeout + 300 s)`; cli/tests 238 passed
- tidy-cove-8382 — the critic's must-fix on that bound: unconditional group SIGKILL, pgid captured while the child lives, 10 s bounded drain, and a mutation-verified regression that fails against the shipped stop; cli/tests 239 passed, 0 skips
- lucky-creek-4303 — monotonic deadline preserves descendants' full termination grace; failing-before cleanup regression and 240 CLI passes

- humble-sky-8445 — real-engine resumed context and note persistence regression, negative control and 245-test CLI gate; live provider iterate remains unmeasured
- floral-grotto-1463 — bounded fixed-stroke seed-0 CPU walk with verified comparison identities, artifact retention and 245-test CLI gate

- honest-banner-0821 — three fixed-input CPU walks pass all legs and retention checks; finite seed direction exhausted; CLI 245 passed
- western-cliff-6876 — ADR-267: the walk's section artifact directory becomes XZ-<derived-offset> and the scaffold, docs/CLI.md artifact table and docs/MUJOCO.md §7c say so; leg semantics unchanged; CLI 256 passed, 0 skipped
- cool-eagle-6400 — ADR-269: docs/CLI.md §2's seven-row leg table is pinned equal to __main__.py's run_leg names in order, with the in-process review last; cli/tests 260 passed, 0 skipped
- copper-canyon-0231 — two token-free reference walks of the documented entry point on this machine, exit 0 in 23.82 s and 23.83 s, all legs and four review calls, 156 and 177 prior run files byte-unchanged, no tokens spent
- humble-star-3313 — ADR-271: the repair turn on a live `--resume` conversation, closed — a real provider turn read this project's own history, disputed its ADR-005, wrote ADR-007 and changed the geometry, and runs/live-iterate-43 (exit 0, 23.62 s) reported clearance offending 1 → 0 with the task digest changed so reward is explicitly not a comparison; cli/tests 262 passed
- dry-rain-0489 — the single uninterrupted `walk --prompt … --resume` attempted and refused by the provider for usage credits, observed through the walk's own path (leg design exit 1, sentence verbatim, no later leg); the criterion stays open on credits rather than on code, and a failed design leg is shown to still rewrite the project's recorded model
