---
node_id: a1de57cf-f3ca-5c7e-bc08-ee64cc0ae2e8
slug: calm-peak-5247
title: The robot lifecycle walk — design to policy to iterate, agent-driven and headless
created_at: '2026-09-06T01:27:43+00:00'
parents:
- salty-isle-4063
summary: ''
---
Status: working

## Current

**Recent project decisions now reach the revisiting agent (ADR-265).** The project-as-codebase contract had a hole under its own budget: `read_project_docs` kept the head of `DECISIONS.md` while `record_decisions` appends newest entries last, so on a long project the newest ADRs — the live constraints — were invisible on revisit, and the agent has no file tool with which to go looking. The decisions selection now uses the tail helper `PROGRESS.md` already used; limits, omission markers, architecture-head and domain-note behavior and all document bytes on disk are unchanged, and entries outside the tail are omitted rather than summarized. Four regressions written to fail first — two on exact selection and omission counts, two driving a real engine through fresh and resumed scripted turns — now pass, with the full CLI gate at **253 passed, no skips**. Retain `working`: this is delivery of recent history into bounded context, not evidence that a live provider reads it and chooses an iterate from it, which stays unmeasured and capacity-blocked [rec: morning-arrow-5841].

**Lifecycle history instructions now match repository ownership (ADR-266).** `docs/MUJOCO.md` §7c promised unconditional first-visit repository initialization and a commit after every accepted run — which the ownership qualification below contradicts, since a nested project without its own `.git` gets documents and rows and no commit at all. The duplicate instructions are removed in favour of a link to `docs/CLI.md`'s contract, the historical measured commits are retained, and the `PROGRESS.md` scaffold now says a row alone does not prove a commit. The reversible choice was to correct the instructions rather than change Git ownership or make accepted modelling depend on a successful commit. No runtime, protocol, payload, shell or training behavior changed and no new regression was added; the existing gate (**253 passed, no skips**) covers the behavior. Retain `working`: a stale-instruction correction closes no criterion [rec: fresh-clover-8521].

**Durable project history survives tested update failures (ADR-264).** Progress, decisions and domain-note updates write a complete sibling temporary file before replacement. Six partial-write/refused-replacement and retry regressions preserve earlier bytes, permissions and file inventory; the recorded full CLI gate is **251 passed, no skips**. Protection is per file, with no multi-document transaction, concurrency or power-loss guarantee; an uncatchable termination can leave a hidden scratch file. First-visit scaffolding is unchanged. Retain `working`: this repairs project-as-codebase persistence without establishing live provider understanding [rec: solar-snow-8543].

Declared by the audit under the working name `robot-lifecycle-walk`; this node is that target [rec: sweet-light-3396].

The agent-driven robot lifecycle walk as a state of its own: design → assembly → MJCF → task → local CPU training → policy verify → rollout → review → **iterate** → compare and record, driven from the CLI with no human in the loop, artifacts landing in the project directory with docs/reports versioned and generated reviews local under fresh defaults; policy/run retention follows explicit Git rules [rec: easy-beacon-3803]. It is the goal's second priority and the `docs/MUJOCO.md` §7b rehearsal re-run as an audit; the training loop's own facts (venv, gate, mg-legs, B7) stay on the RL node [rec: sweet-light-3396].

**Measured 2026-09-06 on the §7b toy, headlessly, on scratch copies of the rehearsal project** (twelve legs, `docs/MUJOCO.md` §7c). **Every row is now the agent's, the CLI's, or doc-only** [rec: sweet-light-3396] [rec: wise-tooth-2750] [rec: restless-harbor-4224] [rec: amber-moon-0981] [rec: keen-sail-4481] [rec: odd-fern-7024] [rec: sunny-fern-8059]:

- **Agent-driven from the CLI**: legs 1 and 2 (design, assembly — proven by the rehearsal, one model turn each), 6 (policy install by `cadex script --set` with name and sha256: accepted, rollout 1719.2 total reward over the full 300 steps against 1729.9 for the 400-iteration run), 7 (review), and since ADR-189 leg 3 as well [rec: sweet-light-3396].
- **Leg 3, bundle out, closed** (ADR-189) [rec: wise-tooth-2750]: `cadex export` copies the task JSON, model XML, policy receipt and rollout trace into `--out` under their **staged** names and names each in the `--json` envelope; the exported folder is a bundle `training/cadex_train.py` accepts unchanged. Staged basenames were kept deliberately, because the task bundle names its model by them. **Leg 7 for a pipeline** closed with it: the trace's `policy` block carries `total_reward` and the five `reward_totals`, so review needs no staging path.
- **Leg 5, policy home, closed** (ADR-190) [rec: restless-harbor-4224]: `cadex asset --put FILE` (repeatable, because a policy travels with its receipt) stores files through the existing `put_asset` op and reports the store's listing — name, bytes, sha256 — as `assets` rows in the envelope; `put_asset` is in the agent's tool surface, and the overlay tells the agent it cannot train and names `cadex export --out` and `cadex asset --put` as the caller's two ends of the leg. No `--rebuild` flag by decision: the rebuild that makes a policy real is the script change naming it.
- **Leg 4, training, closed** (ADR-191) [rec: amber-moon-0981]: `cadex train --out DIR --iterations N --envs N --put` rebuilds, exports the bundle, runs `training/cadex_train.py` under the training venv with flags pinned by name and test-read from the trainer's parser, and stores the policy with its sha256 in the envelope's `training` field. Measured 18.5 s wall for 2 it × 8 envs, task digest `602d62c1…` equal to the audit's. **Legs 3→7 are one command plus `cadex script --set`.** The agent itself has no shell, so the leg is the caller's or a pipeline's by decision.
- **Leg 8, iterate, closed as a script convention plus two flags, not an engine op** (ADR-192) [rec: keen-sail-4481]: the trained policy is declared behind a numeric switch parameter (`policy_on=num(1.0, min=0, max=1, step=1)` guarding `assembly.policy`, `assembly.rollout` and their `result` entries). `set_params` never refuses a dropped output — only `write_script` does (ADR-045) — so `cadex params --set policy_on=0 --set <change>` is accepted where the plain sweep was refused, and exports the bundle at its new digest. `cadex train` carries ADR-161's curriculum pair, `--init-from-parent-task BUNDLE` and `--init-from-task-change REASON`, beside `--init-from`, refused apart as a usage error, so the retrain is warm across the change; then the digest edit with `cadex script --set` and `cadex params --set policy_on=1` verify and roll out. **Four commands, one digest edit, no human step**; the CLI agent's overlay names the convention so a script it authors carries the switch. Measured: refusal exit 3 at `602d62c1…`→`369a0dd5…`, warm retrain 2 it × 8 envs in 17.8 s with iteration 0 at +1.52 reward/step (cold sits at −0.95), re-declared at `4f2d62b1…`, trace 127.8 against the parent's 1729.9; pinned by a real-trainer test. A stored parameter value outlives a script write, which is why the re-enable is its own `params` call rather than a hidden reset.
- **Leg 10, project as a codebase, closed** (ADR-193) [rec: odd-fern-7024]: `cli/cadex_cli/project_docs.py` scaffolds `ARCHITECTURE.md`, `DECISIONS.md` and `PROGRESS.md` in the project root on the first visit from `_engine_session` — idempotent, never overwriting, one note in the envelope. Every `cadex -p` turn gets the three pasted into its system prompt between the overlay and the engine's contract, bounded at 8 000 characters each (head of the first two, tail of the log). The CLI appends one `PROGRESS.md` row per accepted run — time, command, accepted revision, digest, what was done, and the numbers the run produced (trace `total_reward`, trainer `reward_per_step`, wall time, sha256), nothing inferred; no row for the script print or the store listing. A turn's closing `DECISION:` lines land as numbered `## ADR-NNN` entries in `DECISIONS.md` (`ADR-001` is the project's creation). `docs/<subject>.md` (`gear-ratios`, `sensors`, `actuators`, `rejected`) is the documented domain-doc convention, linked from `ARCHITECTURE.md`. The CLI agent still has **no file tool** by decision: "reads them" is the CLI pasting them in and "updates them" is the closing-line convention — a doc convention before a new tool. The shell-attached agent also has no shell or file tools (`--tools ""`, Mesh tools only); the CLI or a person maintains those documents with the GUI attached. This corrects the earlier file-tools claim [rec: red-comet-9710].
- **Leg 9, compare and record, closed** (ADR-194) [rec: sunny-fern-8059]: a `PROGRESS.md` number an earlier row also carried is written with its change against that row — `total_reward -293.4 (Δ -421.2 vs 2996fb73 at 127.8)`: the delta, the digest compared against, and that run's value — so a comparison between two runs is **one recorded row**, not two a reader lines up by eye. `total_reward` and `reward/step` are each compared against the last row that carried the number, read back from the file as written; a row's own delta text is never read as a value. Automatic history depends on repository ownership: fresh roots initialize Git and create default ignore rules only when absent; existing project-root repositories retain their ignore configuration and commit all working changes, including unrelated edits and working versions of staged files; nested projects without their own `.git` get documents and rows without automatic commits, preserving the parent HEAD/index. Three staged acceptances verified these cases and preservation of existing user-file bytes [rec: fresh-flint-1505]. The lifecycle guide, CLI guidance and project-doc scaffolds now state that rows alone do not prove a commit; command notes report `committed <sha>.` on success. Existing project documents are not migrated, and missing Git or commit failure does not invalidate acceptance. Full CLI gate recorded **206 passed, zero skips**. Reconcile judgement: this closes the finite first-visit documentation correction and retains `working`; it supplies no new lifecycle gap or live repair evidence. Fresh-root pre-existing-ignore and commit-failure statements remain code-inspected rather than newly runtime-qualified [rec: little-fern-0464].
- **Doc only**: leg 11 (GUI attached) and leg 12 (remote training, B7 still blocked on the GPU box's checkout) [rec: sweet-light-3396].

**Ordered frontier** (§7c), **fully closed** on 2026-09-06: 1 ~~outputs the CLI hands over~~ (ADR-189) → 2 ~~`put_asset` in the CLI tool surface plus `cadex asset`~~ (ADR-190) → 3 ~~a `cadex train` dispatcher~~ (ADR-191) → 4 ~~an iterate shape~~ (ADR-192) → 6 ~~the project scaffold with `PROGRESS.md` rows~~ (ADR-193, ADR-194) → 5 ~~the engine's `INSPECTION_FAILED` frame fixed with a validator test~~ (ADR-195: built by `tool_failure`, pinned by a validator test and the `inspect.failure` golden, so the CLI's strict validator reads it as a refusal rather than a hard `CadexdError`) [rec: sweet-light-3396] [rec: sunny-fern-8059] [rec: fierce-moss-6382].

**The entry point is `cadex walk`, qualified 2026-09-06** (ADR-199, commit a4248b26) [rec: shy-cabin-0798]. The fresh-toy audit had proved the legs individually (117 tests, warm retraining, two 50-step rollouts, eight project commits) but its helper supplied the digest edit, its outputs sat beside the project and nothing reviewed into it [rec: fond-mesa-1562]. The walk closes that: the digest edit is the walk's own (a two-literal rewrite of the one `assembly.policy` call), `write_review` lands `review.json` under `--out` and `_commit_run` commits the walk (it skipped it before), and the scaffolded `.gitignore` keeps checkpoints, the `train/` policy copies and `*-trace.json` out of the project's history while `assets/*.cxpolicy` stays in. Two real walks on the plate-and-arm toy reproduced the audit's numbers (`-27.1094`, then `-55.3480` with `Δ -28.2 vs 2c1ad3fb at -27.1`; 15.5 s and 16 s wall). `cli/tests/test_walk.py` (13 tests) pins leg order, flags, refusals and the real toy walks; full CLI suite 130 passed, no skips. The criterion node is `crisp-reef-5607`.

**The training leg has two homes behind one command** (ADR-200, commit 5143099c) [rec: green-delta-7130]: the venv's trainer, or `training/remote_train.sh` with `--remote` on `cadex train` and `cadex walk`, sharing one `trainer_flags` source so the tail after `--` is byte-for-byte the local flags. The returned policy is verified against the receipt's sha256 on both paths (it cannot fail locally; one code path is fewer than two), and a warm start with `--remote` is refused before any leg because the dispatcher does not carry the `--init-from` policy. Offline-tested against a stand-in dispatcher and the real engine; 134 CLI tests; never dispatched. The scaffold's `ARCHITECTURE.md` carries a `## Training` section naming the mode, the shared project-relative artifact paths and the cold-run limit, and a `train --remote` row in `PROGRESS.md` says `(remote)` (commit 8c1ff05f, 136 CLI tests) [rec: wild-marsh-9611]. The three-mode criterion is `witty-spark-2613`; the GUI-attached mode is now documented for sequential use only [rec: red-comet-9710] [rec: grand-fjord-0624].

**Second-mechanism qualification** (ADR-203): the unchanged walk now covers revolute/torque and slider/force mechanisms. Repository-owned hinged-arm and linear-carriage examples retain source, docs, baseline numbers and reproduction commands; generated policies, checkpoints, traces and accepted caches remain untracked. Both rollouts reach 50 steps, with 138 CLI tests passing and no skips. The scaffold distinguishes training-batch reward from verified rollout mean. The carriage's one-iteration policy falls rather than holds height, so this is pipeline evidence, not control-quality or hardware validation. The second-mechanism criterion is working at `swift-dusk-2951`; the mode criterion remains separately tracked. [rec: sage-peak-2689]

Noted, not chased [rec: keen-sail-4481] [rec: odd-fern-7024] [rec: sunny-fern-8059]: every `cadex export` and `cadex train` still leaves a fresh complete attempt directory for the same accepted revision; the trainer's `init-from`/`curriculum` lines go to stdout and the dispatcher shows only stderr, so the header carries the facts; the receipt has no warm-start field; the `train` row's `total_reward` is the incumbent's (the dispatcher's rebuild exports the current policy's rollout) — honest but easy to misread as the new policy's score, which only the re-declare row has; a very long `PROGRESS.md` loses its header in the prompt (the overlay restates what the file is); a project predating the convention gets its docs and `ADR-001` dated that day; its history and ignore behavior follow the ownership qualification above [rec: fresh-flint-1505] [rec: little-fern-0464]; a read-only visit leaves the engine's re-staged `script.json` dirty until the next accepted run commits it; the `export` commit message carries the caller's `--out` path.

**The GUI-mode scaffold matches the corrected walk guidance** (ADR-201): `ARCHITECTURE.md`'s Training section is pinned to `docs/CLI.md` by `cli/tests/test_project_docs.py`. It requires Rebuild Model or reopening after CLI acceptance and before the next GUI edit, explaining that the shell's automatic retry can silently overwrite accepted script or parameter values. GUI use is documented, not exercised [rec: red-comet-9710] [rec: grand-fjord-0624].

**Walk review produces inventory, clearance and four named-angle previews.** Reports remain versioned; fresh scaffolds keep generated previews local [rec: easy-beacon-3803]. One render/inspection session rebuilds and snapshots accepted display before inventory/clearance requests invalidate attempt buffers, then preserves both reports and their availability/count/path summaries. Images and summary are retained under `review/render/<accepted-revision>/`, with revision/digest, limits and acquisition/render timings in review. Clearance keeps its initial-pose scope, thresholds, offending pairs and explicit unknowns; findings do not fail a walk, inspection errors do. Malformed acceptance and rebuilt revision mismatch are refused, and failed rendering exposes no new successful review. Real arm/carriage walks and local versus remote-flag CPU stand-in parity cover image bytes, reports and project commits. The implementation gate recorded 181 passed, zero skipped, with two final passing refusal cases [rec: happy-dune-3481] [rec: clear-ash-2884] [rec: square-path-1173].

**Separate fresh-project CPU rehearsal passes with the integrated review.** The documented hinged-arm recipe used one iteration/four environments, verified 32 witness samples at error 1.3841167412e-09 against 1e-4 tolerance, and reproduced 50-step rollout reward -27.1093842209. Four inspected nonblank previews, render summary, review and project docs match clean committed HEAD bytes; revision/digest agree across accepted rollout and render evidence. Inventory has two synthetic components; initial-pose clearance retains one touching below-clearance pair, zero unknowns. Whole command: 17.07 s at 1,054,818,304 bytes sampled peak tree RSS; internal walk_seconds 16.9196 s; acquisition/rendering 1.2289 s. Full built-engine CLI gate: 182 passed, zero skipped (178.80 s), with no monitor cutoff. The gate overlapped the walk; external timing includes sampling delay and internal timing excludes final serialization/commit, so comparisons to earlier single samples are not regression benchmarks. This is toy pipeline evidence, not control-quality, GUI or actual remote-execution qualification [rec: modest-sun-6068].

**Walk review includes sections from the same accepted snapshot as named views.** The fixed world XZ plane at Y = 3.125 mm gives meaningful interior cuts through both reference mechanisms, without mechanism-specific dispatch. SVG/JSON and review metadata retain status, availability, revision/digest, plane/offset/units, paths, approximation, limits and acquisition/contour timings. Empty is available with no contours; unsupported is unavailable with reasons. Revision and rollout-digest mismatches, acquisition and write failures fail the walk without presenting retained artifacts as success. Documentation, scaffold and shared mode-artifact table agree [rec: copper-timber-8947].

Both arm and carriage pass with tracked positive-area contours and matching geometry/pixels between local and remote-flag CPU stand-in paths. The final built-engine CLI gate recorded **195 passed, zero skipped**, in 216.88 s; monitored peak process-tree RSS was 1,175,715,840 bytes. Four whole-walk samples were 14.348–15.754 s excluding final project commit; acquisition 0.645–0.668 s counts once and contour generation 0.000064–0.000069 s excludes serialization/writes. These are toy pipeline observations, not benchmarks, control-quality evidence, GUI or SSH execution. Initial-pose/tessellation limits and the arm's known contact remain [rec: copper-timber-8947].

The separate fresh two-mechanism complete-review rehearsal remains open on `damp-moon-9297`; this integration does not close that criterion [rec: copper-timber-8947].

**Purchased-hardware placement guidance is delivered.** The shared CLI design overlay used by `cadex walk --prompt` and fresh project ARCHITECTURE.md scaffolds require published catalog bodies and separate purchased assembly instances, kept separate from printed solids; transformed catalog bodies may serve as clearance cutters without implying another purchase. Existing project documents are not migrated. The full built-engine CLI suite passed 195 tests with no skips; this proves instruction delivery, not agent compliance. The fresh two-servo rehearsal remains outstanding, with baseline lifecycle status retained as `working` [rec: misty-tide-6394].

**Leg 10's domain-doc convention now has a writer (ADR-245, commit `febaf940`).** ADR-193 documented `docs/<subject>.md` as a convention but gave a design turn nowhere to send the text — the instruction ended by asking the caller to write those files. A closing `NOTE <subject>: <text>` line now appends a dated bullet to `docs/<subject>.md` through the same closing-text channel as `DECISION:` lines, the notes are pasted back on the next visit (2,000 characters, tail-first), `inventory` and `clearance` are refused as subjects, and the instruction asks for `docs/actuators.md` and `docs/sensors.md` from any mechanism with actuators or sensors. Still no file tool, by the same decision as ADR-193. CLI suite 197 passed, no skips [rec: lucid-pebble-4000].

**Iterate, measured once on a real mechanism: the review's findings went back into design and came out answered in geometry (2026-09-08).** The two-MG90S leg walked again through the documented `cadex walk` in a durable project, with the previous rehearsal's three review findings — shin/foot-bolt 5.8373525743 mm³ each, bolt/nyloc-nut 9.2781252741 mm³ each, and the 5 mm servo-to-link gap — stated in the prompt with their numbers, as design inputs rather than as a second turn's correction, because the earlier project under the temporary directory had been reaped and there was no conversation to resume. All four legs plus review completed in 630.22 s at 1.116 GB peak with no operator edit to the script and no source repair. The four positive-volume intersections are gone and the drive gap is closed by a printed shaft boss on each driven link, with the six catalog instances still separately placed `assembly.component`s (10 components, 6 catalogued) and no catalog body entering a printed solid. The design turn wrote `docs/actuators.md` and `docs/sensors.md` — ADR-245's first live evidence — plus five project ADRs, two of them answering the findings directly. Verified rollout 0.853045 under a positively shaped objective, not comparable with the earlier leg's row. The review numbers are on `damp-moon-9297`. Reconcile judgement: retain `working`; leg 8 has now run on an agent-designed mechanism rather than only on the §7b toy's parameter sweep, but through a fresh prompt, so **a repair turn on a live conversation (`--resume`, iterating from the run's own `review.json`) is the shape still unexercised**, and the durable project exists to do it on. The absolute walk-label defect observed there [rec: fond-star-1809] is fixed for future progress rows and commits by ADR-246; historical labels remain [rec: twilight-snow-9071].

**The durable repair attempt stopped before acceptance.** The single review-driven `cadex walk --resume` attempt was refused for provider usage credits at design; repair training, declaration, verification/rollout and review were unreached. Baseline reward remains 0.8530453495479725, with 45 clearance pairs, nine offending zero-volume contacts, zero unknowns and six catalog instances; there is no new like-for-like comparison. Successful stored-conversation resumption remains unestablished. The durable project stays at HEAD `b244f26`, accepted revision `417d0fdf3286` and digest `e6395e3e49c4`, but `agent.json` and `script.json` are dirty from timestamps and restored attempt identifiers. Accepted parameters, script source, domain notes, progress and decisions are unchanged; metadata was preserved, not restored or synthetically committed. The next visit must account for this dirty baseline. No retry or provider switch followed. Reconcile judgement: retain `working` on prior lifecycle evidence; successful same-session repair, comparison and a clean repair end state remain missing [rec: sunny-walrus-5847].

**Refusal metadata is now bounded and unchanged-session churn is fixed (ADR-247).** A controlled offline refusal through the real local engine preserved accepted source, revision, digest, parameters, project documents and HEAD. Restore alone refreshes `script.json` attempt identifiers, staging locator and timestamp because it regenerates accepted artifacts; this is truthful bookkeeping, not a provider geometry edit, and does not justify rollback. The redundant `agent.json` timestamp replacement isolated by that diagnosis is now suppressed when a valid stored nonempty session ID and model match. Changed identity/model still persists on failure; successful accepted edits and pre-existing user changes remain preserved. Three refusal and two success regressions cover this boundary; the full CLI gate passed 206 tests with zero skips. Reconcile judgement: retain `working`; this closes the narrow metadata correction, while successful live resumption, downstream repair/training/review and live output-label verification remain unproven. [rec: vast-ledge-4610] [rec: ready-otter-2700]

**Two standing file-lifecycle qualifications found no missing walk leg.** A provider-free disposable project retained its accepted two-output model, protected docs and stored STL after refusal, reopened cold, accepted intentional replacement and reopened again through six real staged CLI processes, with 14 existing recovery tests passing [rec: silver-rain-7333]; a source-absent consumer cold restore/export passed with accepted identity, exact solid, linked bytes and project docs preserved [rec: calm-sky-2656]. Neither identified a consent/recovery mismatch or a missing lifecycle leg, so neither conditional correction is dispatchable. Reconcile judgement: retain `working` on the completed walk evidence; these close no charter criterion and supply no evidence of successful live conversation repair.

**Model-free iterate is verified on the durable ot4 carriage.** `cadex walk --set carriage_wid=80` changed width from 70 mm, trained cold on CPU at 5 iterations × 16 environments, seed 0, and completed sweep/train/declare/rollout/review in 20.89 s, exit 0. Task bundles differ only in model; both verified rollouts use seed 7, 200 steps and 4 seconds with identical randomisation and no early termination. Reward decreased **3.296298 → 2.760187** (delta -0.536111); the walk automatically recorded the rounded comparison in `PROGRESS.md` and produced five project commits. All 21 baseline files, including its stored policy, stayed byte-identical. CLI gate: 223 passed without skips. Retain `working`: this is one cold-seed toy-scale comparison, not design qualification or a live `--resume` repair turn; GUI and remote execution were not exercised. [rec: mellow-quartz-8093]

**Lifecycle guide maintenance preserves the tested contract.** `docs/CLI.md` distinguishes child reward/delta rows from the successful walk's clearance review row; failed legs preserve earlier rows without adding a walk review row. Runtime and scaffold are unchanged [rec: still-harvest-7242]. The carriage iterate account is 16 lines shorter, retaining its runnable command, fixed task and rollout settings, reward decrease and baseline preservation, with detailed evidence linked through ADR-251 and the immutable rehearsal record. Both documentation units recorded 223 CLI passes, zero skips. Reconcile judgement: retain `working`; these corrections add no runtime qualification [rec: floral-canyon-9424].

**Failed iterate retries retain a recoverable accepted sweep.** After trainer exit 7 with partial output, `policy_on=0` and `lift_weight=0.0003` persist, while successful runs, stored policies and comparison history remain intact; only the accepted sweep adds a progress row/commit [rec: dawn-sky-3891]. Public recovery uses the last successful policy/task, a fresh output directory and new policy name, an explicit task-change explanation and no repeated `--set`. The rehearsal preserved all 46 prior artifact hashes and the progress prefix; new training and rollout comparisons referenced the last successful rows, not failed partial output [rec: dusty-vale-2809]. The real-engine CPU regression now pins that continuation, retained weight with `policy_on=1`, stored-policy/verified-rollout digest equality, prior artifact/history preservation, actual last-success comparison values/digests and clean project Git status. Final CLI gate: **223 passed, zero skips**; ADR-252 consolidates the regression and recovery guidance without changing production behavior. Reconcile judgement: retain `working`; the finite recovery sequence is covered at toy scale, not evidence of improved control under changed reward weights or new GUI, actual remote or live-resume qualification. [rec: placid-ember-6741]

**Leg 10's domain-note convention is now read back as well as written (ADR-256).** ADR-245 gave the design turn a way to author `docs/<subject>.md` through a closing `NOTE <subject>:` line and pasted the notes back on the next visit, but nothing checked the convention against the mechanism: a walk on a driven, observed rig whose turn wrote neither note produced a review indistinguishable from one on a fully documented project, and the gap was visible only to a person who opened `docs/` and knew what to expect there. The walk now parses the MJCF it trained on and takes each declared section as an expected subject — `<actuator>` with children asks for `docs/actuators.md`, `<sensor>` for `docs/sensors.md` — putting that beside the notes the project keeps in `review.json`'s `documentation` block and in the `PROGRESS.md` row. It reports and never writes, by the same decision that kept the CLI agent without a file tool: an invented note would be pasted back into the next design turn as if it were knowledge. `cli/` and `docs/` only; CLI gate **227 passed, 0 skipped**, real engine and real CPU trainer [rec: eager-lake-5745]. Reconcile judgement: retain `working` — the convention clause of leg 10 now has an exercise rather than a description. The scaffold half of the same change is on `crisp-reef-5607` [rec: chilly-glacier-9294].

**Correction (2026-09-08): the write half is exercised on this machine, three times.** The paragraph above previously closed by claiming that every note in evidence here was placed by hand or by a unit test, and that a real design turn authoring a note remained the open unit. That is false: `docs/actuators.md` and `docs/sensors.md` in each of `~/cadex-projects/ot4-carriage`, `ot4-swing2` and `ot4-swing` were committed by a `cadex prompt:` commit — the design turn's own closing text, on this machine [rec: sleepy-hollow-9498]. What had never run on a real prompt walk is only the ADR-256 **read-back** eye. Folded as a judgement call: the correcting record declares plan-view impacts only, but leaving a claim in the projection that a record node measured false is worse than folding one that was not declared.

**The documentation eye, read on a recipe walk.** Both repository-owned lifecycle examples were re-run through the documented headless entry point on this machine, and the ADR-256 eye reported `0 domain note(s) for 2 declared subject(s); no note for actuators, sensors` on each. Correct rather than a defect — `script --set` installs the recipe alone and a recipe walk runs no design turn, so nothing had the chance to write the note — and the README now says so. The example directories nonetheless carried only `docs/sensors.md`, half keeping the convention their own review declares, so both gained `docs/actuators.md` written from the exported MJCF (`j/motor`, `forcerange -0.4 0.4` = 400 N·mm on the arm, `-4 4` = 4 N on the carriage) and naming what is **assumed** rather than selected: no damping, friction, gearing or end stops, no manufacturer part, and the N·mm-versus-N effort difference that is why the two control-cost terms do not compare. The fix was a documented convention, not a CLI change that would copy sibling files into a project — a CLI change was barred here, and the eye reports and never writes for the same reason [rec: western-gate-9567]. Reconcile judgement: retain `working`; this qualifies the eye's behaviour on a recipe walk and completes the examples' own domain docs, adding no runtime lifecycle evidence. The mechanism numbers are on `swift-dusk-2951`, and the reproduction command's correction on `crisp-reef-5607` [rec: blue-quill-9477].

**The iterate comparison is real for the first time: the walk's row says what changed (ADR-260, commit `2b0c1678`, 2026-09-08).** Leg 9 — compare and record — has carried a delta on the *train* leg's row since ADR-194, but the walk's own row never did: `_record_progress` branches on the command and only the non-walk branch passed `previous=previous_numbers(...)`, so no walk row of any project had ever shown a change against its previous walk, for any figure. ADR-259's travel figure landed on that row and so could not be compared either, and its spelling (`motion 0 mm (swing), 178.8° (swing)`) was unparseable by `_NUMBER_RE` in any case. Both are fixed in one commit: `previous_numbers()` is read once for both branches, `_motion_cell` spells the channels `travel_mm N on <component>, travel_deg N on <component>` through the now-public `compared_number`, and `COMPARED_NUMBERS` gains both labels. The row a second walk of a project writes joins the travel figure to what changed since the last walk that carried it — `travel_mm 103.7 (Δ +0.419 vs 4b0a1c2d at 103.3) on carriage` — and the run note is the same cell with its prefix stripped, so one spelling serves both. `review.json` is unchanged, keeping `millimetres` and `degrees` under their own keys. The worked example is the durable carriage iterate this node already records: travel 103.298 → 103.719 mm while `total_reward` fell 3.296298 → 2.760187, an iterate that held its motion and lost reward — a distinction the loop could produce but not state. Two calls taken without asking: **a delta is not a verdict** (ADR-259's refusal to rank millimetres against degrees stands, and a row reporting more travel and less reward makes no claim about which mattered), and units are dropped from the note so the two wordings of one figure cannot drift. Pinned offline by a `test_project_docs.py` round-trip and by the toy walk's first-walk-carries-no-delta assertion, and end to end by the extended real-engine iterate lifecycle test. CLI gate **233 passed, 0 failed**; `cli/` and `docs/` only. Reconcile judgement: retain `working` — this closes the *iterate* leg's reporting half, which was the last place the walk produced a comparison and then discarded it. Unchanged: a repair turn on a live `--resume` conversation, GUI-attached and actual remote execution, and any claim about control quality [rec: northern-comet-5917].

**The durable quill has a measured parameter-only iterate.** `cadex walk --set stroke=60`, without a prompt, completed sweep/train/declare/rollout on CPU at 5 iterations × 16 environments, training seed 0, in 24.61 seconds with exit 0. Stroke changed 40 → 60 mm; reward expressions/weights, observation units and the 4-second, 50-Hz horizon stayed fixed, while geometry and action high changed. Verified rollout reward moved -74.791975 → 175.487211 (delta **+250.279186**), quill travel 20.283552 → 30.078469 mm (delta **+9.794917 mm**), and rotation remained 0 degrees. All 19 baseline files stayed byte-identical. The target becomes the widened action range's midpoint, so one seed cannot distinguish action-scaling effects from improved control or establish significance. Initial-pose intersection remains 960 mm³; no swept-fit certification. Reconcile judgement: retain `working` for iterate evidence; the recorded artifact-retention failure is resolved by the fresh default and explicit Git recipe on `chilly-union-8972` [rec: easy-beacon-3803]. This rehearsal ran no source pytest/build gate [rec: young-timber-2761].

**Artifact retention is repaired for fresh scaffolds (ADR-262).** Generated project-root `/review/` output stays local; docs, source and reports remain versioned, and stored policies remain included by default. The quill incident was Git ignore precedence, not force-add: root `!assets/*.cxpolicy` overrides `.git/info/exclude`, while review output had no exclusion. Explicit policy exclusions must follow that negation in the root ignore file; new run/policy names need exclusions before dispatch. Existing repositories retain configuration, and tracked or explicitly staged outputs still enter ordinary `git add -A` commits. Migration and forward untracking remain explicit. A real-Git regression failed before the scaffold fix and now verifies local outputs, committed docs/source, default assets and tracked/pre-staged content. Render, section and walk assertions retain geometry checks while requiring generated reviews to remain local and absent from Git. Full CLI suite: 241 passed, no skips. Reconcile judgement: the recorded defect is closed by the fresh default and tested retention recipe; this supplies no silent migration or history-removal claim [rec: easy-beacon-3803].

**Train/walk comparisons identify seeds, objective and action scaling (ADR-263).** Training seeds are bounded to 0..4294967295; rollout seeds come independently from the exported trace policy block. Objective v1 hashes compact key-sorted JSON of schema, observations, reward, termination, episode and functions, excluding model identity, actions, seeds and stochastic conditions. Actions have a separate identity; review JSON preserves full action metadata and objective fields. Current/prior same-kind rows retain identity evidence; legacy missing seeds/identity remain unavailable, and per-metric deltas may reference a different row. Standalone rollout rows keep their numeric format. Equal objective identity establishes neither equal control difficulty nor significance or improvement. The real CPU iterate regression verifies seed propagation and objective change after a reward change; full CLI gate: **244 passed, no skips**. These are recorded checks, with no new rehearsal, GUI or actual remote qualification [rec: modest-prairie-3356].

**Project history reaches resumed agent turns with current edits.** The real-engine two-turn regression checks existing decisions and domain notes plus between-visit architecture, sensor and progress edits at the resumed factory boundary; it retains the session id/project cwd and verifies changed geometry plus appended decisions/notes without losing existing text. Its negative control fails when project-doc delivery is removed. No runtime omission was found. Retain `working`: delivery and persistence are pinned by **245 passing CLI tests**, while actual provider understanding and a live history-guided geometry/train/review iterate remain unmeasured [rec: humble-sky-8445].

**The four-seed quill comparison is measured at fixed stroke 60.** The retained training-seed-0 reference reproduces reward **175.487211145051** and travel **30.078469436328 mm / 0 degrees** at rollout seed 7 [rec: floral-grotto-1463]. Training seeds 1–3 retain full parameters, byte-identical MJCF/task and canonical objective/action identities. Across seeds 0–3, reward ranges **170.952827–175.935972** (span 4.983145), travel **30.078469–31.421760 mm** (span 1.343291), and angular travel remains zero; each rollout is 200 steps / 4 s. These descriptive results and exact review references are in docs/CLI.md and committed project PROGRESS. The 30 mm target is the action midpoint, so near-zero normalized actions already command it: no significance, winner or learned improvement is established. Prior run/policy bytes, history and unrelated content survive; new generated outputs remain excluded throughout new project commits. Retain `working`; the selected seed measurement is exhausted [rec: honest-banner-0821].

**The iterate leg is no longer pinned to this machine (ADR-268, commit `2addc0b3`).** Iterate — leg 11, change what did not work and retrain from the last policy — was a local-only operation for as long as `remote_train.sh` carried only two files out: ADR-200 refused `--init-from` and `--init-from-parent-task` with `--remote` before any leg ran, because the curriculum pair (ADR-161) names paths on the dispatching machine. The dispatcher now lifts both flags out of the trainer's arguments, copies the files into a `warm/` subdirectory of the run directory and re-emits the flags pointing at the copies, so `cadex walk --remote` with the curriculum triple reaches the **train leg and no other**, exit 0, verified against a stand-in dispatcher; `train --dry-run` names the warm files in `artifacts` and in the `copy-out` step in both modes. The rewrite is the script's, not the CLI's: `remote_trainer_command` still builds the local trainer's flags byte for byte, so the artifact-parity identity this node already rests on is unweakened. `cli/tests` **258 passed, 0 skipped**. Reconcile judgement: retain `working` — this removes a declared shape difference from the iterate leg and adds no runtime evidence, because nothing was dispatched: no ssh, no box, no GPU run, and `--detach` still does not travel through the walk. The mode-parity half of the same change is on `witty-spark-2613` [rec: idle-falcon-3004].

## Negative knowledge

- [scope: a parameter sweep on a project with a policy declared, without the switch convention | confidence: high | evidence: sweet-light-3396, keen-sail-4481] `cadex params --set` is refused at exit 3 when the change moves the task digest, because the declared policy no longer fits, and nothing is written. Iterate cannot be a plain sweep; the ADR-192 answer is a script that declares the policy behind a `policy_on` switch the sweep blanks. A `params --drop-policy` flag and an engine op were considered and not taken.
- [scope: the CLI agent asked to train inside a model turn | confidence: high | evidence: sweet-light-3396, restless-harbor-4224, amber-moon-0981] It has no shell, and it is not given one: before ADR-190 it guessed trainer flags and invented commands; now the overlay names `cadex train` and `cadex asset --put` as the caller's, and the real flags live in the `cadex train` contract, not in the agent's answer.
- [scope: exporting a training bundle | confidence: high | evidence: wise-tooth-2750] Do not rename exported artifacts to the output's name. The task bundle references its model by the staged basename and the trainer resolves it beside the task by that name; a renamed copy was tried first and the trainer needed a change to accept it.
- [scope: the CLI agent maintaining the project's documents | confidence: medium | evidence: odd-fern-7024, lucid-pebble-4000] Not given a file tool: its whole world is the engine on purpose, so the CLI pastes the documents in and records `DECISION:` lines and `PROGRESS.md` rows itself. The convention did prove too thin for the domain docs — the leg's design turn wrote none — and the answer was still not a tool: ADR-245 added `NOTE <subject>:` lines to the same channel, and the next live turn wrote both notes. ADR-256 closed the loop the other way without a tool either: the walk reads the exported model's declared sections and *reports* which notes are missing, because a CLI that wrote the note would be inventing the content the next turn reads back [rec: eager-lake-5745]. On a recipe walk that report is always "all missing", because no design turn ran — read the eye's count against whether the walk had a design leg at all [rec: western-gate-9567].
- [scope: iterating a walk project that lived under the system temporary directory | confidence: high | evidence: lucid-pebble-4000, fond-star-1809] Reaped between iterations, so there is no `DECISIONS.md` to write findings into and no conversation to `--resume`; the findings had to be restated in a fresh prompt. Walk a durable project outside the checkout when the unit is iterate.

## Provenance

- sweet-light-3396 — the lifecycle audit: twelve legs measured headlessly on the §7b toy, four agent-driven, three a person's, one blocked, two missing, two doc-only, and the frontier ordered in docs/MUJOCO.md §7c
- wise-tooth-2750 — ADR-189: cadex export hands over every staged non-BREP output under its staged name, the exported folder accepted by the trainer unchanged, §7c rows 3 and 7 closed
- restless-harbor-4224 — ADR-190: cadex asset --put and put_asset in the CLI tool surface, §7c row 5 closed, chain 3→7 run headlessly with only the trainer command by hand
- amber-moon-0981 — ADR-191: cadex train, the offboard trainer's dispatcher, documented and recorded, §7c row 4 closed, legs 3→7 one command plus cadex script --set
- keen-sail-4481 — ADR-192: iterate as the policy_on switch convention plus the curriculum pair on cadex train, §7c row 8 closed, the chain run on a scratch copy and pinned by a real-trainer test
- odd-fern-7024 — ADR-193: the project is a codebase — ARCHITECTURE/DECISIONS/PROGRESS scaffolded, pasted into every turn, one PROGRESS.md row per accepted run, DECISION: lines as ADR entries, §7c row 10 closed
- sunny-fern-8059 — ADR-194: the delta against the last row that carried the number, in the row; the project root git-inited on first visit and committed after every accepted run; §7c row 9 closed, the walk complete headlessly
- fierce-moss-6382 — ADR-195: §7c item 5 closed, the INSPECTION_FAILED frame is the one tool-failure envelope, the ordered frontier fully closed

- empty-wolf-3962 — broader charter criteria tracked as separate open gaps
- fond-mesa-1562 — fresh-toy audit confirms individual legs, not a general product walk
- shy-cabin-0798 — ADR-199: cadex walk qualified on the toy, the digest edit and review.json the walk's own, checkpoints and traces out of the project's history, 13 tests
- green-delta-7130 — ADR-200: the training leg's second home behind --remote, one trainer_flags source, the returned policy verified by sha256, warm start with --remote refused
- wild-marsh-9611 — ADR-200 follow-up: the scaffold's Training section and the (remote) row marker
- red-comet-9710 — ADR-201: GUI-attached walk and lock scope documented; scaffold/doc wording pinned; false shell file-tools claim corrected
- grand-fjord-0624 — corrects ADR-201: retry can silently overwrite CLI acceptance; refresh required before GUI edits; 137 CLI tests and 15 final scaffold tests passed
- sage-peak-2689 — unchanged walk qualified on slider/force and revolute/torque mechanisms; reward definitions separated and reproduction examples versioned

- happy-dune-3481 — inventory report, availability/count/path review block and project commit wired into the walk; bounded documented hinged-arm run passes
- clear-ash-2884 — ADR-238: clearance report, review summary and comparable progress counts committed without an extra rebuild; 161 CLI passes and both public recipes verified
- square-path-1173 — walk integrates committed revision-bearing previews; arm/carriage and CPU stand-in parity pass; sections remain unavailable
- modest-sun-6068 — separate fresh arm rehearsal verifies committed previews, preserved clearance and reward; full CLI gate 182 passed with zero skips
- copper-timber-8947 — walk sections share accepted preview snapshot; scaffold/docs agree, final CLI 195 passed; fresh complete-review rehearsal remains
- misty-tide-6394 — placement and cutter guidance delivered in overlay/scaffold; CLI 195 passed, agent compliance unmeasured
- lucid-pebble-4000 — ADR-245: the domain-doc convention gets a writer, `NOTE <subject>:` closing lines landing docs/<subject>.md and read back next visit; no file tool, no protocol op; 197 CLI tests, no skips
- fond-star-1809 — the leg walked again in a durable project with its review findings as design inputs: four positive-volume intersections removed, the 5 mm drive gap closed by a printed boss, six catalog instances still separately placed, actuator and sensor notes written; the repair turn on a live conversation remains
- twilight-snow-9071 — ADR-246 supersedes the outstanding walk-label defect; historical labels unchanged
- sunny-walrus-5847 — repair downstream legs unreached after credit refusal; baseline model/notes retained with dirty runtime metadata
- vast-ledge-4610 — controlled offline refusal separates truthful restore bookkeeping from redundant unchanged-session timestamps
- ready-otter-2700 — ADR-247 suppresses unchanged-session replacement; five refusal/success regressions and all 206 CLI tests pass; live repair remains unproven

- fresh-flint-1505 — three staged first-visit acceptances qualify Git ownership, ignore preservation and root versus nested index behavior
- little-fern-0464 — qualified guidance/scaffold mismatch corrected without changing Git behavior; full CLI gate 206 passed with no skips
- silver-rain-7333 — bounded consent and cold-recovery qualification preserves completed walk evidence; no missing leg or corrective unit
- calm-sky-2656 — linked-part qualification holds; no conditional correction or missing lifecycle leg, return to separate planning
- mellow-quartz-8093 — Model-free iterate is verified on the durable ot4 carriage
- still-harvest-7242 — guide corrected to distinguish child progress rows from successful walk clearance review; CLI 223 passed, no skips
- floral-canyon-9424 — carriage iterate guide shortened while preserving command, comparison and review limits; CLI 223 passed, no skips
- dawn-sky-3891 — failed fresh-directory retraining preserves prior policies and comparison history, retains the accepted sweep; CLI 223 passed, no skips
- dusty-vale-2809 — public CPU recovery preserves prior artifacts/history and compares against last successful training and rollout rows
- placid-ember-6741 — successful CPU recovery pinned in the real-engine regression, including preserved history and all four review outputs; CLI 223 passed, zero skips
- eager-lake-5745 — ADR-256: the walk's review reads the domain-note convention back from the MJCF it trained on; the write half stays exercised only by hand-placed notes and unit tests; CLI 227 passed, 0 skipped
- chilly-glacier-9294 — the ARCHITECTURE.md scaffold documents that review inside the project; the scaffold half is tracked on crisp-reef-5607
- western-gate-9567 — the ADR-256 eye read on both documented example walks: all subjects reported missing, correct for a recipe walk, and both examples gain docs/actuators.md written from the exported MJCF
- blue-quill-9477 — bounds that unit's reproduction claim to the one walk that exercised the flagless documented command
- sleepy-hollow-9498 — planner measurement that falsifies this node's "write half exercised only offline" claim: three ot4 projects carry notes committed by a `cadex prompt:` commit
- northern-comet-5917 — ADR-260: the walk's own PROGRESS.md row carries a delta for the first time (the walk branch never passed `previous`), and travel is spelled travel_mm/travel_deg so it can be read back; the carriage iterate's held travel against fallen reward is the worked example; CLI gate 233 passed
- young-timber-2761 — real quill stroke iterate with unchanged objective, both travel channels and bounded comparison caveats

- easy-beacon-3803 — ADR-262: fresh review exclusion, tested Git retention recipe and corrected force-add diagnosis; CLI 241 passed
- modest-prairie-3356 — ADR-263: independent seeds, objective/action identities and real CPU iterate evidence; CLI 244 passed

- humble-sky-8445 — resumed decisions/domain notes and fresh between-visit context delivered and preserved; no runtime omission; CLI 245 passed
- floral-grotto-1463 — fixed-stroke seed-0 reference reproduces exact reward/travel, verifies identities and preserves 48 prior run files plus project history

- honest-banner-0821 — fixed-input four-seed descriptive ranges and preserved project history, with action-midpoint caveat
- solar-snow-8543 — document history survives tested write/replacement errors; per-file limits; CLI 251 passed
- morning-arrow-5841 — ADR-265: the newest DECISIONS.md entries reach fresh and resumed turns inside the existing budget; four before-failing regressions, CLI 253 passed
- fresh-clover-8521 — ADR-266: §7c's unconditional initialization and commit promises corrected against the CLI ownership contract; scaffold distinguishes a row from a commit; CLI 253 passed
- idle-falcon-3004 — ADR-268: the remote dispatcher carries a warm start out and re-points it, so `cadex walk --remote` with the curriculum triple reaches the train leg and no other; stand-in dispatcher only, no ssh; CLI 258 passed, 0 skipped
