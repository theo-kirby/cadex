---
node_id: ff6cdc91-cf5a-51a4-a1b7-818df5e531ce
slug: forest-shade-2752
title: 'Bet: fix the walk''s absolute --out path, then the resume repair turn on the durable leg'
created_at: '2026-09-08T07:24:00+00:00'
parents:
- fond-star-1809
summary: ''
---
## What

Replace the landed iterate unit and the landed conditional domain-doc unit with two ranked units on the durable leg project: first the small CLI defect the walk itself named (the walk's `PROGRESS.md` row and project commit subject record `--out` as an absolute path, so a version-controlled project carries the operator's home directory in its history), then the one leg of the walk criterion that is still unexercised — a second design turn on a live conversation (`--resume`) that iterates from the run's own review documents rather than from a prompt that was told the answers, retrains, and lands the first like-for-like ADR-194 comparison. Fold the sixth clean walk, the first live ADR-245 evidence and the durable project into medium and long. No new direction, no charter-gap retirement, no Later promotion.

## Why

Both units short held are landed. The domain-doc correction shipped as ADR-245 [rec: lucid-pebble-4000] and the iterate ran clean end to end in a durable project: all four legs plus review in 630.22 s at 1,115,717,632 bytes peak, the design turn writing `docs/actuators.md` and `docs/sensors.md`, the four positive-volume intersections gone, the 5 mm drive gap closed by a printed boss, six catalog instances still separately placed, ninety bound comparisons passed [rec: fond-star-1809]. The short horizon therefore holds nothing, and the ladder's rule is that a clean run names the next unit.

It named two. The first is a defect in the walk's own artefact: the row and the commit subject in the durable project both read `walk 1 it × 4 envs → /Users/…/nt3-leg/runs/baseline`. `review.json` already relativises its paths under `--out`; the row and subject do not, because `_progress_subject` in `cli/cadex_cli/__main__.py` formats `str(args.out)` while the `train` branch beside it already takes only the name. Mission 2 asks for every artefact under version control in the project directory, and mission 9 asks for many machines running the same walk; a machine path in the project's history breaks both, and AGENTS.md forbids committing machine paths. It is one branch in the LGPL CLI zone with a test, so it goes first: it is certain, it is cheap, and if it lands before the repair turn, the repair turn's own row verifies it live instead of writing a second machine path into the durable project [rec: fond-star-1809].

The second is the walk criterion's remaining gap as the walk record states it: the iterate ran as a fresh conversation whose prompt carried the findings with their measured volumes, because the earlier temporary project had been reaped, so a repair turn on a live conversation is still unexercised [rec: fond-star-1809] [rec: lucid-pebble-4000]. The durable project now exists with a stored session (`agent.json` updated 07:13:57Z), five commits, the two notes and the generated `docs/clearance.md` and `docs/inventory.md`, which ADR-245 pastes back into the next turn's prompt. This is what the previous bet asked for and could not get [rec: weathered-hill-5955], and it is mission 2's iterate in its own words: change what did not work, retrain, compare. The reward expression is now fixed within this project, so the second row's total_reward change against 0.8530453495479725 is the first ADR-194 comparison that is like for like; the earlier cross-project rows were not comparable [rec: fond-star-1809] [rec: proud-beacon-8002]. The prompt must not restate the findings: the point is to measure whether the agent reads its own review back and acts on it. If `--resume` fails on a stale session, that is recorded as the finding for this leg and the documented fallback (a fresh conversation on the same project) is not silently substituted.

Catalog compliance has now been observed twice under the shipped guidance, once from a plain prompt and once from a modification-stated prompt; the pan-tilt rerun had fused, so this stays an observation and no diagnostic is selected [rec: proud-beacon-8002] [rec: fond-star-1809] [rec: morning-summit-7848].

Budget from the signals only: 185 iterations, 10.7 h elapsed, 4.3 h left, $82.49 API-equivalent, Claude five_hour 74% (up 61 this run), seven_day 54%, overage-included 99%, Codex seven_day 32%. Percentages establish no availability; a refusal on the design turn is recorded once and scheduling returns to the controller. The path fix spends no model tokens; the repair turn spends one design turn inside the existing 850 s / 2.9 GiB guard. Two units fit; a third is not justified. Cold catalog identity, worker races, bundle refresh, GUI launch and remote execution stay unselected [rec: early-gate-3510] [rec: lawful-dune-3795] [rec: clear-crest-9910].

## Method

Read the two newest records, the three horizons, the view root, the STATE lines for the three lifecycle criteria and the eyes criterion, the charter ladder, and `docs/CLI.md`'s walk and `--resume` contract. Confirmed the durable project exists with a stored session, six commits' worth of history, the two notes and the absolute path in both its `PROGRESS.md` row and its HEAD commit subject; confirmed the `walk` branch of the subject formatter takes `str(args.out)`. Mint one Bet parented to fond-star-1809 with three plan impacts, rewrite the short, medium and long bodies, advance the plan mark, sync and commit. No code, no state node, no charter edit.

## Result

Short: (1) relativise `--out` in the walk's `PROGRESS.md` row and commit subject, with a test that fails on the old behaviour, ADR line, CLI suite reported honestly; (2) the `--resume` repair turn on the durable leg project through `cadex walk`, prompt naming the review documents and not the findings, then train, declare, verify/rollout and review, with the like-for-like ADR-194 comparison and the catalog tally as evidence. Medium: the baseline becomes six clean walks including a durable one; ADR-245 has live evidence; catalog compliance observed twice. Long: the measured baseline gains the nt3-leg numbers. The human-owned checkboxes remain the human's.

## Repo

- repo: git@github.com:theo-kirby/cadex.git
- branch: ouroboros/nt3
- commit: cc4016246fbd256c8138c2396b2d8df3dfe60632

## State Impact

- target: plan/young-crane-9546 — both short units landed; short becomes the --out path fix and the --resume repair turn on the durable leg project
- target: plan/strong-birch-7412 — fold the sixth clean walk, the durable project, live ADR-245 evidence and a second catalog-compliance observation
- target: plan/late-valley-7350 — add the nt3-leg walk numbers to the measured baseline; standing maintenance updated
