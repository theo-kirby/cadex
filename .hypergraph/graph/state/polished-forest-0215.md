---
node_id: fa126afd-6bd8-51d7-896f-0c1aaa676854
slug: polished-forest-0215
title: F4. The agent repairs from measurements alone
created_at: '2026-09-14T17:28:05+00:00'
parents:
- mild-ledge-7157
summary: ''
---
Status: open

## Current

**F4 has one completed turn, and it is the run's first measured design result: the frozen repair prompt on `ot7-heron-repair-d` reached zero failing product checks static and swept, resolved two of the three ot6 defects, and declared the third, the 0.2 mm horn-to-link gap, as clearance rather than closing it [rec: stormy-snow-5452].** The repair prompt is spent, zero continuations are used and three are unspent; the next turn is `continue-1.prompt.txt` resumed on that project into the agent's own session, one turn per window, dispatched only when the first `rate_limit_event` frame shows room, since a turn of the observed size takes about half a five-hour window [rec: terse-mesa-7963]. No continuation was dispatched in iteration 49: the window stood at 74 % with the reset at 15:20 UTC [rec: terse-mesa-7963].

**The completed turn [rec: stormy-snow-5452].** Dispatched at 10:24:40 UTC on 2026-09-15, four minutes after a window reset at 8 % utilization, on a fresh validated copy of the seed (script `3339a178…`, revision `7e9eff5c…`, 15 failures before), through `run.py repair` under the ADR-356 collector, with no actor edit. The turn ended on its own after 1,461.9 s: 39 model messages, 46 tool calls (35 inspect, 9 edit_script, 1 describe_api, 1 rebuild), six accepted revisions and three rejected edits (two malformed replacement calls, one MuJoCo refusal of a plane on a non-static body). Failing count by accepted revision: 15 → 2 → 1 → 2 → 1 → 0 → 0; final revision `252e73b5…`, script `27680dc0…` (93 lines changed, 521 → 578), transcript 1,384,115 bytes captured frame by frame. Economics: 80,451 output tokens (65,887 thinking), 5.45 M cache-read input tokens, longest silent gap 254 s, no message at the 32,000-token cap; the window went from 8 % to 57 %. Receipt: `docs/probes/ot7/retained/repair-completed-d.json`, whose historical `status: exhausted` and `slots_spent: 1` are marked by a `ruling` field under ADR-357 [rec: terse-mesa-7963].

**What the collector measured, not what the agent said [rec: stormy-snow-5452].** Static fit on `252e73b5…`: pass, 120 pairs, 0 failing (seed 105 pairs, 15 failing). Swept fit: complete at 5°, shoulder [−90°, +65°] in 32 samples, elbow [−100°, +25°] in 26 samples, solved-pose agreement true, maximum common volume 0 on every pair at every sample; the 19 pairs at 0 mm are all declared contacts. The floor plane is gone: a grounded 400 × 400 × 8 mm `bench` slab component with a declared base contact replaced it after MuJoCo refused the plane. The buried tab (248.2 mm³ per joint) is resolved by cheek pockets and screw overlaps by holes at shank diameter. `servo_shoulder` and `servo_elbow` became `uncatalogued_sources` (spline bores cut into catalog bodies). Joint ranges narrowed from ±90°/±100° to the swept first contacts less 5°, mirrored into command limits, `sweep_step_degrees=5.0` declared. **Attachment assessment: fail.** `comp_horn_shoulder`/`comp_upper_arm` and `comp_horn_elbow`/`comp_forearm` sit at the seed's exact 0.2 mm; the agent declared both pairs *clearance, minimum 0.05 mm*, so the product's checker passes them. Against F4's bar: "accepts with zero failing fit checks" is met; "resolves all three defects" is not, two of three. No smoke rollout ran (repair mode does not request one; `cadex smoke` on d is a small follow-on).

**Slot accounting is settled by two ADRs, not by an owner ruling [rec: sunny-chart-3499] [rec: terse-mesa-7963].** ADR-356: a turn whose child exited `timeout` or `launch_failed` and is not void is **interrupted**, its slot returned and `continuations_used` held, counted in `interrupted_calls` apart from `void_calls` and `slots_spent`; a provider error the call returned on its own is `failed` and keeps its slot. So the `ot7-heron-repair-b` kill spent nothing; its receipt keeps the historical `slot_consumed: true`, `slots_spent: 1` values with a `ruling` field carrying decision #44 [rec: sunny-chart-3499]. ADR-357: the repair prompt is F4's first prompt, not its only one; `frozen('repair')` is the repair prompt followed by `continue-1`, `continue-2` and `continue-3`, the first prompt of any schedule counts no continuation, and a receipt written under the old rule resumes from its rows [rec: terse-mesa-7963]. Reconcile judgement: `stormy-snow-5452`'s "the repair slot is spent, F4 has no further frozen prompt" is superseded by `terse-mesa-7963`'s declared impact; F4 stays `open` with three continuations, and its iteration 48 measured result stands unchanged.

**Earlier calls on the same prompt (`5d846901…`, 830 bytes, unchanged throughout).** Four are void under ADR-355: three pre-restart calls refused at the session limit in about 4 s [rec: narrow-wave-7452] [rec: keen-wing-6569] [rec: blue-slope-0916], and `ot7-heron-repair-c`, dispatched into a window already at 95 %, cut off mid-turn at 101 % after six reads in 79.9 s, the first void call whose full stream (216,946 bytes) the runner retained itself; the seed was unchanged at 15 failures [rec: stormy-snow-5452]. One is interrupted: on `ot7-heron-repair-b` the model made 17 reads, then one message of 64,000 output tokens, 63,999 of them thinking, ending on the provider's output cap with no text and no tool call, and was still generating when the runner killed it at 1,800.0 s; no submit, seed unchanged; the stream was recovered from the Claude Code session store because the collector of that day wrote `transcript.jsonl` only after the turn returned (`docs/probes/ot7/retained/repair-timeout-b.json`) [rec: rare-birch-0755]. The two collector defects that attempt named are fixed by ADR-356: the stream is written frame by frame, and every CLI turn runs at an explicit effort with a 32,000-token per-message output cap [rec: sunny-chart-3499]. The pre-restart void calls are listed in `docs/probes/ot7/attempts/void-calls.json` [rec: narrow-wave-7452].

**The frozen repair collector.** Before/after `repair-assessment.json` receipts retain accepted identity and require static fit plus measured contact at `comp_horn_shoulder/comp_upper_arm` and `comp_horn_elbow/comp_forearm` (distance within 0.001 mm, common volume within 0.000001 mm³); fixtures catch the otherwise unflagged 0.2 mm gaps [rec: western-fox-7010]. The collector pins seed identity and empty overrides, requires complete unchanged before measurements, consumes hash-checked frozen prompts, retains before/after reports and transcript hashes even on refusal, and refuses an existing evidence directory, which is why a retry needs a fresh seed copy [rec: blue-sky-2193] [rec: blue-slope-0916]. Known-answer fixtures exercise the real pagination reader and fit summary [rec: long-spark-1984] [rec: true-wolf-3979]. Since ADR-357 `run.py repair` dispatches the repair prompt alone and pauses; `run.py resume PROJECT` dispatches exactly the next continuation with `--resume`, refusing a closed project or a design changed by anything but a product-agent turn [rec: terse-mesa-7963].

**The seed's measured baseline.** Heron's first accepted ot6 revision `7e9eff5c…`: 105 unique pairs, 15 inventory components; 91 pairs clear; shoulder servo/base overlap 248.20162986795066 mm³; both horn/link gaps about 0.2 mm, not failing because the seed declares no contact intent; swept coverage unavailable [rec: long-falcon-7461]. ADR-353 removed six nominal-0.1 mm flags, explaining an earlier 21-failure restored report becoming 15 without a design change [rec: lucky-willow-8039] [rec: keen-quill-2265].

The design-agnostic prompt is frozen at `docs/probes/ot7/prompts/repair.prompt.txt`, wording test-pinned [rec: silent-union-5108]. Charter criterion: an unassisted session on the first Heron revision resolves all three defects from tools and frozen continuation prompts, accepting zero failing fit checks, with before/after reports and turn/transcript evidence [rec: kind-dusk-1609] [rec: keen-wing-6569].

## Negative knowledge

- [scope: the F4 repair prompt on `ot7-heron-repair-b` under the CLI's then-unbounded thinking and the runner's 30-minute turn bound | confidence: medium | evidence: rare-birch-0755, sunny-chart-3499] The model reads the measured fit but then thinks to the output cap without submitting; the turn ends on the runner's clock, not the model's. ADR-356 bounds every message since; on d no message reached the cap.
- [scope: the product's declared-intent checker (F2) on an attachment the design never declared | confidence: high | evidence: stormy-snow-5452] A horn 0.2 mm from the link it drives is never a failing row: 0.2 mm clears the 0.1 mm default and no contact was declared, so the agent declared it clearance and the checker passed it. Only the collector's ot6-informed attachment assessment sees it. Whether a horn on a spline should imply contact is a product decision not taken.
- [scope: dispatching a frozen prompt after a one-word probe answers | confidence: high | evidence: stormy-snow-5452] An answering probe proves nothing about room in the window: `ot7-heron-repair-c` was dispatched at 95 % and cut off at 101 %. The first `rate_limit_event` frame of a turn carries the utilization and is the signal.

## Provenance

- kind-dusk-1609 — the ot7 directive (ADR-341) declared this criterion as gap `gap-f4-agent-repairs-from-measurements`
- silent-union-5108 — ADR-345 prompt freeze recovered with digest pins; no design or repair evidence
- lucky-willow-8039 — one frozen invocation refused, zero completed design turns; restored seed identity and 21-failure report retained
- keen-quill-2265 — second refusal, zero completed turns, 15 before/after failures and unchanged accepted identity
- blue-sky-2193 — seeded-repair collector and fixtures preserve identity and frozen prompting; no provider invocation or real repair
- long-spark-1984 — static pagination and late-page-error fixtures; mutation detects lost failures
- true-wolf-3979 — nested sweep pagination fixture and mutation; CLI 706 passed / 1 skipped
- long-falcon-7461 — real unchanged-seed baseline and retained artifacts; 15 failures, unflagged horn gaps and unavailable sweep
- western-fox-7010 — before/after geometry assessment requires static fit and both original horn contacts; fixtures verified, no real repair
- blue-slope-0916 — third refusal at the session limit in 4.171 s; receipt retained, 15 failures and both 0.2 mm gaps unchanged
- keen-wing-6569 — the ot7 restart directive (ADR-355): usage-limit calls are void and consume no slot; the repair prompt is unspent
- narrow-wave-7452 — the three F4 calls classified void by the runner's code; retry named `ot7-heron-repair-b` on a fresh seed copy
- rare-birch-0755 — the repair prompt reached the model on `ot7-heron-repair-b`: 17 reads, a 64k thinking burst, no submit, killed at 30 minutes; seed unchanged
- sunny-chart-3499 — ADR-356: the b kill is interrupted and returns its slot; streams captured frame by frame; every CLI turn effort-pinned and output-capped
- stormy-snow-5452 — the repair prompt completed on `ot7-heron-repair-d`: zero failing product checks static and swept, two of three defects resolved, horn gap declared as clearance; c void
- terse-mesa-7963 — ADR-357: the repair prompt is the first of four; F4 open with three continuations; runner pauses per window and resumes without replay; no dispatch at 74 %
