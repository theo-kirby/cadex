---
node_id: 03afbde2-2f51-5b20-af37-18276eeb7e44
slug: flat-cove-2253
title: 'F5: the arm create turn completed on ot7-heron-c at medium in 1,530.4 s — accepted two-DoF arm, static fit 7 of 120 failing (six screw engagements, one world bench), no sweep declared, servos and horns uncatalogued; one in-turn repair 8 → 6; describe_api at 82,523 characters still refused, cap bounded 20,717–82,523; create slot spent, continue-1 next after the 07:20 UTC reset'
created_at: '2026-09-16T02:54:10+00:00'
parents:
- crimson-wind-8698
summary: ''
---
## What

The F5 arm create turn, dispatched and completed: the unchanged frozen `heron.create.prompt.txt` (digest `bcda5af5…`) on the fresh project `ot7-heron-c` through `run.py heron … --turns 1`, at effort `medium` (ADR-359), two minutes after the 02:20 UTC reset with the runner's probe reading 5 %. The turn ended on its own in 1,530.4 s (exit 0; 123 model messages, 35 thinking blocks, no output-cap hit, 82 tool calls: 1 `describe_api`, 68 `inspect`, 6 `write_script`, 6 `edit_script`, 1 `rebuild`). The create slot is spent; all three continuations are unspent; the runner left the attempt `paused` with `continue-1` next. No actor edited any design.

**The measured result.** Three accepted design revisions, each with the product's fit summary in its reply: `c186fe25…` (105 pairs, 8 failing: six screw intersections and two declared horn-to-screw contacts missed by 0.55 mm), `9f7b39c4…` after one measurement-led edit that restored the catalog horn's bore (105 pairs, 6 failing), and `512c157c…` after the agent added a bench slab marked `world=True` and welded to the base (120 pairs, **7 failing**: six intersections of 4.0715 mm³ per M2×6 tab screw and 18.096 mm³ per M2×16 centre screw, which the agent's closing message calls thread engagement matching analytic values, plus `bench` reported as world geometry by the F2 check). The **swept check is unavailable**: both revolute joints declare limits (±90°, ±100°) but the script declares no `sweep_step_degrees`, so nothing was published to sweep. The inventory lists the two servos and two horns under `uncatalogued_sources` (a tap-drill bore cut into the servo shaft, horns re-clocked on the spline); the two MR128 bearings and six screws keep catalog identity. Eleven contacts declared, no clearances. Five `DECISION:` lines and three `NOTE` lines written into the project. Against F5's bar the revision fails on three counts: 7 failing static checks, no swept measurement, two purchased families uncatalogued.

**Two tooling facts the transcript measured.** (1) The ADR-359 `describe_api` view, 82,523 characters, was **still refused** by the harness ("exceeds maximum allowed tokens", written to a file the agent has no tool to read); the largest tool result the harness accepted in the turn was 20,717 characters, so the cap lies between those numbers and the 90,000-character budget does not reach it. The agent paged the contract through 43 `inspect scope=api` reads in 2 min 40 s (iteration 55: 44 reads, 3 min 51 s). (2) `medium` fits the bound: one thinking message of about 30,000 estimated tokens ended on its own; iteration 55's three cap-limited messages did not recur.

**Evidence committed.** `docs/probes/ot7/retained/heron-create-c.json` (9,259 bytes: prompt digest, window readings, timing, the twelve-edit sequence with each result, the failing set with distances and volumes, the sweep status, the inventory verdict, the decisions added, the stream digest `4bd8071a…` and the measurement digests); REPORT.md's F5 paragraph, remaining-slots row, F1–F9 row and closing section updated, and an "Iteration 57" section with the timeline, what it measures and the ot6 comparison; the runner README's iteration 56 describe_api claim corrected forward and an iteration 57 section with the dispatch and resume commands. Transcript, exports and measurements stay in the project under `evidence/turn-0/`.

## Why

The critic's message: once the window resets and the gate permits, dispatch the unchanged frozen arm prompt on fresh `ot7-heron-c` at medium, then use its continuations as measurements require, and verify from the transcript whether `describe_api` fits the harness cap. The window read 3 % at 02:22 UTC, so the dispatch was the unit's first action. The continuations were **not** sent: the turn took the window from 5 % to 64 %, over the runner's 45 % bound, and ADR-355/ADR-358 forbid dispatching a design turn without room; `continue-1` is the next unit's first action after the 07:20 UTC reset, through `resume`. The describe_api question is answered by measurement: it does not fit, and the bound on the cap is now known to within a factor of four. No tooling change was made this iteration, because a change to the bridge is a separate recorded decision and the turn's evidence had to be written first. Advances F5 (`stormy-aspen-5433`), which now has its first completed turn and a fit report, and the ot7 charter root (`mild-ledge-7157`).

## Method

1. Read the last record and the runner README's dispatch command; confirmed the time (02:22 UTC) against the reset, probed the window (3 %, room), confirmed no `ot7-heron-c` existed and the engine was built.
2. Dispatched `run.py heron "$PROJECTS/ot7-heron-c" --model claude-fable-5 --turns 1` in the background and watched the stream with a monitor on script writes, edits, rebuilds, output-cap hits and the runner's exit, reading each accepted revision's fit block as it landed.
3. After the runner exited: read the receipt, the fit, clearance and inventory measurements, the closing message, the stream statistics (messages, thinking blocks, cap hits, tool calls, first and last `rate_limit_event`), the accepted script's declarations, the project's DECISIONS.md, and the digests; probed the window again (64 %, no room).
4. Wrote the retained receipt from the receipt and measurements; edited REPORT.md and the runner README; ran `cli/tests/test_ot7_prompts.py`, `test_retained_fit.py` and `test_ot7_runner.py` (79 passed).

## Result

**What is true now.**

- F5 has one completed create turn on `ot7-heron-c` (1,530.4 s, effort `medium`, no cap hit), an accepted design at `512c157c…` with static fit failing 7 of 120 checks, no swept measurement (no sweep step declared), and servos and horns uncatalogued after modification. The create slot is spent, `continue-1` is next through `resume`, and all three continuations are unspent.
- The product's fit report reached the agent in every build reply and it repaired from it once inside the create turn (8 → 6 failing) without a continuation; it then accepted six explainable intersections and a reported world component rather than treating the report as the bar.
- The harness refuses `describe_api` at 82,523 characters and accepted results up to 20,717 characters in this turn; ADR-359's 90,000-character budget is not a fit. `medium` keeps a create turn inside the 30-minute bound.
- The window read 64 % after the turn (reset 07:20 UTC); no design turn may be dispatched until the probe reads at or under 45 %.
- No engine source, protocol, payload, prompt byte or design changed; no build or packaged gate needed. Suites: the three ot7 doc tests, 79 passed; the full suites were not re-run for a docs-and-receipt change.

**Concerns and assumptions the next iteration must know.**

1. The next unit's first action, when `run.py window` reads room after 07:20 UTC, is `run.py resume "$PROJECTS/ot7-heron-c"`, which sends `continue-1` (digest `80d725d2…`) at the receipt's `medium`. The continuation asks, without naming anything, for every failing check resolved at rest and through every joint's range, every purchased part a catalog part, and nothing from the world in the design, which is exactly the three counts this revision fails on. Whether the agent declares a sweep step, removes the bench and restores the catalog bodies is the measurement that turn makes.
2. A `describe_api` cut below the harness cap is a separate tooling unit under a new ADR, to be done while the window is limited, not on a design project. The compact rendering ADR-359 measured (69,587 characters) is above the 20,717 accepted, so the cut has to be of a different kind (an index of names and signatures with descriptions on demand, or a paged reply); the choice is that unit's decision, and the bound is 20,717 < cap ≤ 82,523 characters as the harness counts them.
3. The agent's six thread-engagement intersections raise a charter question the record does not decide: F2 provides declared contact and clearance but no declared engagement, so a screw modelled in a tapped hole always fails the overlap check. That is what the charter ordered ("any overlap on any pair") and the continuation prompt asks the agent to resolve it; if the agent resolves it by retracting screws to tip contact as it did on `ot7-heron-repair-d`, the design passes at the cost of a modelled engagement. This is reported, not changed.
4. The unreconciled tail is now three records (`mellow-summit-9733`, `crimson-wind-8698`, this one); a reconcile is due next by the charter's rule of three, before or alongside the continuation, as the critic names.

Dispatch closed: 1 unit — the F5 arm create turn completed on `ot7-heron-c` at medium in 1,530.4 s with no cap hit, an accepted two-DoF arm whose measured static fit fails 7 of 120 checks (six screw engagements, one world bench), no sweep declared and two purchased families uncatalogued; one measurement-led repair inside the turn (8 → 6); describe_api at 82,523 characters still refused, cap bounded between 20,717 and 82,523 characters; create slot spent, three continuations unspent, `continue-1` next through `resume` after the 07:20 UTC reset.

## Repo

- repo: git@github.com:theo-kirby/cadex.git
- branch: ouroboros/ot7
- commit: 516c71351224884a71c7b89c54d917434a5683fc

## State Impact

- target: stormy-aspen-5433 — F5 has its first completed turn (iteration 57, effort medium, ADR-359): the frozen create prompt on fresh ot7-heron-c ended on its own in 1,530.4 s with no output-cap hit, 82 tool calls and three accepted design revisions; the last, 512c157c, is a two-DoF MG90S arm whose measured static fit fails 7 of 120 checks (four 4.07 mm³ tab-screw and two 18.10 mm³ centre-screw intersections the agent calls thread engagement, and a bench slab marked world=True reported as world geometry by F2); the sweep is unavailable because no sweep_step_degrees is declared; the inventory lists both servos and both horns as uncatalogued after modification while bearings and screws keep catalog identity. The agent repaired once from the reply's measurements inside the turn (8 → 6 failing) and then accepted explainable intersections as fit. Create slot spent, all three continuations unspent, next prompt continue-1 through run.py resume, dispatched only when the window reads at or under 45 % (it read 64 % after the turn, reset 07:20 UTC). Evidence: docs/probes/ot7/retained/heron-create-c.json, REPORT.md iteration 57, the stream and measurements under the project's evidence/turn-0
- target: chilly-union-8972 — Measured on ot7-heron-c: the ADR-359 describe_api view at 82,523 characters is still refused by the harness's tool-result cap and written to a file the product agent cannot read; the largest tool result the harness accepted in the turn was 20,717 characters, so the cap lies between 20,717 and 82,523 characters as the harness counts them and the 90,000-character budget does not reach it. The agent recovers by paging inspect scope=api (43 reads, 2 min 40 s). A further cut is a separate recorded decision. Effort medium at the 32,000-token cap kept a create turn inside the 30-minute bound with no cap hit
- target: first-snow-5587 — REPORT.md carries F5's first completed turn: the F5 paragraph, the remaining-slots row (1 completed turn, create spent, 3 of 3 continuations unspent), the F1–F9 row and the closing section updated, and an Iteration 57 section with the timeline, the three counts the revision fails F5 on, and the ot6 comparison; the retained receipt heron-create-c.json is linked; the runner README's describe_api claim is corrected forward and an iteration 57 section carries the dispatch and resume commands
- target: mild-ledge-7157 — Iteration 57 dispatched the F5 create turn after the reset (window 3 % before, 64 % after): completed, slot spent, measured fit failing 7 of 120 with no sweep and two purchased families uncatalogued; F6 and F7 untried with every slot unspent; F4 exhausted; next unit is continue-1 on ot7-heron-c through resume when the probe reads room after 07:20 UTC, or a describe_api cut under a new ADR while limited; the unreconciled tail is three records and a reconcile is due
