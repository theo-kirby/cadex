# cadex — State

> Generated by `tools/hypergraph.py render` from the state-graph export.
> Do not hand-edit — run the hypergraph-reconcile skill instead.

Reconciled through `tidy-banner-0293`, `shy-glade-0050`, `empty-light-4558`, `neat-tower-5715`, `careful-key-9041`, `weathered-falcon-4350`, `deep-branch-6721`, `sleepy-shade-1485`, `shady-lodge-6077`, `wild-prairie-9912`, `quiet-creek-7756`, `idle-arrow-2946`, `candid-creek-2410`, `pale-river-6583`, `ancient-crest-4588`, `proud-cliff-9629`, `curious-badger-0797`, `amber-glade-2813`, `lively-grove-8186`, `hidden-reef-8369`, `simple-quartz-9812`, `royal-water-8317`, `weathered-sage-2750`, `falling-ocean-4411`, `honest-path-3451` at 2026-09-13T00:27:45+00:00.

## Frontier

- [open] **Inherited-tree reduction, and the unscheduled replacements** (`round-glacier-2865`) — The standing work of shrinking both inherited trees in place, under the two-commit removal protocol: dependency audit → a **disable commi…
- [open] **Live headless project review — the ot5 charter (ADR-284)** (`crisp-sun-1239`) — **Owner-directed work for run `ot5` (ADR-284): make a headless Cadex project observable while work happens and reviewable afterward** [re…
- [open] **Parts library — hardware catalog and remaining families** (`brave-stone-9609`) — Catalogued hardware is available as the **lib script namespace** over `CadexCatalog`, composed as parametric BREP values with source-qual…
- [open] **The RL training loop and mg-legs** (`late-pond-2851`) — **The training loop itself is proven, locally, end to end** — what remains blocked is gait scale. The whole arc (agent-authored mechanism…

## Architecture

- **cadex — state** (`nimble-pine-0740`)
  - [working] **The engine — cadexd and the xscript pipeline** (`forest-wind-0342`) — The engine is a FreeCAD fork at the repository root, stripped to one AI-native modelling engine. A release build produces `FreeCADCmd` an…
    - [working] **The wiring and electronic-assembly surface** (`clear-banner-8342`) — Procedural wire routing and the electronic-assembly surface, marked **experimental** in `docs/ARCHITECTURE.md` and carrying stated limits…
    - [open] **Parts library — hardware catalog and remaining families** (`brave-stone-9609`) — Catalogued hardware is available as the **lib script namespace** over `CadexCatalog`, composed as parametric BREP values with source-qual…
  - [working] **The shell — the Blender fork and mesh_agent** (`shy-crane-2573`) — The shell is a Blender fork under `shell/` carrying `mesh_agent` as application code under `shell/scripts/startup/` [rec: curious-sail-83…
    - [working] **File lifecycle — open, save, Save-As, and .blend vs .cadex** (`simple-willow-8989`) — The file-handling path inherited from Blender — opening, saving, Save-As, creating a new file, how the `.cadex` project directory relates…
  - [working] **The headless CLI** (`chilly-union-8972`) — **Walk run recording and a read-only review reader exist (ADR-285).** `cadex walk` writes `runs/<name>/run.json` beside `review.json` at …
  - [working] **Dynamics and control on MuJoCo** (`salty-isle-4063`) — Dynamics and control is ordinary product surface on the one branch, not a vertical on a fork [rec: open-key-6334]. It is operations on th…
    - [open] **The RL training loop and mg-legs** (`late-pond-2851`) — **The training loop itself is proven, locally, end to end** — what remains blocked is gait scale. The whole arc (agent-authored mechanism…
    - [working] **The robot lifecycle walk — design to policy to iterate, agent-driven and headless** (`calm-peak-5247`) — **Recent project decisions now reach the revisiting agent (ADR-265).** The project-as-codebase contract had a hole under its own budget: …
  - [working] **Organic modelling and the CAD/mesh interface** (`idle-lantern-9094`) — Making the shapes a person actually asks for — a body, a limb, a skin over a mechanism — buildable by the agent and shapeable by the user…
  - [working] **Orientation, build and the working environment** (`early-arbor-7123`) — **Read this before touching anything.** The author's answer to "what would waste a fresh agent's whole day" was immediate and was not abo…
  - [open] **Inherited-tree reduction, and the unscheduled replacements** (`round-glacier-2865`) — The standing work of shrinking both inherited trees in place, under the two-commit removal protocol: dependency audit → a **disable commi…
  - [working] **Structural analysis, topology optimisation and shape search** (`kind-marsh-2645`) — Stress-testing a part, finding where material can be removed safely, and
  - [working] **Compliance and licensing** (`easy-wind-9848`) — The repository's compliance and licensing posture after the ADR-171 audit (PR #13, version 0.0.7) [rec: wild-sea-9905]:
  - [working] **The walk exists and is tested headlessly** (`crisp-reef-5607`) — Charter criterion: **The walk exists and is tested headlessly.** One documented entry point (a CLI prompt or a headless script) takes a m…
  - [working] **Three modes, one shape** (`witty-spark-2613`) — Charter criterion: **Three modes, one shape.** The walk runs headless (exercised), with the GUI attached (documented, not exercised while…
  - [superseded] **A fresh machine runs the walk** (`ancient-sun-3952`) — Open charter criterion: **A fresh machine runs the walk** after one documented install script, headlessly, with no step that needs a pers…
  - [superseded] **The catalog is broad enough for a robot prompt** (`ancient-tide-5930`) — Open charter criterion: **The catalog is broad enough for a robot prompt**: at least five servos, ten actuators, a bearings family, and M…
  - [working] **Phase 8 `src/Gui` delete commit landed** (`civic-sand-2641`) — **The Phase 8 `src/Gui` directory-delete criterion is met (ADR-214).** After separate metatype and disable prerequisites, thirteen GUI di…
  - [working] **The agent can see its work without a screen** (`damp-moon-9297`) — Charter criterion: **The agent can see its work without a screen.** One CLI call each, with outputs landing in the project directory: ren…
  - [superseded] **A variant study exists** (`easy-bloom-3674`) — Open charter criterion: **A variant study exists**: several variants of one joint generated by one script, tested the same way, ranked in…
  - [superseded] **The robot prompt works unattended** (`fond-clover-3062`) — Open charter criterion: **The robot prompt works unattended**: from a prompt naming the servos, bearings, and hardware, the loop produces…
  - [superseded] **The fork's delta against upstream is smaller than at the start of this run** (`green-sea-3991`) — Open charter criterion: **The fork's delta against upstream is smaller than at the start of this run**, measured by the delta manifest AG…
  - [superseded] **Compound mechanisms exist as parametric library values** (`idle-tower-1624`) — Open charter criterion: **Compound mechanisms exist as parametric library values** built from catalog parts: a rack and pinion and a plan…
  - [superseded] **A research report renders headlessly** (`late-sky-8010`) — Open charter criterion: **A research report renders headlessly** from a project's `PROGRESS.md` numbers into a document with graphs a per…
  - [working] **L2 boards family with real-kernel and packaged verification** (`ready-falcon-6286`) — L2 boards exist over `CadexCatalog` through `lib.board`: ESP32-DevKitC V4 (WROOM-32E), Pi Zero 2 W and Adafruit 815 PCA9685 revision C. S…
  - [superseded] **25T horns and servo pigtails come from manufacturer STEP sources** (`red-loom-6298`) — Open charter criterion: **25T horns and servo pigtails come from manufacturer STEP sources**, with the provenance recorded the way `docs/…
  - [superseded] **L3 motors and mechanisms families exist** (`rising-banner-4325`) — Open charter criterion: **L3 motors and mechanisms families exist** over `CadexCatalog`, same test shape as the boards family, and the pa…
  - [superseded] **One mechanism in the repo came from a paper or a real product** (`spring-shore-5413`) — Open charter criterion: **One mechanism in the repo came from a paper or a real product**: the source is cited in the project's `DECISION…
  - [working] **The walk holds on a second mechanism** (`swift-dusk-2951`) — Charter criterion: **The walk holds on a second mechanism.** The same entry point, with no code change specific to the mechanism, takes a…
  - [working] **The `hide_render` shell bug from `docs/IDEAS.md` is fixed** (`wild-comet-8096`) — **The `hide_render` shell defect is fixed with a regression that fails on the old source**, meeting the declared charter criterion [rec: …
  - [working] **Two Phase 13b engine-side removals landed** (`windy-pebble-4630`) — **Two Phase 13b engine-side whole-tree removals are complete:** Help and Start each have separate disable/delete commits, ADRs and record…
  - [superseded] **mg-legs tips at the declared shove band, backward first** (`salty-fjord-6965`) — Open charter criterion: **mg-legs tips at the declared shove band, backward first**, with the numbers in the project's `PROGRESS.md`. [re…
  - [open] **Live headless project review — the ot5 charter (ADR-284)** (`crisp-sun-1239`) — **Owner-directed work for run `ot5` (ADR-284): make a headless Cadex project observable while work happens and reviewable afterward** [re…
    - [working] **D4. Policy videos render, persist and play headlessly** (`candid-harvest-2614`) — **Wren's checkpoint-20 video was verified, rendered, played and downloaded while training remained active; its final-policy video is also…
    - [working] **D3. Training is visible while it runs** (`dawn-delta-4361`) — **Wren's first GPU run retained 240 points in each reward, loss and episode-estimate history.** The persistent browser observed seven upd…
    - [working] **D1. A live project dashboard is reachable** (`jolly-loom-0622`) — **D1 has its documented command and private-address browser smoke evidence (ADR-286).** The one-project inspection dashboard serves accep…
    - [working] **D5. Review history survives a design change** (`sharp-union-6036`) — **Wren's 85→105 mm foot edit preserves all 114 retained run files byte-for-byte.** Persistent-browser checks measure 85 mm historical mes…
    - [working] **D2. The browser shows the right model and specs** (`shy-meadow-0959`) — **Wren's repaired accepted display passes private-address browser review:** eight solids, twelve declared parameter defaults, accepted id…
    - [working] **D6. Save, reopen and restart preserve the project** (`clever-field-7845`) — **In-place restore now preserves accepted tessellation (ADR-303); D6 returns to `working`.** Identical revision/digest replay without an …
    - [working] **D7. Save-As/copy produces an independent project** (`cold-vale-4232`) — **Real Reed copy/edit/GPU retraining and source-unavailable reopen now prove D7.** A stopped-writer whole-directory copy initially matche…
    - [working] **D8. Interrupted and failed runs remain understandable** (`cool-gate-3332`) — **A successful new GPU attempt now follows the retained real interruption, with both outcomes readable in the browser.** Probe3 exits 0 a…
    - [working] **D9. The fresh biped completes the whole recorded lifecycle** (`silent-river-6649`) — **Reed has completed the recorded lifecycle, including the product-agent revision, retraining, comparison and test-guarded D1–D8 report.*…
    - [working] **D10. The persistent operator dashboard stays current** (`deep-clover-6012`) — **Persistent port 8765 remains on `ot5-wren`: latest attempt `wren1-final` is visibly historical relative to accepted revision `a90b84033…
    - [working] **D11. Viewport and videos match the neural-whoop visual reference** (`fair-wolf-4645`) — **One shipped browser scene now renders both the live viewport and the recorded policy videos in the neural-whoop style; the charter evid…
