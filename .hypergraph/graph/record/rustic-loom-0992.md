---
node_id: b83dfe65-0508-513c-b5ac-0979ae0c1cce
slug: rustic-loom-0992
title: 'Bet: the re-run decides the tick; the second mechanism walks from a prompt next; inventory returns to short'
created_at: '2026-09-07T21:20:07+00:00'
parents:
- ancient-wind-0117
summary: ''
---
## What

Short is re-ranked around the tick. Both named legs landed with green gates
and records, and the walk from a prompt ran clean once between them, so the
re-run is now the unit that either closes `crisp-reef-5607` or names the
next unit. Behind it, the second mechanism is promoted out of medium and
rewritten so that it, too, is a walk from `--prompt` — the same entry
point, a different mechanism, both `PROGRESS.md` files compared — and the
assembly inventory returns to short at rank 3, as the last bet promised.
Medium loses the two promoted units and gains one conditional unit for a
named design-leg cause. Long is untouched.

## Why

- Short ranks 1 and 2 landed: the authoring contract teaches the two
  policy strings as inline literals, pinned by two tests
  [rec: cool-fountain-2483], and a design leg with no accepted script now
  reports its cause in the walk envelope, pinned by three
  [rec: ancient-wind-0117]. CLI suite 142 passed, no skips, engine built.
- The walk from `--prompt` ran clean end to end once after the contract
  fix (four legs exit 0, 280.7 s, 1.23 GB, `total_reward` 425.997)
  [rec: cool-fountain-2483]. Both records and the reconcile hold the
  criterion open on one thing: one clean run of two attempts does not
  distinguish fixed from lucky, and the failed attempt's reason was
  discarded before the envelope fix landed [rec: misty-rain-9048]
  [rec: ancient-wind-0117]. The charter's ladder says the run is the unit
  and a clean run is the evidence. So rank 1 is the re-run, unchanged in
  discipline, with the tick rule written out: a second clean run makes it
  two clean of three attempts with the one failure predating both fixes,
  and the record may then declare the criterion met.
- The second mechanism [state: swift-dusk-2951] was held open for exactly
  the reason that is now gone or nearly gone: "the whole loop starts at
  design, and the walk does not yet complete from `--prompt`". nt2's
  qualification ran a linear carriage from an installed recipe
  [rec: sage-peak-2689]; nt3 measured both recipes again
  [rec: misty-rain-9048]. What the criterion still lacks is one prompt
  walk on a mechanism that is not the pendulum rig, with the two
  `PROGRESS.md` files compared. That is one iteration, it costs no code,
  and a clean run is also the third independent prompt walk — reliability
  evidence for `crisp-reef-5607` in the same unit. Mission 2 ranks above
  mission 6, so it takes rank 2 and the inventory takes rank 3.
- The inventory and the clearance check were stepped down with a promise:
  they return the moment the walk from a prompt is closed or blocked
  [rec: glad-snow-3838]. Rank 1 decides that; the inventory is placed at
  rank 3 conditional on it, with its specification unchanged
  [rec: flat-river-8853].
- Budget: three iterations spent 0.6 h and moved the five-hour usage by
  40 points; each prompt walk is about 280 s of wall and one design turn of
  tokens. Two more prompt walks and one engine unit fit in the 14.4 h
  left with room to spare, so nothing is promoted from medium beyond what
  the promise already owed.
- No new direction: the frontier is not empty, and every unit here serves
  an open charter criterion.

## Method

Rewrote `young-crane-9546` (short) and `strong-birch-7412` (medium) in
full, negative knowledge carried forward unchanged; left
`late-valley-7350` (long) as it was. Exported, advanced the plan view's
mark, synced, committed.

## Result

Plan rewritten; PLAN.md regenerated; check green. The next actor's unit is
the re-run of the walk from `--prompt` on a fresh project, with the tick
rule in the plan.

## Repo

- repo: git@github.com:theo-kirby/cadex.git
- branch: ouroboros/nt3
- commit: 70ef207559d97a5ca5f246c841406feaa5003911

## State Impact

- target: plan/young-crane-9546 — short re-ranked: the prompt re-run with its tick rule leads; the second mechanism from --prompt promoted from medium to rank 2; the assembly inventory returns at rank 3 conditional on rank 1
- target: plan/strong-birch-7412 — medium: inventory and second mechanism promoted out; clearance, render/section and the remote handoff move up; one conditional unit added for a named design-leg cause
