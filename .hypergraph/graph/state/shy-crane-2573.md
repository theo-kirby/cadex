---
node_id: b270d71a-7c79-5efb-afe8-4ded59f911eb
slug: shy-crane-2573
title: The shell — the Blender fork and mesh_agent
created_at: '2026-08-09T15:21:41+00:00'
parents:
- nimble-pine-0740
summary: ''
---
Status: working

## Current

The shell is a Blender fork under `shell/` carrying `mesh_agent` as application code under `shell/scripts/startup/` [rec: curious-sail-8332]. It is the application a user launches, and it ships the engine inside its own bundle, discovered through a `cadex-engine.json` manifest so a built application needs no configuration [rec: simple-hollow-8675] [rec: merry-eagle-4093].

- It is a **protocol client and nothing else**. It may not import anything from `src/`; `cadexd_client.py` is deliberately a plain GPL NDJSON client with no cadex imports. Being in one repository does not relax this [rec: simple-hollow-8675] [rec: merry-eagle-4093].
- Product surface: viewport, chat, parameter sliders, model tree and script view; native macOS File/Edit menus; Wiring, Environment, Policy, Training and Live editors; linked-part and printable-export actions; presentation toggles in the Parameters Interface grid [rec: curious-cloud-7186] [rec: crisp-glacier-6395] [rec: mellow-hawk-8610] [rec: ancient-current-9419] [rec: still-wave-6655] [rec: civic-glade-9153]. The **Blueprint Editor** is the seventh Cadex editor; its drawing controls live in its own header, and the Parameters panel no longer has the blueprint settings box [rec: wild-prairie-9912].
- **The presentation views are registry-wired** (ADR-150): `cadex_views.py` holds one record per view — collision 20, section 30, explode 40, dimensions 50, blueprint 60 — with optional `on_hydrate`/`on_preview`/`suspend` hooks, and `cadex_backend.hydrate`, `_finish_preview` and `capture.render_views` walk the registry instead of carrying five copies of the same hand wiring. Per-record try/except preserves each view's stated failure terms (a malformed record costs its view, never the geometry), `suspend_for_render()` returns one undo unwinding in reverse order, and the accepted `{display, revision}` write is hoisted outside every view's failure domain. Proven no-behavior-change by the untouched gate before any new view landed [rec: windy-wolf-5012].
- **Mesh tools use one authenticated TCP bridge with two transports:** MCP via `mcp_shim.py` for Claude/Codex, and `pi_tools.js`, a native pi extension. Each backend translates into the same text/tool/final event contract [rec: forest-chart-2781] [rec: merry-water-7647]. `get_script` serves the full script or an `offset`/`limit` window with range and next-offset metadata; read limits must not be answered by deleting source [rec: shy-glade-0050]. `make_blueprint` drafts and `save_blueprint` stores; both are `_ENGINE_TOOLS`, neither is a scene-mutating tool [rec: winter-bloom-8543].
- **`cadex_dimension.py` draws a declared measurement as a drawing sheet would** — extension line at each anchor, dimension line between them, the number in the middle with the line broken around it. Everything except the two anchors is computed **in screen space**: the offset direction is perpendicular *on the screen*, so it cannot go edge-on however far you orbit — by construction rather than by care — and gaps, ticks and text are pixel constants, so a 2 mm boss and a 2 m beam read identically at any zoom. Below `MINIMUM_SPAN_PX`, which is what looking straight down the measured axis gives you, it degrades to a leader still carrying the number, so the value survives every viewing angle. One `POST_PIXEL` pass where the force arrows need two, because a dimension is not a world object [rec: forest-wind-3489].
- It creates **no Blender objects**, so unlike `cadex_collision` and `cadex_cage` it needs no sibling collection and cannot be swept by the contract GC. The Measure button queues a request on the pin queue rather than writing the script, so **the script keeps exactly one author** [rec: forest-wind-3489].
- **`cadex_section.py` cuts the model open, and the cut face is filled** (ADR-148). A hidden, scaled unit-cube cutter plus a Boolean DIFFERENCE modifier on each hydrated solid, and an eight-node geometry-nodes clip on each edge wire — two mechanisms because the model is drawn as two kinds of object and a boolean has nothing to say about a mesh with no faces. Measured **6.3 ms** per offset change, against the 650 ms slider bar it shares a viewport with. It is a **view, not a feature**: nothing reaches the engine, nothing is written to the script, and the gate asserts the accepted revision is unchanged either side of switching it on. Because it is modifiers rather than an overlay it survives a rebuild for free — hydration swaps the mesh datablock and keeps the object — so `cadex_hydrate` needed no change at all. `render_views` suspends it, because a cut model is not what was built [rec: grand-peak-3688]. The original section workflow sentence exceeded the then-3500-character overlay budget; ADR-178 later raised the test cap to 3800 for the draft workflow [rec: neat-tower-5715] [rec: winter-bloom-8543].
- **`cadex_explode.py` spreads the assembly apart, and the engine authored every number** (ADR-149). A toggle plus a factor slider 0→1 interpolating the engine's exploded-view display record: staged windows (stage *i* of *N* owns factor [i/N, (i+1)/N]), lerp positions, hemisphere-corrected slerp orientations, leader lines as one wire object in a sibling `Exploded` collection growing with each component's own staged progress. Poses ride `matrix_world` — the channel hydrate already owns — and are re-applied by the hydrate hooks, so engine poses land first and the spread lands on top, always; delta channels were rejected as a second owner for one question. It **refuses while a simulation is baked**: F-Curves and `matrix_world` writes cannot share an object honestly, so mutual exclusion rather than layering. One exploded view per model; two is refused naming both. `render_views` suspends the spread, and the gate's first exploded assertion compares the factor-0.5 viewport pose against the pure half's own prediction — the re-application risk made a test [rec: zesty-cove-4881].
- **Blueprint styling** offers blue, cutting-mat green, grey, and **technical** black-on-paper-white. Styling composes with section/explode views and is restored after capture; technical sheets use theme-colored wires while white-lined themes use object color. Declared dimensions appear on sheet cells (default on for orthographic cells), with radius and angle geometry also shared by the viewport overlay [rec: windy-wolf-5012] [rec: happy-valley-9134].
- **Composed sheets** retain triptych defaults, template or freeform mosaic layouts, per-cell hiding/isolation/section/explosion, sheet and cell aspect, part-name callouts, parameters/text panels, titles and a stored round-trippable recipe. `based_on` loads that recipe for revision, and named saves version the drawing. The renderer snapshots presentation state once and restores it after rendering [rec: morning-walrus-8074] [rec: wild-walrus-5718] [rec: clever-hill-0361] [rec: humble-peak-6095] [rec: green-tree-7595]. **Draft then save**: `make_blueprint` renders session state and refreshes it on model rebuild; `save_blueprint`/Save is the durable store write, and Export writes a PNG independently [rec: winter-bloom-8543]. Each Blueprint Editor selects the draft or a stored sheet independently, with sheet menu, pager, Save and Export in its header and a matching theme ground. Draft cells tag into chat as `@cell-N`; stored sheets are view-only and selections are session-only [rec: wild-prairie-9912]. The store index is read from disk, avoiding inspect pagination that stubs real-size lists [rec: calm-flame-0305] [rec: wild-prairie-9912].
- **The assistant is a harness preference:** Claude Code (default), Codex or pi; saved sessions carry `session_provider` and resume only into the harness that minted them [rec: forest-chart-2781] [rec: merry-water-7647]. Model menus and account metadata come from installed harnesses; searchable selections persist per harness as stable IDs. The harness default sends no model override, discovered unavailable IDs are blocked, and native Terminal login refreshes account state without storing tokens in Cadex. This supersedes the static model menus and shell default recorded in ADR-182; real OAuth login was not performed in validation [rec: honest-harvest-7271] [rec: windy-sage-5295].
- **The model can leave for a slicer** (cadex ADR-156), **and the ticks are ours** (cadex ADR-158). The Parameters editor draws one row per candidate output — an **operator per row, not a `BoolProperty`**, because the roster is whatever the last accepted rebuild published and a property would mean registering a `PropertyGroup` at runtime the way the sliders do — and **File > Export Printable Parts...** calls `export_printable`, naming the ticked parts on the call. The Overwrite / Keep Both dialog is built from the **engine's own refusal** (`PRINT_FILES_EXIST`, file list attached), `Link Part`'s arrangement exactly — the shell never lists the store's directory itself [rec: still-wave-6655]. ADR-156 pushed each tick through a `set_printable` op into `script.json`; since ADR-158 a tick is **one scene ID property** (`cadex_printable`), so it saves with the .blend, costs no round trip, no store write and no revision, and never reaches the engine until an export names the job. `cadex_print.marked()` filters the stored ticks against the roster, so a stale tick cannot turn a whole job into one refusal, and `adopt()` prunes them when a rebuild changes the roster — ADR-039's silent drop-on-drift, now on this side of the boundary while the engine keeps the loud half. The roster still costs no round trip: it rides in the `inspect scope="script"` block `_adopt_script_state` already adopts. Neither operator sets `bl_options`: a tick is a view setting like a selection, and putting one on the undo stack would make Ctrl-Z step through checkboxes on its way back to the model. The diff rule held again — the whole change is inside `mesh_agent/` and `shell/tests/python/` [rec: gilded-wind-5121].
- **Inherited-tree changes are explicit decisions.** Application work lives under `shell/scripts/startup/mesh_agent/` and tests; BLENDER-TREE §2a remains eight files. Ledgered exceptions include window chrome (§2d), the Blueprint Editor via the §2b checklist, and the AI Preferences DNA value/RNA row. The startup move preserves both old/new application path prefixes in the licensing manifest [rec: open-key-6334] [rec: curious-cloud-7186] [rec: wild-prairie-9912] [rec: curious-sail-8332].
- Nothing in `shell/` imports mujoco, and a test asserts it. The shell never learns MuJoCo exists — a policy rollout reaches it as the simulation trace it already knew how to play [rec: sage-wood-0687] [rec: open-key-6334].
- The latest recorded application gate passed on 2026-09-05 with native recipe hydration, parameter reversal, rollback on failure and one undo push per agent turn. Picking was 372/372; ordinary CAD slider median was 0.542 s against 0.65 s. That timing does **not** measure native recipe rebuild latency [rec: simple-bramble-8616]. Harness discovery and UI suites also passed; native OAuth itself remains untested [rec: windy-sage-5295].

- **The chat column is the chat's** (ADR-164, ADR-165). The message box is anchored to the window floor — the execute region carries `RGN_FLAG_DYNAMIC_SIZE | RGN_FLAG_NO_USER_RESIZE` (the project editor's recipe) and `cadex_chat_init` enforces it on areas loaded from old files — and its button row holds only chat actions, gather + turn. Rebuild Model and every viewport toggle moved to the Parameters panel's Interface section. The standing rule that came with it: **no button whose job is to show or hide an editor**, ever — ~230 lines of open-a-view machinery (`toggle_params`, `show_script`, `toggle_wiring`) deleted, because the editor dropdown and the tiling manager already are the door [rec: tender-crane-5909] [rec: civic-glade-9153].
- **The window chrome is the OS's, and the product has a version** (ADR-166). File and Edit are native NSMenus built in `GHOST_SystemCocoa.mm`; each item pushes its tag as a `GHOST_kEventNativeMenu` (`GHOST_EventString`, the `OpenMainFile` shape) and the tag→operator map lives in `wm_window.cc`, so GHOST never learns an operator name; the items carry no key equivalents because Blender's keymap owns shortcuts, and a Cocoa equivalent would intercept the key first. Both global bars are freed rather than created, stripping them off saved files too. Open Recent and the stock Import/Export submenus did not survive (dynamic Blender menus a static NSMenu cannot host) — revisit if missed. The version: repo-root `VERSION` (0.0.5) bumped deliberately by `bump_version.sh`, stamped by `build_app.sh` into `cadex_version.txt` + `CFBundleShortVersionString` with a commit-count `CFBundleVersion`, and read back into the window title — "Untitled - Cadex 0.0.5" — with no CI change needed [rec: curious-cloud-7186].
- **A fresh launch opens onto a landing screen** (ADR-167, ADR-168 — not a reversal of ADR-042: Blender's modal splash stays disabled and `wm_splash_screen.cc` is untouched). `cadex_landing.py` draws a start page in the viewport on `cadex_dimension`'s mechanics (POST_PIXEL handler, pure `landing_layout`/`hit_test`), takes input through three add-on keymap items (click owns the region while up, hover, Esc) rather than a modal grab, and dresses itself entirely from the app: theme greys read live off `wcol_regular` and the viewport ground, rounded-by-geometry corners, `blf` font 0, and the Dock's own mark extracted from `cadex_icon.icns`. Body: the EXAMPLE PROJECT card plus New File / Open… / Tutorial (a stub that says so); footer "Esc to skip"; deliberately no Start Chatting button — the live chat beside it is its own explanation, and the first message dismisses via the one choke point in `Agent.start_turn`. The demo arc: the wcv12 drone shipped with ADR-167 [rec: lucid-otter-3511], was removed by the ADR-171 audit (seven commercial-part STLs, no recorded origin) [rec: wild-sea-9905], and since ADR-173 the **MG90S biped ships** (`mesh_agent/demo/`: `biped.blend` + the store pruned to its accepted revision + `card.png`, ~3 MB, LFS) — entirely script-authored so the provenance bar is met by construction, its one asset the Cadex-trained balance policy the script replays; the open probe restores the shipped store to its accepted digest with 48 objects [rec: dawn-oak-0677]. The card is a 1152x720 offscreen render in the product viewport look — matcap + cavity + edge wires, three-quarter camera fitted to the robot, simulation floor hidden, workspace chrome suppressed — operator direction, twice, replacing a first blueprint-sheet card [rec: crimson-vine-9992] [rec: shady-lodge-6077]. Sanitizing the payload meant opening the `.blend`, not just sweeping the store's text files: the chat transcript saves into it as a text block and hydrate leaves absolute `cadex_sidecar` paths on every object — scrubbed, and `test_landing_demo_payload_ships` now subprocess-opens the shipped file to pin zero text blocks and zero machine paths [rec: sleepy-shade-1485]. `open_demo()` still always copies to a fresh-numbered stem under `~/Documents/Cadex Demo/`, so the bundle is never opened in place and a saved demo session is never clobbered. The stale VibeCAD-era `docs/images/cadex-mark.svg` that ADR-059 flagged and parked is deleted [rec: lucid-otter-3511] [rec: wandering-mist-0460] [rec: hollow-spring-0679].
- **The startup layout since ADR-168**: viewport top-left, **Parameters | Outliner** sharing the bottom row, chat as the right **third** of the window — still `Mesh/startup.blend` and no code, re-saved and committed, with the gate pinning both the four areas and the proportions [rec: hollow-spring-0679].
- **The viewport at rest since ADR-172**: cavity shading on (SCREEN type over the unchanged solid/matcap), transform gizmos off, and overlays on but bare — no floor, no ortho grid, only the Z axis. Same mechanism as every startup-look change since ADR-037: the look *is* `Mesh/startup.blend`, re-saved through the bundle; the gate pins it and `cadex_blueprint.PRODUCT_LOOK` follows it, so the blueprint view's overlays-ON dependency is now also the product default [rec: deep-branch-6721].

**A shipped defect, found and fixed while adding linked parts.** `cadex_backend._assets_in` filtered Save-As's carry-forward on the three-suffix `ASSET_SUFFIXES` the shell mirrors by name, so a Save-As would have **dropped every `.cxpart`** and left the new file with a script that could not run — ADR-046's bug arriving on a new file type. A separate `CARRIED_ASSET_SUFFIXES` fixes it and the gate pins it. **The identical gap is still open for `.cxpolicy`**: it was named and deliberately not fixed, because carrying trained weights on every Save-As is its own decision [rec: ancient-current-9419].
- **The agent's tools are wired by two settings that only work together, and getting it wrong is silent** (ADR-163). `backend.py` passes `--tools ""` so Claude Code's own file and shell tools are unreachable — every mutation must arrive through the Mesh tools, on Blender's main thread — **and** sets `ENABLE_TOOL_SEARCH=false`, because Claude Code defers MCP tool *schemas* behind its built-in `ToolSearch`, which is one of the built-ins `--tools ""` removes. For a period ending 2026-08-24 only the first was set, so **every chat turn in the shipped app ran with no tools at all** [rec: weathered-sand-9705].


- **Application settings** live in `prefs.py`: `cadex_agent.json`, mirrored into a WindowManager PropertyGroup, with an AI Preferences section opened by the chat gear. Startup registration is idempotent; no add-on enablement or `bl_info` remains [rec: curious-sail-8332].
- Chat messages have clipboard buttons, consecutive tool calls collapse before transcript trimming, and normal turns have no per-turn tool-call cap. `tool_cap_override` remains for tests/evaluation [rec: honest-harvest-7271] [rec: damp-fountain-8719].
- Component instances and their wire children hydrate into an **Assembly child collection of Model**, migrating old root links and removing the collection with its last component [rec: cool-jasper-0086].
- The assistant sees `lib` in `api_overview` and can request its catalog through `describe_cad_api domain=lib` [rec: twilight-lake-8164].
- For `mesh.blender`, the shell supplies an absolute runtime executable path to cadexd; execution stays in the sandboxed child and the visible scene only hydrates accepted output [rec: simple-bramble-8616].

## Negative knowledge

- [scope: shell/ edits outside mesh_agent | confidence: high | evidence: open-key-6334] Every line added to the inherited Blender tree is a future merge conflict. Adding one is a decision to bring back, not a fix to slip in — the empty-diff rule was retired but the boundary it stood proxy for was not, and BLENDER-TREE.md section 2a must stay eight files.
- [scope: gate coverage | confidence: high | evidence: solemn-chart-6274, grand-peak-3688] The gate runs blender --background, so anything needing a real VIEW_3D is not covered by it. Verify those by driving the built application and say so, rather than implying coverage the gate does not have. Read forward as well as backward: where two mechanisms would both do the job, the one the gate can *see* is worth real cost. The section view is a boolean partly because `rv3d.clip_planes` needs a `RegionView3D`, which `--background` does not have at all, and would also have been absent from every offscreen render the agent takes.
- [scope: reading the gate's result | confidence: high | evidence: forest-wind-3489] **`pixi run gate` exits 0 when a test raises mid-run.** An `AttributeError` in one test ended the suite after 355 checks with no `CADEX-BLENDER-GATE` line at all, and the exit code was still 0. Checking the exit code is not enough — the `"ok": true` line has to be read. Pre-existing and not fixed.
- [scope: any new asset suffix | confidence: high | evidence: ancient-current-9419] Widening the engine's `_STORED_ASSET_SUFFIXES` is free, but the shell carries its **own** suffix list for Save-As and it does not follow. A new asset type that is not added there is silently dropped by Save-As, and the failure surfaces later as a script that cannot run in the copy.
- [scope: asserting anything about an output from the viewport | confidence: high | evidence: ancient-current-9419, forest-wind-3489] The viewport polygon count tells you nothing about what an output *is* — the mesh in the viewport is a tessellation whatever it came from, so a 40-face linked BREP solid shows 520 polygons and a measurement shows whatever its subject shows. A gate assertion on `obj.data.polygons` looks like it proves this and does not; **this assertion has now been written and thrown away twice in this tree.** For a drawn overlay the check that works is calling the drawing function with a made-up region and view matrix — which is why `drawing_for` is a function rather than something living inside a draw handler.
- [scope: an overlay that must read from any angle | confidence: high | evidence: forest-wind-3489] A plane fixed in the model is the wrong home for a dimension: any such plane goes edge-on if you orbit far enough. Compute in screen space instead, and the failure mode is removed by construction rather than defended against. Likewise a circle's in-plane basis must not come off a fixed reference axis — a bore drilled down Z is the single most common thing anyone measures, so the naive choice fails on the first real model rather than an exotic one; take the basis off the world axis least parallel to the circle's own.
- [scope: any per-tool exemption from a result cap | confidence: high | evidence: shy-glade-0050] `MAX_RESULT_CHARS = 4096` governs every mesh tool **except** `get_script`, and that unannounced exemption is what an agent got wrong — it concluded "the engine only shows me a 4 KB window", which was false, and never checked. The general lesson is worse than the specific one: **a tool that cannot serve part of a thing invites the model to make the thing smaller**, and the model has write access. 122 lines and 4.8 KB of a user's design rationale were deleted to advance a read.
- [scope: adding a table to docs/INTEGRATION.md | confidence: high | evidence: forest-wind-3489] Its two contract tables are scraped by regex on any line starting with `` | ` `` — across the whole document for ops, and within the response section for keys. A new markdown table anywhere in the file is read as protocol rows. Document nested records as a bullet list.
- [scope: adding a line to the Cadex system-prompt overlay | confidence: high | evidence: neat-tower-5715, winter-bloom-8543] The overlay budget is test-held in `bl_mesh_agent.py`, a suite `pixi run gate` does not run. ADR-178 raised the former 3500-character cap to 3800 for the draft workflow; passing the application gate alone does not verify this budget.
- [scope: measuring the model from the shell | confidence: high | evidence: grand-peak-3688] **`obj.bound_box` reflects EVALUATED geometry, not the object's data.** A 20 mm cube with a boolean that cuts it at z = 5 reports a top of 5 — measured, not assumed. Anything that both modifies the display objects and derives numbers from their extent feeds back on itself: the centre of an axis drifts into the surviving half, and a range a person is dragging against moves as they drag. Read the mesh datablocks (numpy `foreach_get`) when the number must mean *the model as built*, and suspend any such modifier before measuring for a camera fit.
- [scope: where model state may live | confidence: high | evidence: forest-wind-3489] Scene data in the `.blend` is much cheaper than a script declaration and is the wrong answer for anything that is part of the model: it would be the only model state not in the script — lost by a Save-As, unauthorable by the agent, and unable to travel in a `.cxpart`. In the script it is anchored by selector and recomputed, which is what makes it follow a parameter.
- [scope: a view module's clear() when the view was never applied | confidence: high | evidence: windy-wolf-5012] A restore path with a fallback must be gated on "was anything ever applied", not run unconditionally: blueprint's first `clear()` applied the fallback product look on a settings write with the view off, stomping the user's viewport — which the subsequent toggle-on then captured as "the look to restore". A property update callback fires on any assignment, including one made while switching the view ON, so the off-path runs at surprising times. The gate's exact-restore assertion is what caught it.
- [scope: the Edges wires and viewport overlays | confidence: high | evidence: windy-wolf-5012] The true-BREP `… Edges` wire objects draw in the **overlay** wireframe pass (`overlay_wireframe.hh` skips wire objects when overlays are off). Any styling that needs them visible must turn `show_overlays` ON — and must then hold every sub-overlay explicitly, because whatever is not pinned draws whatever the user last had there.
- [scope: a per-cell shape ask on a composed sheet | confidence: high | evidence: green-tree-7595] The cells of a sheet compete for **one fixed field**, so a per-cell aspect cannot be solved, only approached: place, measure what each cell came out at, scale its track, place again. A cell that owns a whole track gets what it asks (a triptych column asked 1:3 and was drawn 1:2.97); a cell **sharing** a track cannot (the same ask in a three-cell column reached 1.31:1, because the width is not that cell's to spend). Spending the full ask on the stacked cell's own axis was measured — it reached 0.98 and squashed two innocent siblings to 96 px strips — and rejected. Report the shape drawn, in the text the agent reads back, rather than implying the ask was met.
- [scope: a tile drawn under a label the sheet dressing draws | confidence: high | evidence: green-tree-7595] A cell's own label is drawn **over** its tile by `_dress_sheet`, so any tile that starts its content at the top edge collides with it. The parameters panel had done exactly that since ADR-153 and no test could see it: in a short cell the first slider row was drawn through the word "parameters". A windowed probe of the built bundle is what found it, and `top_pad` — the parameter the text panel needed anyway — is what fixes it.
- [scope: text drawn into a rendered cell | confidence: high | evidence: humble-peak-6095] Dressing that needs room beside the model has a size below which it is worse than nothing. Part-name callouts read well in a 341 px column and landed **on** the part in the 85 px columns of a 256 px sheet — the same code, the same arithmetic, a cell that simply has no label band. Measure it on the rendered sheet and give the feature a floor that drops it honestly (counted, and said in the tool's reply), rather than trusting text to shrink with its cell.
- [scope: a panel row added to CADEX_PARAMS_PT_parameters | confidence: high | evidence: still-wave-6655] **No suite in this repo draws a panel**, so a draw bug is invisible to `pixi run gate` and to `bl_mesh_agent.py` alike — both can prove the operator registers and the backend works while the row appears nowhere. Two such bugs shipped into a windowed probe of the built bundle and neither was catchable otherwise: `CADEX_PARAMS_PT_parameters.draw` **returns early** when a model declares no parameters, so anything added below that branch is absent from exactly the simplest model (`result = {"bracket": part.box(...)}` — which is the model somebody wants to print); and a full-width `emboss=False` operator centres its label, putting the text at the far side of the panel from its own icon and reading as two unrelated widgets (`row.alignment = 'LEFT'`). Probe the bundle and look at it.

- [scope: reading a latency bar off a loaded machine | confidence: high | evidence: gilded-wind-5121] The gate's `slider_latency` check is the one number in `CADEX-BLENDER-GATE` that a busy machine can fail on its own. A run at load average 13.7 measured 1.124 s against the 0.65 s bar — and **every** other number in that same run was 2–3× its usual value (open 3.0 s vs 2.0 s, refine 3.1 s vs 1.4 s). The re-run on a quiet machine came back at 0.538 s. Read the whole line before believing a latency failure, and re-measure rather than chasing a regression that is not there.

- [scope: bit-equal restore assertions over the viewport | confidence: high | evidence: morning-walrus-8074] Two kinds of viewport state cannot be asserted bit-equal naively. A parented wire child's `matrix_world` is an EVALUATED value — a snapshot taken between a matrix write and the next depsgraph pass captures the stale pose and blames the restore for it; settle (`view_layer.update()` + `evaluated_depsgraph_get()`) before capturing. And a **derived artifact** like the section cutter's matrix is computed from model bounds at refresh time, so live state can legitimately hold a stale value no restore reproduces — a restore that recomputes is more correct than the staleness, and the fixture must make the live value reproducible rather than the restore matching it.
- [scope: an agent that cannot reach its tools | confidence: high | evidence: weathered-sand-9705] **It does not say so.** It writes the call out as prose — `<invoke name="mcp__mesh__get_script">` — invents a plausible result, and answers as though the work happened. There is no error, no non-zero exit and no empty reply, so the turn reads *better* than a real one. Caught only from a user transcript in which the model quoted back a complete "current script" that did not exist, in an API this product does not have, on 1670 tokens of context. `agent.py` now watches the streamed text for `<invoke name=` — which a real call can never produce, since it arrives as a `tool_use` block — and says so once per turn.
- [scope: two safety settings that each look right | confidence: high | evidence: weathered-sand-9705] `--tools ""` is correct and MCP schema deferral is a reasonable default; **together** they disable the only key to the tools the product depends on. Neither is wrong alone, so no review of either would have caught it. `test_the_mesh_tools_are_not_deferred_behind_a_disabled_tool` pins the *join*, not the halves.
- [scope: the agent test suite | confidence: high | evidence: weathered-sand-9705] The only test that would have caught this is the one that never runs: `MESH_AGENT_LIVE=1`. Every other agent test drives the mock backend, which by construction cannot reproduce a CLI flag interaction. An opt-in live test is a test you do not have.
- [scope: proving an application-module fix reaches the user | confidence: high | evidence: weathered-sand-9705, curious-sail-8332] Shell harnesses swap the bundled mesh_agent copy for source, so a green source gate does not establish that installed modules match. Rebuild/install and compare the installed code; the startup move also requires pruning the obsolete addons_core copy because CMake install does not delete it.
- [scope: code that runs during add-on register() | confidence: high | evidence: lucid-otter-3511] `bpy.data` is `_RestrictData` while an add-on registers — reading `filepath` raises, and an exception there aborts the REST of `mesh_agent.register()`, silently costing the session its save/load handlers while everything registered earlier keeps working. Defer the decision through `bpy.app.timers` (the template's own idiom), which also never fires under `--background`.
- [scope: screen-layout ops driven from a script | confidence: high | evidence: hollow-spring-0679] `SCREEN_OT_area_move`/`area_split` poll on `screen->active_region == NULL` — the mouse must sit on an area EDGE, and no context override can fake that. `window.cursor_warp()` onto the edge being moved, one event-loop beat, then the ops run under a plain window+screen override. The poll error is context-opaque; the fix is the mouse position.
- [scope: rasterizing an SVG on a stock Mac | confidence: high | evidence: wandering-mist-0460] `qlmanage` renders SVGs but bakes an opaque white page behind the art — the PNG reports hasAlpha while every pixel is alpha=255, and the defect only shows when drawn over a dark ground. A border-connected flood key recovers it; better, take the art out of an existing `.icns` with `iconutil`, which keeps true alpha.

- [scope: sanitizing a .blend for shipping | confidence: high | evidence: sleepy-shade-1485] A `.blend` passes any text-file sweep while carrying the whole chat transcript — `history.py` mirrors it into a `bpy.data.texts` block that saves with the file — plus an absolute `cadex_sidecar` cache path on every hydrated object. Sanitization must open the file and hold it to zero text blocks and zero machine-path properties; a sweep of the store's text files proves nothing about a datablock container.
- [scope: hiding objects before an offscreen draw | confidence: high | evidence: shady-lodge-6077] `hide_set` alone does not reach an immediately following offscreen `draw_view3d` — the hidden object still renders. Run `bpy.context.view_layer.update()` between the hide and the draw. Related: the simulation floor's objects are named `c_floor`, not `floor`, so exclude scenery by measured extent rather than by name.


- [scope: mesh_agent startup registration | confidence: high | evidence: curious-sail-8332] `keyconfigs.addon` does not yet exist when startup modules register; landing/drawing keymaps must be deferred and retried until ready.
- [scope: stored drawing lists | confidence: high | evidence: calm-flame-0305, wild-prairie-9912] Inspect pagination replaces large values with stubs. The editor reads the schema-checked on-disk index; an assistant scope read must resolve stubs through `_inspect_full`.
- [scope: Codex noninteractive resume | confidence: high | evidence: forest-chart-2781] `exec resume` rejects flags accepted by `exec`; sandbox configuration travels as a config override on resume. Noninteractive MCP calls also require the verified approval setting or tools silently decline.

## Provenance

- simple-hollow-8675 — the shell became the product when the Qt shell was deleted; it is a protocol client and nothing more
- merry-eagle-4093 — the shell moved into this repository and the engine ships inside its bundle
- open-dew-7293 — the menus, the editor types and the saved-layout startup file
- crisp-glacier-6395 — the Wiring editor
- mellow-hawk-8610 — the Live editor and the force-arrow overlay
- solemn-chart-6274 — render_views and the section-cage overlay
- open-key-6334 — the diff rule that replaced the empty-diff rule
- sage-wood-0687 — nothing in shell/ imports mujoco, and a test asserts it
- ancient-current-9419 — the two linked-part menu rows, the 17th tool, and the Save-As carry-forward defect they exposed
- forest-wind-3489 — `cadex_dimension.py`: the screen-space overlay, the Measure button that does not author the script, the gate's exit-code trap, and the 675-check run
- shy-glade-0050 — `get_script` serves windows, so a read limit is never answered by editing the script; and the 696-check run
- grand-peak-3688 — `cadex_section.py`: the section view is a capped boolean rather than a clip plane, it is a view and not a feature, and `obj.bound_box` reads the cut it made
- neat-tower-5715 — the section view's prompt line did not fit the overlay's 3500-character cap, and the cap did not move
- zesty-cove-4881 — `cadex_explode.py`: the factor slider over the engine's exploded-view record, matrix_world over delta channels, and the baked-simulation refusal
- windy-wolf-5012 — ADR-150: the view registry, the blueprint view, `make_blueprint`/`render_blueprint`, the clear()-fallback stomp the gate caught, and the overlays-ON dependency of the Edges wires
- morning-walrus-8074 — ADR-151: the composed, dressed sheet (`cadex_sheet.py`), hero-right, `hide_set` over `hide_viewport`, the flat snapshot/restore, and the two bit-equality traps its gate test surfaced
- wild-walrus-5718 — the ADR-151 addendum: the triptych default, the graceful explode degrade, and the uniform ground sampled off the colour-managed tiles
- clever-hill-0361 — ADR-152: only (the isolate as a complement hide), the mosaic layout (freeform cells held to the tiling invariant by refusal), and the curation rewrite of the tool description
- humble-peak-6095 — ADR-153: 16:9 by default, part-name callouts with their measured width floor, and the parameters panel as a sheet cell
- careful-key-9041 — ADR-154: Opus 5 as the default model in both front ends, and the one constant the picker now reads
- still-wave-6655 — ADR-156: printable parts in the shell — `cadex_print.py`, the per-row checkbox operator, the File menu row, the conflict dialog built from the engine's refusal, and the two panel-draw bugs only a windowed probe could find
- green-tree-7595 — ADR-157: named, revisable sheets (`based_on` + the stored recipe), the per-cell aspect honoured by measure-and-replace, the text panel, and the params-panel label collision a windowed probe found
- gilded-wind-5121 — ADR-158: the print tick becomes a scene property — no op, no store write, no revision — and drop-on-drift moves to the shell
- weathered-sand-9705 — the tool-deferral bug, its silent failure mode, the fix and the test that pins the join
- tender-crane-5909 — ADR-164: the chat input anchored to the window floor, the chat-only button row, and the Interface section
- civic-glade-9153 — ADR-165: one Interface grid, and the no-open-a-view rule that deleted three operators
- curious-cloud-7186 — ADR-166: native File/Edit via GHOST_kEventNativeMenu, both window bars gone, VERSION 0.0.5 with the commit-count build stamp, and BLENDER-TREE §2d
- lucid-otter-3511 — ADR-167: the landing screen in the viewport, the sanitized wcv12 demo in the bundle, and the restricted-context register trap
- wandering-mist-0460 — the ADR-167 addendum: theme greys, rounded geometry, and the qlmanage baked-white alpha trap
- hollow-spring-0679 — ADR-168: Parameters | Outliner, chat at a third, the Dock's mark on the landing page, and the cursor-on-the-edge poll of the screen ops
- deep-branch-6721 — ADR-172: the default viewport look — cavity on, gizmos off, overlays on but bare
- dawn-oak-0677 — ADR-173: the biped example project returns to the landing screen, provenance-clean
- sleepy-shade-1485 — the demo .blend carried the chat transcript; scrubbed, and the payload test now opens the shipped file
- crimson-vine-9992 — the demo card becomes a viewport render of the model, not its blueprint
- shady-lodge-6077 — the demo refreshed to the current biped and the card drops the floor
- forest-chart-2781 — Codex backend, event contract and provider-tagged sessions
- merry-water-7647 — pi backend and second bridge transport
- happy-valley-9134 — technical theme and sheet/viewport dimensions
- cool-jasper-0086 — Assembly collection and blueprint inspection scope
- calm-flame-0305 — disk index avoids inspect pager stubs
- winter-bloom-8543 — draft/save split, section pins and overlay budget
- wild-prairie-9912 — dedicated Blueprint Editor with independent selections
- damp-fountain-8719 — product tool-call cap removed, test override retained
- twilight-lake-8164 — library catalog surfaced to the assistant
- honest-harvest-7271 — copy buttons, collapsed calls and settings gear; static default later superseded
- curious-sail-8332 — startup application code, JSON settings and AI Preferences
- windy-sage-5295 — harness-owned account/model discovery and login; validation bounds
- simple-bramble-8616 — runtime discovery, accepted hydration and native recipe gate
