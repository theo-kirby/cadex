This project already holds an accepted design of yours. Rebuild it with the engine as it is today and accept it again, unchanged. Do not edit the script, do not change a parameter, and do not alter anything about what the design is: submit the script exactly as this project already holds it through normal acceptance, so that the accepted state and every artifact beside it are produced again from unchanged source. Then read the measured evidence your tools return for the newly accepted revision -- the fit report at the rest pose and through every joint's declared range, the inventory of what the design is made of and where each part came from, and the result of the project's own bounded smoke -- and report what each one says, as measured. If any of them names a failing check, report it exactly as it reads and still change nothing. Finish with a DECISION: line recording that the design was rebuilt and re-accepted from unchanged source, and NOTE design_specs: lines carrying the measured outcome of each kind of evidence you read. Do not train. Do not ask questions.
