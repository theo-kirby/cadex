# cadex — State

> Generated by `tools/hypergraph.py render` from the state-graph export.
> Do not hand-edit — run the hypergraph-reconcile skill instead.

Reconciled through `tidy-banner-0293`, `shy-glade-0050`, `empty-light-4558`, `neat-tower-5715`, `careful-key-9041`, `weathered-falcon-4350`, `deep-branch-6721`, `sleepy-shade-1485`, `shady-lodge-6077`, `wild-prairie-9912`, `quiet-creek-7756`, `idle-arrow-2946`, `candid-creek-2410`, `pale-river-6583`, `ancient-crest-4588`, `proud-cliff-9629`, `curious-badger-0797`, `amber-glade-2813`, `lively-grove-8186`, `hidden-reef-8369`, `simple-quartz-9812`, `royal-water-8317`, `weathered-sage-2750`, `falling-ocean-4411`, `patient-pond-3886`, `copper-union-7670`, `soft-bay-5837`, `neat-reef-5625`, `lawful-grotto-1291`, `restless-slope-6471`, `calm-signal-9170`, `clear-clock-9191`, `silent-rose-4316`, `humble-fox-6370`, `rough-ridge-4729` at 2026-09-21T02:25:24+00:00.

## Frontier

- [open] **F10. A closing report exists and the critic accepted done** (`first-snow-5587`) — **F10's report now exists in the shape the criterion asks for, and it claims done (ADR-397, commit `f7205560`) [rec: old-dew-1568].** `do…
- [open] **F4. The agent repairs from measurements alone** (`polished-forest-0215`) — **F4 is exhausted: the frozen repair prompt and all three continuations reached the model on `ot7-heron-repair-d`, and the final measured…
- [open] **F5. The arm is designed unassisted** (`stormy-aspen-5433`) — **F5 is exhausted: the create prompt and all three continuations reached the model on `ot7-heron-c`, every turn ended on its own, and the…
- [open] **F6. The balancer is designed unassisted** (`narrow-dune-9454`) — **F6 is exhausted: the create prompt and all three continuations reached the model on `ot7-robin-c`, every turn ended on its own, four sl…
- [open] **F7. The biped is designed unassisted** (`rapid-grove-9687`) — **F7's sixth requirement has a measured verdict at last, and it is a pass — taken with no slot, on a model rebuilt outside the accepted p…
- [open] **Inherited-tree reduction, and the unscheduled replacements** (`round-glacier-2865`) — The standing work of shrinking both inherited trees in place, under the two-commit removal protocol: dependency audit → a **disable commi…
- [open] **Parts library — hardware catalog and remaining families** (`brave-stone-9609`) — Catalogued hardware is available as the **lib script namespace** over `CadexCatalog`, composed as parametric BREP values with source-qual…
- [open] **The RL training loop and mg-legs** (`late-pond-2851`) — **The training loop itself is proven, locally, end to end** — what remains blocked is gait scale. The whole arc (agent-authored mechanism…

## Architecture

- **cadex — state** (`nimble-pine-0740`)
  - [working] **The engine — cadexd and the xscript pipeline** (`forest-wind-0342`) — **Restore no longer collects the accepted attempt it is comparing against (ADR-398, commit `775974f9`) [rec: fierce-bloom-1076].** Found …
    - [working] **The wiring and electronic-assembly surface** (`clear-banner-8342`) — Procedural wire routing and the electronic-assembly surface, marked **experimental** in `docs/ARCHITECTURE.md` and carrying stated limits…
    - [open] **Parts library — hardware catalog and remaining families** (`brave-stone-9609`) — Catalogued hardware is available as the **lib script namespace** over `CadexCatalog`, composed as parametric BREP values with source-qual…
  - [working] **The shell — the Blender fork and mesh_agent** (`shy-crane-2573`) — The shell is a Blender fork under `shell/` carrying `mesh_agent` as application code under `shell/scripts/startup/` [rec: curious-sail-83…
    - [working] **File lifecycle — open, save, Save-As, and .blend vs .cadex** (`simple-willow-8989`) — The file-handling path inherited from Blender — opening, saving, Save-As, creating a new file, how the `.cadex` project directory relates…
  - [working] **The headless CLI** (`chilly-union-8972`) — **The collector can now measure a paused attempt without spending a design slot, and the measurement it takes has its own probe (ADR-392 …
  - [working] **Dynamics and control on MuJoCo** (`salty-isle-4063`) — **A weld written unconnected-first exported its body at the *inverse* of its pose, for as long as the exporter has existed — found, fixed…
    - [open] **The RL training loop and mg-legs** (`late-pond-2851`) — **The training loop itself is proven, locally, end to end** — what remains blocked is gait scale. The whole arc (agent-authored mechanism…
    - [working] **The robot lifecycle walk — design to policy to iterate, agent-driven and headless** (`calm-peak-5247`) — **Recent project decisions now reach the revisiting agent (ADR-265).** The project-as-codebase contract had a hole under its own budget: …
  - [working] **Organic modelling and the CAD/mesh interface** (`idle-lantern-9094`) — Making the shapes a person actually asks for — a body, a limb, a skin over a mechanism — buildable by the agent and shapeable by the user…
  - [working] **Orientation, build and the working environment** (`early-arbor-7123`) — **Read this before touching anything.** The author's answer to "what would waste a fresh agent's whole day" was immediate and was not abo…
  - [open] **Inherited-tree reduction, and the unscheduled replacements** (`round-glacier-2865`) — The standing work of shrinking both inherited trees in place, under the two-commit removal protocol: dependency audit → a **disable commi…
  - [working] **Structural analysis, topology optimisation and shape search** (`kind-marsh-2645`) — Stress-testing a part, finding where material can be removed safely, and
  - [working] **Compliance and licensing** (`easy-wind-9848`) — The repository's compliance and licensing posture after the ADR-171 audit (PR #13, version 0.0.7) [rec: wild-sea-9905]:
  - [working] **The walk exists and is tested headlessly** (`crisp-reef-5607`) — Charter criterion: **The walk exists and is tested headlessly.** One documented entry point (a CLI prompt or a headless script) takes a m…
  - [working] **Three modes, one shape** (`witty-spark-2613`) — Charter criterion: **Three modes, one shape.** The walk runs headless (exercised), with the GUI attached (documented, not exercised while…
  - [superseded] **A fresh machine runs the walk** (`ancient-sun-3952`) — Open charter criterion: **A fresh machine runs the walk** after one documented install script, headlessly, with no step that needs a pers…
  - [superseded] **The catalog is broad enough for a robot prompt** (`ancient-tide-5930`) — Open charter criterion: **The catalog is broad enough for a robot prompt**: at least five servos, ten actuators, a bearings family, and M…
  - [working] **Phase 8 `src/Gui` delete commit landed** (`civic-sand-2641`) — **The Phase 8 `src/Gui` directory-delete criterion is met (ADR-214).** After separate metatype and disable prerequisites, thirteen GUI di…
  - [working] **The agent can see its work without a screen** (`damp-moon-9297`) — Charter criterion: **The agent can see its work without a screen.** One CLI call each, with outputs landing in the project directory: ren…
  - [superseded] **A variant study exists** (`easy-bloom-3674`) — Open charter criterion: **A variant study exists**: several variants of one joint generated by one script, tested the same way, ranked in…
  - [superseded] **The robot prompt works unattended** (`fond-clover-3062`) — Open charter criterion: **The robot prompt works unattended**: from a prompt naming the servos, bearings, and hardware, the loop produces…
  - [superseded] **The fork's delta against upstream is smaller than at the start of this run** (`green-sea-3991`) — Open charter criterion: **The fork's delta against upstream is smaller than at the start of this run**, measured by the delta manifest AG…
  - [superseded] **Compound mechanisms exist as parametric library values** (`idle-tower-1624`) — Open charter criterion: **Compound mechanisms exist as parametric library values** built from catalog parts: a rack and pinion and a plan…
  - [superseded] **A research report renders headlessly** (`late-sky-8010`) — Open charter criterion: **A research report renders headlessly** from a project's `PROGRESS.md` numbers into a document with graphs a per…
  - [working] **L2 boards family with real-kernel and packaged verification** (`ready-falcon-6286`) — L2 boards exist over `CadexCatalog` through `lib.board`: ESP32-DevKitC V4 (WROOM-32E), Pi Zero 2 W and Adafruit 815 PCA9685 revision C. S…
  - [superseded] **25T horns and servo pigtails come from manufacturer STEP sources** (`red-loom-6298`) — Open charter criterion: **25T horns and servo pigtails come from manufacturer STEP sources**, with the provenance recorded the way `docs/…
  - [superseded] **L3 motors and mechanisms families exist** (`rising-banner-4325`) — Open charter criterion: **L3 motors and mechanisms families exist** over `CadexCatalog`, same test shape as the boards family, and the pa…
  - [superseded] **One mechanism in the repo came from a paper or a real product** (`spring-shore-5413`) — Open charter criterion: **One mechanism in the repo came from a paper or a real product**: the source is cited in the project's `DECISION…
  - [working] **The walk holds on a second mechanism** (`swift-dusk-2951`) — Charter criterion: **The walk holds on a second mechanism.** The same entry point, with no code change specific to the mechanism, takes a…
  - [working] **The `hide_render` shell bug from `docs/IDEAS.md` is fixed** (`wild-comet-8096`) — **The `hide_render` shell defect is fixed with a regression that fails on the old source**, meeting the declared charter criterion [rec: …
  - [working] **Two Phase 13b engine-side removals landed** (`windy-pebble-4630`) — **Two Phase 13b engine-side whole-tree removals are complete:** Help and Start each have separate disable/delete commits, ADRs and record…
  - [superseded] **mg-legs tips at the declared shove band, backward first** (`salty-fjord-6965`) — Open charter criterion: **mg-legs tips at the declared shove band, backward first**, with the numbers in the project's `PROGRESS.md`. [re…
  - [working] **Live headless project review — the ot5 charter (ADR-284)** (`crisp-sun-1239`) — **Run ot5 is closed: the owner ticked D1–D11 on 2026-09-13, and no criterion under this root is open [rec: patient-pond-3886].** The run …
    - [working] **D4. Policy videos render, persist and play headlessly** (`candid-harvest-2614`) — **D4 is repeated on the third fresh project, Lark, across three bounded training runs plus a retry, and now includes Lark's own real enco…
    - [working] **D3. Training is visible while it runs** (`dawn-delta-4361`) — **D3 is repeated on the third fresh project, Lark, in both of its real GPU runs.** During `lark1` a fresh visit to the persistent URL sel…
    - [working] **D1. A live project dashboard is reachable** (`jolly-loom-0622`) — **The dashboard now follows the current run and its dispatched project (ADR-387) [rec: silent-rose-4316].** `tools/operator_review.py` re…
    - [working] **D5. Review history survives a design change** (`sharp-union-6036`) — **Every run on the persistent Lark copy has its policy in the project store with its recorded digest (ADR-327).** `lark96-restart` and `l…
    - [working] **D2. The browser shows the right model and specs** (`shy-meadow-0959`) — **A project straight out of the product agent's turn now has a model to show, and the reader accepts a first accepted attempt.** Lark's c…
    - [working] **D6. Save, reopen and restart preserve the project** (`clever-field-7845`) — **Engine restart during real Lark training passes, closing D6's last undemonstrated fact (ADR-325).** On the persistent working copy `ot5…
    - [working] **D7. Save-As/copy produces an independent project** (`cold-vale-4232`) — **D7 is repeated on the third fresh project, Lark, including the retraining half.** `ot5-lark` was copied whole to `ot5-lark-copy85` afte…
    - [working] **D8. Interrupted and failed runs remain understandable** (`cool-gate-3332`) — **A failed run whose policy was saved but never stored, and a completed run in the same state, are each explained on the page with the ex…
    - [working] **D9. The fresh biped completes the whole recorded lifecycle** (`silent-river-6649`) — **Lark has a finished, user-facing lifecycle report whose every D1–D11 receipt now comes from Lark or its working copy: `docs/probes/lark…
    - [working] **D10. The persistent operator dashboard stays current** (`deep-clover-6012`) — **Port 8765 is served by the `cadex-operator-review` user unit again, and stayed so through the last three ot5 experiments and five delib…
    - [working] **D11. Viewport and videos match the neural-whoop visual reference** (`fair-wolf-4645`) — **D11 now has a current Lark comparison on the persistent private dashboard, so the visual evidence no longer belongs only to Wren.** On …
  - [superseded] **Real models, reference-grade review — the ot6 charter (ADR-328)** (`round-sun-8398`) — **Superseded on 2026-09-14: the run ended with the critic's done acceptance, and the owner ticked nine of ten criteria.** After the closi…
    - [working] **D4. The viewer shows the real model, and says so** (`chilly-banner-4507`) — **D4's evidence list is complete pending the owner's tick (ADR-333, ADR-334).** *The toggle half* (ADR-333, commit `fe181a85`) [rec: wind…
    - [working] **D1. The dashboard is one designed page** (`floral-marsh-2830`) — **D1's evidence list is complete pending the owner's tick; its three units have all landed on the operator URL.** The spec `docs/REVIEW-D…
    - [superseded] **D3. Viewport and videos match the neural-whoop reference** (`silver-ledge-4640`) — **Superseded on 2026-09-14: the owner did not tick D3 and took the rendered look into manual work outside unattended runs.** Reviewing th…
    - [working] **D2. The dashboard works on a phone** (`western-journey-2108`) — **D2 has its layout half and its interaction half, both measured at 400×850 under headless touch emulation, on the fixture project and on…
    - [working] **D10. A closing report exists and the critic accepted done** (`chilly-road-8573`) — **The closing report exists and is test-pinned, the critic accepted done, and the owner ticked D10 on 2026-09-14** (commits `45e6618b`, `…
    - [working] **D8. A single servo arm goes through the lifecycle** (`civic-creek-8215`) — **Heron, a two-DoF MG90S arm designed by the product agent, has been through the full recorded lifecycle: designed from one prompt, corre…
    - [working] **D9. Everything ot5 proved still holds** (`civic-lily-1239`) — **D9 has its final regression assessment, taken after D7 and D8 closed; its evidence list is complete pending the owner's tick** (commit …
    - [working] **D5. The biped is a buildable mechanism** (`cool-hill-9617`) — **D5's evidence list is complete pending the owner's tick (ADR-334).** Finch uses four catalog MG90S servos, horns, MR128 bearings and M2…
    - [working] **D6. The real biped trains, is measured and is recorded in the new look** (`dusty-otter-7562`) — **D6's evidence list is complete pending the owner's tick (ADR-336).** Finch's stand task declares 400 steps at 50 Hz, a fall below 84.0 …
    - [working] **D7. A two-wheeled balancing robot goes through the lifecycle** (`ready-sand-2621`) — **Robin has been through the full recorded lifecycle: designed from a prompt, accepted with fits, reopened, trained once (bounded), evalu…
  - [superseded] **The agent designs it right — the ot7 charter (ADR-341)** (`mild-ledge-7157`) — **Superseded on 2026-09-20: ot7 merged as `fa75c531`, and the ot8 follow-up charter (ADR-399, `ancient-vine-9908`) replaces this one [rec…
    - [working] **F1. The agent sees measured fit** (`wild-horizon-5461`) — **F1's evidence is complete pending the owner's checkbox (ADR-346), and it now has field evidence from a real design turn [rec: stormy-sn…
    - [working] **F2. Fit intent is declared and checked** (`winter-key-1482`) — **ADR-379's fifth check is withdrawn (ADR-380): a `clearances=` declaration on a welded pair is judged by the minimum it declares, exactl…
    - [working] **F3. Fit is checked across each joint's range** (`curious-quill-9036`) — **The product publishes opt-in, bounded exact-solid sweeps for limited rigid-tree hinges and sliders (ADR-349, ADR-351).** `assembly.asse…
    - [working] **F9. Nothing regressed** (`eager-summit-3153`) — **F9 had one measured exception for two iterations, and it is closed [rec: fresh-dawn-0892] [rec: frosty-sea-6051].** "Existing projects …
    - [working] **F8. A smoke rollout is one command** (`honest-ivy-8824`) — **F8's bounded smoke command is implemented and fixture-verified (ADR-352), pending owner/critic review.** `cadex smoke --out DIR` reads …
    - [open] **F6. The balancer is designed unassisted** (`narrow-dune-9454`) — **F6 is exhausted: the create prompt and all three continuations reached the model on `ot7-robin-c`, every turn ended on its own, four sl…
    - [open] **F4. The agent repairs from measurements alone** (`polished-forest-0215`) — **F4 is exhausted: the frozen repair prompt and all three continuations reached the model on `ot7-heron-repair-d`, and the final measured…
    - [open] **F7. The biped is designed unassisted** (`rapid-grove-9687`) — **F7's sixth requirement has a measured verdict at last, and it is a pass — taken with no slot, on a model rebuilt outside the accepted p…
    - [open] **F5. The arm is designed unassisted** (`stormy-aspen-5433`) — **F5 is exhausted: the create prompt and all three continuations reached the model on `ot7-heron-c`, every turn ended on its own, and the…
    - [open] **F10. A closing report exists and the critic accepted done** (`first-snow-5587`) — **F10's report now exists in the shape the criterion asks for, and it claims done (ADR-397, commit `f7205560`) [rec: old-dew-1568].** `do…
  - [working] **The unassisted-design evidence finished — the ot8 charter (ADR-399)** (`ancient-vine-9908`) — **Owner-directed work for run `ot8` (ADR-399): finish the unassisted-design evidence ot7 left measured-but-open [rec: keen-stone-1720] [r…
    - [working] **G1. The follow-up has a frozen, bounded experiment contract** (`wise-aspen-8848`) — **G1 is met in its artifacts and its tests; what remains for the owner's tick is the closing report, which is G6's work [rec: honest-ash-…
    - [working] **G3. The biped's smoke is measured against its accepted artifacts** (`empty-arrow-8425`) — **G3's success bar is met and measured [rec: sunny-quill-9617].** Work happened on `ot8-plover`, an independent copy of `ot7-plover-e` pr…
    - [working] **G5. The regression floor still holds** (`lucid-flame-4255`) — **Met in its evidence at the final code revision, pending the owner's tick [rec: hidden-delta-8675].** Measured against a freshly built a…
    - [working] **G4. The balancer's failed smoke has an actionable measured diagnosis** (`scarlet-hill-8037`) — **Met in its evidence, pending the owner's tick: the balancer's failed smoke is diagnosed as a missing feedback control, not a design def…
    - [working] **G6. A closing report carries every outcome and is accepted by the critic** (`smooth-vine-2389`) — **Met in its artifacts and tests; what remains is the owner's tick and the critic's acceptance [rec: rough-ridge-4729].** `docs/probes/ot…
    - [working] **G2. The arm's catalog-provenance gap has a bounded measured outcome** (`tender-bay-4302`) — **G2's success bar is met and measured, on the initial create prompt alone, with all three continuations unspent [rec: winter-creek-7660]…
