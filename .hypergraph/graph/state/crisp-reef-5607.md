---
node_id: f9d8b739-9a02-5f30-89eb-3a1cb1a32b7a
slug: crisp-reef-5607
title: The walk exists and is tested headlessly
created_at: '2026-09-06T19:18:33+00:00'
parents:
- nimble-pine-0740
summary: ''
---
Status: working

## Current

Charter criterion: **The walk exists and is tested headlessly.** One documented entry point (a CLI prompt or a headless script) takes a mechanism from design → assembly → MJCF → task → toy-scale local CPU training → policy verify → rollout → review, on this machine, with no human step. Declared target `gap-walk-exists-tested-headlessly-one` [rec: empty-wolf-3962]. The nt3 operator directive re-seeded the same criterion, unticked, as this run's leading frontier [rec: modest-summit-8554].

**Met, 2026-09-07: the walk from `--prompt` completed unattended twice in a row, on two mechanisms designed inside the walk** [rec: cool-fountain-2483] [rec: placid-sky-7374], and a third clean prompt walk followed on 2026-09-08 [rec: empty-banner-7438]. Before the quota-blocked leg rehearsal below, five prompt-walk attempts across nt3: one failure predating both fixes below, then four clean runs — two on different prompts, mechanisms, joint types, actuator types and seeds, and two on the pan-tilt prompt against the installed bundle [rec: morning-summit-7848]. The second run changed nothing in the tree (`dfeccc9f`; `git status` clean) and the CLI suite passed 142 with no skips on it, engine-backed walks included [rec: placid-sky-7374]. This is the plan's tick rule satisfied — two clean of three with the one failure older than both fixes [rec: rustic-loom-0992, on the plan view] — and it answers the reliability doubt that held the node open after the first clean run.

| prompt walk | mechanism, joint, actuator | design | train | declare | rollout | wall | peak RSS | `total_reward` | witness err |
|---|---|---|---|---|---|---|---|---|---|
| nt3b [rec: cool-fountain-2483] | pendulum rig, revolute, position servo | 262.03 s | 14.50 s | 1.43 s | 1.76 s | 280.7 s | 1.23 GB | 425.997 | 4.48e-09 |
| nt3c [rec: placid-sky-7374] | vertical carriage, prismatic, bounded force motor | 172.59 s | 13.06 s | 1.25 s | 1.50 s | 189.4 s | 1.115 GB | 0.0035556 | 3.679e-09 |
| nt3 pan-tilt [rec: empty-banner-7438] | pan-tilt head, two revolute in series, two catalog MG90S position servos | 347.25 s | 17.49 s | 1.54 s | 1.74 s | 371.70 s | 1.419 GB | -2380.357 | 4.028e-09 |
| nt3 pan-tilt, refreshed bundle [rec: morning-summit-7848] | pan-tilt head, same prompt; two MG90S bodies fused into hand-authored solids this time | 374.52 s | 24.65 s | 1.52 s | 1.72 s | 406.33 s | 1.750 GB | -237.568 | 2.882e-09 |
| nt3 two-servo leg, qualified payload [rec: proud-beacon-8002] | hip-and-knee leg, two revolute in series, two catalog MG90S position servos placed separately | 539.06 s | 19.13 s | 1.82 s | 2.09 s | 565.72 s | 1.271 GB | -5144.792 | 3.758e-09 |
| nt3 two-servo leg, durable project, findings as inputs [rec: fond-star-1809] | same leg, same payload; prompt names the previous review's three findings; six catalog instances placed separately | 599.74 s | 20.03 s | 2.33 s | 2.69 s | 630.22 s | 1.116 GB | 0.853045 | not recorded |

All at 1 iteration × 4 environments (seeds 0, 1, 0, 0, 0 and 0), `--timeout 600`, under the charter's guards (0.2 s process-tree RSS sampling, 2.9 GB and 850 s cutoffs), inside the 15-minute and 3 GB limits. `review.json` (`cadex-walk-review-v1`) landed in each project beside `ARCHITECTURE.md`, `DECISIONS.md` and `PROGRESS.md`. The nt3c design turn wrote the inline-literal `assembly.policy(...)` call with `policy_on` unprompted and recorded three of its own `DECISION:` lines as project ADRs (no collision geom on the rail; a raw force motor so the action space is the force range; termination at `carriage_z < 3` mm) [rec: placid-sky-7374]. Neither number is a claim about learned control: one PPO iteration is a smoke test of the loop, which is what the criterion asks for.

**Earlier evidence, still standing.** `cadex walk` is the documented headless entry point since ADR-199 (commit a4248b26; `docs/CLI.md` walk section, `docs/MUJOCO.md` §7c, ROADMAP tick): two real walks on the plate-and-arm toy from an existing script, `review.json` under `--out`, the scaffolded `.gitignore` keeping checkpoints, `train/` copies and `*-trace.json` out of the project's history while `assets/*.cxpolicy` stays committed; `cli/tests/test_walk.py` pins the digest edit, review reader, leg order and flags plus both real walks [rec: shy-cabin-0798]. Both `examples/lifecycle` recipes ran clean on this machine in nt3 (hinged-arm 15.3 s, 1.03 GB, -27.109384; linear-carriage 13.1 s, 0.98 GB, -24159.195), and commit `64b92b29` made `docs/CLI.md` state all three counts the digest edit refuses on [rec: misty-rain-9048].

**The two legs the first prompt run named are closed** — both were the walk's own defects, not the model's [rec: misty-rain-9048]:

1. **The digest edit no longer refuses an agent-authored script** (commit 7d089bff) [rec: cool-fountain-2483]. `CLI_OVERLAY` in `cli/cadex_cli/agent.py` teaches `weights=` and `sha256=` as inline literals at the call site, names constant-factoring as the mistake, and marks the call as the one exception to the parametric rule; `test_turn_loop.py` pins the contract text and `test_walk.py` refuses the constant-factored script on both keywords, then feeds the prompt's own example through `walk._replace_keyword` byte for byte. `declare_policy` is unchanged by decision.
2. **A failed design leg now names its cause** (commit dca63b13) [rec: ancient-wind-0117]. The `accepted is None` branch in `cli/cadex_cli/__main__.py` reports the last engine refusal (op, failure code, clipped message) or "the engine refused nothing" with the ops called, plus the agent's closing words, each clipped to 400 characters; `run_leg` already carries a child envelope's `error` into the walk envelope, so no `walk.py` change. Three stubbed-turn tests pin it. No automatic retry, no leg-order change, no `OP_ARG_SPECS` change.

Reconcile judgement: flipped `open` → `working` on the second clean prompt walk [rec: placid-sky-7374]. The 2026-09-06 `working` flip rested on walks from an existing script and was reverted when the prompt path failed [rec: misty-rain-9048]; the earlier caution after one clean run ("fixed or lucky") is now answered by an independent second run with every variable changed. What remains on the walk is not its shape: the GUI-attached and remote modes stay documented-not-exercised (`witty-spark-2613`), and the review frontier remains tracked by `damp-moon-9297`. The cold revisit also confirms committed render and section persistence [rec: fair-cedar-7455].

**Latest bounded recipe and cold-revisit evidence:** a fresh hinged-arm CPU walk completed in 16.80 s at sampled peak process-tree RSS 1,056,636,928 bytes. Its verified 50-step rollout retained total reward -27.109384220927513 and witness error 1.3841167412209642e-09. After the producing process exited, six separate public CLI commands (script, asset, inventory, clearance, render and section) preserved accepted revision/digest, policy assets and a byte-identical restored trace. Full built-engine CLI gate: **195 passed, zero skipped** (218.91 s). No persistence defect or recovery was found; this maintenance direction is exhausted and status remains `working`. This is toy CPU evidence, not useful learned control, a prompt reliability trial, GUI/remote execution or packaged qualification; timings overlapped the gate and are observations, not benchmarks [rec: fair-cedar-7455].

**Documentation corrections landed:** VISION principles 5 and 3 now agree with the supported CPU walk: offboard training is a dependency/payload boundary, and policy asset status follows the script rebuild boundary rather than GPU duration. Reconcile judgement: retain `working` on the existing toy CPU evidence; neither prose correction reran or recertified the runtime, identified a missing leg, or expanded GUI/remote qualification [rec: polished-moss-9358] [rec: neat-summit-3586].

**Ordinary installed-bundle qualification now passes.** The baseline design refused the stale bundled API import before acceptance, training or review; this deployment failure did not invalidate the prior working lifecycle evidence [rec: northern-hill-9362]. Supported staging, one shell build and bundle installation resolved it without a source fix. Fresh hinged-arm design and the full CPU walk then passed in 16.04 s at 1,054,588,928 bytes peak process-tree RSS, with one iteration, four environments and seed 0. Witness error was 1.3841167412209642e-09 and rollout total reward -27.109384220927513. Same installed-root packaged lifecycle and full CLI gates passed 15 and 195 tests respectively, with no skips; the matching built-bundle background shell gate also passed. Reconcile judgement: retain `working`; the packaging direction is exhausted. This qualifies the local installed-engine route at toy scale, not useful learned control, a new prompt reliability trial, hermetic portability, GUI-attached lifecycle or remote training [rec: strong-raven-3067].

**Engine-selection guidance is bounded to its verified check.** CLI payload resolution refuses only a nonempty manifest/module schema mismatch, and `docs/CLI.md` §6 and the resolver comment say so (commit c84fd306). Runtime unchanged; installed-engine CLI gate 195 passed, zero skips. Prose-only, no leg added, no walk rerun [rec: tiny-haven-0347] [rec: keen-pebble-3574].

**Third clean prompt walk, on the installed bundle, on a topology the walk had never carried (2026-09-08).** One `cadex walk --engine <installed bundle> --prompt` invocation designed a two-axis pan-tilt head — two revolute joints in series on orthogonal axes, driven by two catalog TowerPro MG90S — and took it through design, train, declare and rollout with **all four legs exiting 0, no leg needing a person and no leg needing a guess**. The tree was unchanged and clean at `a31cb828`; bundle identity matched the refresh record before use; environment overrides were unset and the engine chosen by explicit `--engine`. Whole walk 371.70 s wall and 1.419 GB peak process-tree RSS, inside the 850 s and 2.9 GB guards. The design turn wrote two `fixed` mounts, two `revolute` joints, two `position` actuators carrying the MG90S stall torque with damping from its free speed, `joint_dynamics`, observations, a task with closeness bonus, tracking and effort costs, ±10% head-mass randomisation and joint-limit termination, plus three of its own `DECISION:` lines. The project landed five commits, clean, with `review.json`, the three project documents, inventory, clearance, four views and the XZ section. What the walk's review leg reported about clearance was wrong for catalog parts; that is a review-surface defect tracked and since fixed on `damp-moon-9297`, not a walk failure — an offending pair is a finding by design and the walk correctly exits 0. Reconcile judgement: retain `working`; three clean of four prompt attempts, the one failure older than both walk fixes. Toy CPU evidence only: one iteration cannot track, and GUI-attached and remote modes stay documented-not-exercised [rec: empty-banner-7438].

**Fourth clean prompt walk, on the refreshed installed bundle (2026-09-08).** After `build-shell` and the install script put ADR-241 and ADR-242 into the `.app` (worker byte-identical to source; packaged lifecycle gate 15 against that root), the same pan-tilt prompt ran once more through `cadex walk --engine <bundle>` with environment overrides unset and a fresh scratch project outside the checkout: **four legs exit 0, no leg needed a person and no leg needed a guess**, 406.33 s wall and 1.750 GB peak inside the 850 s and 2.9 GB guards, no source change and a clean tree. Accepted revision `8ad39702…`, 4,738 parameters, witness error 2.882e-09; six project commits carrying the three documents, inventory, clearance, four views and the XZ section, all inspected. The design turn is nondeterministic: this time it called `lib.servo("mg90s")` twice but fused both bodies into its three hand-authored solids (3 components, 0 catalogued), so the review leg's clearance rows never touched a catalog part — the catalog-clearance verdict came from a separate cross-check on `damp-moon-9297`, not from this walk. Reconcile judgement: retain `working`; four clean of five prompt attempts, the one failure older than both walk fixes. Toy CPU evidence only (-237.568 is one project's objective after one iteration); GUI-attached and remote modes stay documented-not-exercised [rec: morning-summit-7848].

**Two-servo leg rehearsal blocked by provider quota (2026-09-08).** Design exited 1 after 2.34 s with "You've hit your session limit · resets 8:30am (Europe/Madrid)"; whole command 2.8271 s, peak process-tree RSS 386,695,168 bytes, no cutoff. No script or revision was accepted; training, rollout and review were not reached, and project scaffolds are not an accepted design. Reconcile judgement: retain `working` on prior successful walks; this attempt adds no mechanism or review qualification. The selected bounded leg walk remains unperformed beyond the quota refusal; a retry depends on provider availability [rec: silent-mist-5233].

**Worker prerequisite qualified; fresh two-servo rehearsal still pending.** The repaired worker snapshot boundary passed full engine (2085 passed, 52 skipped), built-engine CLI (195 passed, no skips) and fresh packaged lifecycle (15 passed) gates after successful build/install and staging. All staged worker members matched payload bytes with link count 1; real acceptance/rebuild preserved the digest. This qualifies the local payload prerequisite, not a fresh design/training/review run or the still-stale installed application [rec: lawful-dune-3795].

At the recorded 06:12 Europe/Madrid dispatch on 2026-09-08, rehearsal remained deferred until the reported 08:30 reset (06:30 UTC) or affirmative provider availability evidence. No provider probe or walk was invoked, no lifecycle leg was reached and no new project or review artifacts were produced. Source and staged runtime hashes still matched the qualification record. Reconcile judgement: retain `working` on prior successful walks; the fresh two-servo run and comparable progress numbers remain missing, and this deferral establishes neither a new provider failure nor availability [rec: calm-beacon-7800].

**Fifth clean prompt walk: the eligible two-servo leg rehearsal completed on the qualified payload (2026-09-08).** After the one permitted local UTC observation (06:30:04, past the 06:30:00 boundary), the exact recorded leg prompt ran once through the unchanged documented `cadex walk --engine <staged payload>` with environment overrides unset, a fresh external project and the existing RSS/wall monitor. **Design, train, declare and verify/rollout all exit 0, no operator correction, no quota refusal:** 565.72 s whole-monitor wall and 1,270,939,648 bytes sampled peak, inside the 850 s and 2.9 GiB guards. The payload's `CadexScriptedRuntime.py` matched the worker qualification hash before dispatch, and the child argv records the explicit payload on all four legs. The design turn read the API through 34 inspect calls, accepted a first script, then replaced its fallback density with the catalog's `effective_density_kg_m3`; it modeled the two MG90S as torque-limited position servos with damping from stall torque over no-load speed. Accepted revision `671a1c28…`, 4,738 parameters, training wall 2.23 s, final batch reward/step -34.199; verified rollout 200 steps at 50 Hz, no range termination (truncated at horizon), total reward -5144.792 (crouch pose -5144.792, effort -0.000227), witness error 3.758e-09. The project has five automatic commits and clean status carrying source, ARCHITECTURE, five project ADRs, PROGRESS, inventory, clearance, review JSON, four views and the section; the stored policy asset is committed, checkpoints and trace are ignored. No repository edit, and the engine/CLI gates were not rerun by decision, the walk itself being the runtime check on the qualification already recorded. Reconcile judgement: retain `working`; five clean of six prompt attempts, the one failure older than both walk fixes. The rehearsal the previous three records left pending is now evidenced. Toy CPU evidence only: one PPO iteration says nothing about learned crouching, the leg is not mechanically qualified (its review findings are on `damp-moon-9297`), and GUI-attached and remote modes stay documented-not-exercised [rec: proud-beacon-8002].

**The walk's domain-doc convention is now reachable by a design turn (ADR-245, commit `febaf940`).** The leg rehearsal above left no actuator or sensor note, and the reason was in the instruction: it told the model to "ask the caller to write those", and nothing but the CLI's own generated reports could write `docs/<subject>.md`. A closing `NOTE <subject>: <text>` line now appends a dated bullet to the project's `docs/<subject>.md` through the same closing-text channel that lands `DECISION:` lines as ADRs; the subject is slugged so it cannot escape `docs/`, and `inventory` and `clearance` are refused because a note must never append to a measurement. The notes are pasted back into the next turn beside the three project documents, bounded at 2,000 characters tail-first, and the design instruction asks for `docs/actuators.md` and `docs/sensors.md` from any mechanism with actuators or sensors. All in the LGPL CLI zone plus `docs/CLI.md`, ADR-245 and the ROADMAP tick; no protocol op, engine or shell change. CLI suite **197 passed, no skips** (218 s, engine half included); the two new tests fail on the previous source. The record is explicit that this verified the mechanism and not the behaviour — no design turn ran under the new instruction in that unit [rec: lucid-pebble-4000].

**Sixth clean prompt walk, in a durable project, and the first live evidence for ADR-245 (2026-09-08).** The documented `cadex walk --engine <qualified payload> --prompt` ran the two-MG90S leg again, this time in a durable project outside this repository and outside `~/arch` (basename `nt3-leg`), with a prompt that stated the previous rehearsal's three review findings with their measured volumes as design inputs. **All four legs plus review completed headlessly, no leg needed a person or a guess, no leg was unreached**: monitor exit 0 in 630.22 s, peak process-tree RSS 1,115,717,632 bytes, inside the 850 s and 2.9 GiB guards; the payload's `CadexScriptedRuntime.py` matched the qualification hash before dispatch, HEAD `88f7b732` clean. The design turn read the API (one self-inflicted `inspect` path error it recovered from), accepted revision `417d0fdf…`, and **left `docs/actuators.md` and `docs/sensors.md` behind**: the torque-limited position-servo model (176.52 N·mm, 600 °/s no-load from the MG90S 4.8 V datasheet, 30 N·mm/deg stiffness, damping 0.294 N·mm·s/deg so torque saturates at no-load speed) and six sensor channels (hip/knee angle in degrees, joint velocity, actuator effort in N·mm). Five project ADRs landed from the same closing paragraph. Training 4,738 parameters, trainer wall 2.3 s, verified rollout total 0.853045 (crouch_tracking 0.853094, effort −4.857e-5) — a positively shaped objective, **not comparable** with the earlier leg's −5144.79. The project has five commits, 72 tracked files, clean status; this repository's status is clean and carries no project artefact. The geometry answers to the findings are on `damp-moon-9297` and `calm-peak-5247`. Reconcile judgement: retain `working`; six clean of seven prompt attempts, the one failure older than both walk fixes, and the convention ADR-245 introduced is now observed working on a live turn. **What remains unexercised is a repair turn on a live conversation** — a second design turn through `--resume` that iterates from the run's own review rather than from a prompt told the answers; the durable project exists to do it on. That run exposed the absolute output-label defect [rec: fond-star-1809]; ADR-246 now fixes future walk rows and commit subjects [rec: twilight-snow-9071].

**Walk output labels are portable (ADR-246).** Progress rows and project commit subjects use a project-relative output path, or the basename for external outputs. Four persisted-row/commit regressions failed on the old formatter and now pass; the full CLI run recorded 200 passed and one stale absolute-path assertion, then the corrected real toy-walk/iterate test passed alone with no skips. This is regression and toy evidence, not a live durable-run verification; historical rows and commits are unchanged, and external outputs sharing a basename can share a label [rec: twilight-snow-9071].

**Durable resumed repair remains unproven.** One `cadex walk --resume` attempt stopped at design with a provider usage-credit refusal (exit 1; 3.9086 s monitored wall, 432,635,904 bytes peak RSS, no cutoff). Training, declaration, rollout and review were unreached; no repair, comparable reward, new progress row or project commit exists. The argv contained `--resume` and the stored session id stayed unchanged, but successful conversation resumption is not established. No retry or provider switch followed. Reconcile judgement: retain `working` on the completed walks; this attempt proves neither repair nor live portable-label persistence and does not change GUI/remote qualification [rec: sunny-walrus-5847].

**Offline refusal and acceptance coverage now pins session persistence (ADR-247).** Real local restore plus a controlled provider refusal preserved accepted source/revision/digest/parameters and project history while legitimately refreshing accepted-artifact attempt locators. The identified unchanged-session timestamp rewrite is fixed: matching stored nonempty session ID/model preserves `agent.json`; changed identity persists even on refusal. Three real-engine refusal regressions and two successful acceptance regressions cover unchanged identity, changed session/model, preserved user work and accepted edits; all 206 CLI tests passed with zero skips. Reconcile judgement: retain `working` on existing walk evidence. These offline tests establish neither successful live conversation resumption nor the parked repair's downstream training/review or live portable-label evidence; scheduling remains controller-owned. [rec: vast-ledge-4610] [rec: ready-otter-2700]

**Latest provider-free rehearsal and scratch repair (2026-09-08).** The existing qualified stage accepted the hinged-arm script and completed CPU training, policy declaration, verification/rollout and review in 15.6578 s at 0.987 GiB sampled peak RSS (one iteration, four environments, seed 0). Witness error was 1.3841167412209642e-09; the 50-step rollout returned -27.109384220927513. The absolute output path persisted as `runs/baseline` in the progress row and commit subject. This is downstream toy execution evidence; no provider design or resumed repair ran [rec: lucid-snow-0350].

Preinitializing scratch Git bypassed first-visit ignore scaffolding and committed generated files. Scratch commit `faeced2` repaired the index forward: 33 generated/transient paths are now ignored and untracked, all local bytes preserved; earlier commits still contain them. The stored policy asset and review evidence remain tracked, 67 unchanged existing files are hash-identical, and scratch status is clean. Reconcile judgement: retain `working`; the setup error is repaired without a product change or rerun, while first-visit scaffolding remains unexercised and live resumed repair remains unproven [rec: scarlet-journey-0203].

## Negative knowledge

- [scope: what the walk commits from a training leg | confidence: medium | evidence: shy-cabin-0798] Before ADR-199 the `train` leg's commit carried `job.cxpolicy`, `job.best.cxpolicy` and the store copy — three copies of one policy. The store's asset is the project (ADR-194); checkpoints and traces are not, and the `.gitignore` says so. Reversible per project, because the file is editable.
- [scope: `declare_policy` in `cli/cadex_cli/walk.py` against agent-authored scripts | confidence: high | evidence: misty-rain-9048, cool-fountain-2483] The rewrite handles inline `weights="…"` / `sha256="…"` literals only; a script that names them through module constants is refused at exit 3. The fix went into the authoring contract, not the rewrite: resolving constants would make the walk guess at a script it did not write. A test pins the refusal as intended behaviour.
- [scope: the first design-leg failure inside `cadex walk --prompt` on 2026-09-07 | confidence: medium | evidence: misty-rain-9048, ancient-wind-0117] One observed fast failure (3.76 s, after one `describe_api` call) with the child's reason discarded, and one identical retry that succeeded in 214 s. Its cause is unrecoverable: the envelope that would have named it landed afterwards. Not reproduced on purpose; the two clean runs since [rec: placid-sky-7374] make it the outlier, not the pattern.
- [scope: what one prompt walk proves about a review surface | confidence: high | evidence: morning-summit-7848] The design turn is nondeterministic across runs of the same prompt: the pan-tilt prompt placed two catalog servos once and fused them into hand-authored solids the next time. A walk selected to exercise a catalog-part code path may not exercise it at all; check the inventory's catalogued count before reading its review numbers as evidence for anything catalog-shaped.
- [scope: comparing `total_reward` across prompt-walk projects | confidence: high | evidence: placid-sky-7374, empty-banner-7438, fond-star-1809] The reward expressions are different objectives in different units, so 425.997, 0.0035556 and -2380.357 compare runs of one project only, never designs against each other — the same caveat `examples/lifecycle/README.md` records for the two example mechanisms. The pan-tilt total is dominated by its quadratic tracking cost after one iteration, a toy execution measurement rather than learned control. The two leg walks make the same point on one mechanism: −5144.79 and 0.853 are the same leg under a negatively and a positively shaped objective.
- [scope: a walk project under the system temporary directory | confidence: high | evidence: lucid-pebble-4000, fond-star-1809] It is reaped between iterations, so its review findings cannot be written back into its own `DECISIONS.md`, and there is no conversation left to `--resume`. The findings survived only in this graph. An iterate unit must walk a durable project outside the checkout and outside `~/arch`, which the sixth walk now leaves behind.

## Provenance

- empty-wolf-3962 — operator-declared charter gap
- fond-mesa-1562 — fixed-toy evidence does not close the general entry point
- shy-cabin-0798 — ADR-199: cadex walk qualified on the toy through two real walks, review.json in the project, checkpoints and traces out of its history, 13 tests
- modest-summit-8554 — nt3 operator directive re-seeds the criterion unticked as this run's leading frontier
- misty-rain-9048 — the walk run end to end on this machine: clean on both example recipes with numbers, refused at the digest edit on an agent-designed project; docs/CLI.md names all three refusal counts
- cool-fountain-2483 — the authoring contract teaches weights=/sha256= as inline literals, two tests tie the prompt's example to the rewrite, and the walk from a prompt ran clean end to end (280.7 s, 1.23 GB, total_reward 425.997)
- ancient-wind-0117 — a turn with no accepted script reports its cause (last engine refusal or "refused nothing" plus closing words, 400-char clip); the walk carries it with no walk.py change; 142 CLI tests, no skips
- placid-sky-7374 — the second independent prompt walk (vertical carriage, prismatic, force motor, seed 1) ran clean end to end on an unchanged tree: 189.4 s, 1.115 GB, total_reward 0.0035556; two clean of three attempts, the criterion declared met
- brave-delta-4193 — bounded documented arm walk refreshes CPU training, verified baseline rollout and committed review evidence; CLI 161 passed without skips
- fair-cedar-7455 — fresh CPU walk and six cold public commands preserve accepted identity, policy assets and restored trace; CLI 195 passed without skips
- lucky-canyon-6724 — preserve working CPU-walk evidence while tracking the unimplemented VISION rationale correction
- polished-moss-9358 — VISION principle 5 corrected to the offboard dependency boundary and supported CPU path; no runtime rerun
- neat-summit-3586 — VISION principle 3 policy-asset rationale corrected without changing the asset contract or runtime evidence
- northern-hill-9362 — installed-bundle baseline stops at design; prior working lifecycle evidence preserved
- strong-raven-3067 — refreshed installed engine accepts design and full bounded CPU walk; same-root lifecycle/CLI gates and matching background shell gate pass
- tiny-haven-0347 — resolver-only probe bounds the payload schema check; no walk change
- keen-pebble-3574 — payload guidance corrected to the verified check; installed-engine CLI gate 195 passed, zero skips; criterion preserved
- empty-banner-7438 — third clean prompt walk on the installed bundle: pan-tilt head, first serial two-revolute topology, four legs exit 0 in 371.70 s at 1.419 GB, no leg needed a person or a guess
- morning-summit-7848 — fourth clean prompt walk, on the refreshed installed bundle carrying both clearance fixes: four legs exit 0 in 406.33 s at 1.750 GB, witness 2.882e-09, no person and no guess; the design fused its servos, so the catalog path was not exercised by this walk
- silent-mist-5233 — two-servo design refused by provider quota; no acceptance, training or review; prior working evidence preserved
- lawful-dune-3795 — worker prerequisite qualified on fresh local payload; two-servo rehearsal still pending eligibility
- calm-beacon-7800 — pre-reset dispatch deferred without probing or invoking the walk; matching runtime hash retained, no new lifecycle evidence
- proud-beacon-8002 — fifth clean prompt walk: the eligible two-servo leg through the qualified payload, four legs exit 0 in 565.72 s at 1.271 GB, witness 3.758e-09, no person, no guess, no quota refusal; the pending rehearsal evidenced
- lucid-pebble-4000 — ADR-245: a closing `NOTE <subject>:` line lands `docs/<subject>.md` and is read back next visit; the design instruction asks for actuator and sensor notes; 197 CLI tests, no skips; mechanism verified, behaviour not yet
- fond-star-1809 — sixth clean prompt walk, in a durable project with the previous findings as inputs: four legs plus review in 630.22 s at 1.116 GB, no person, no guess; the design turn writes docs/actuators.md and docs/sensors.md; the repair turn on a live conversation is what remains
- twilight-snow-9071 — absolute walk output-label defect fixed and verified by regressions/toy iterate
- sunny-walrus-5847 — durable resumed repair refused for usage credits; resumption and live label verification unreached
- vast-ledge-4610 — controlled offline refusal separates truthful restore bookkeeping from redundant unchanged-session timestamps
- ready-otter-2700 — ADR-247 suppresses unchanged-session replacement; five refusal/success regressions and all 206 CLI tests pass; live repair remains unproven
- lucid-snow-0350 — one existing-stage provider-free hinged-arm walk passes; scratch preinitialization limits ignore coverage
- scarlet-journey-0203 — scratch index repaired forward, preserving local bytes, stored policy, review evidence and history; no rerun
