This is the last correction turn for this design. Read the measured fit report for the accepted revision, at the rest pose and through every joint's declared range, and resolve every failing check it names, verifying from the measurements and never from the script's printout. Keep every purchased part from the catalog, every other part a modelled printable part, and nothing from the world in the design. Submit the whole corrected script through normal acceptance and re-accept. Then finish the documentation the project asks for: a DECISION: line for each change and NOTE design_specs: lines stating the measured fit outcome, with the count of checks that pass and the count that still fail. Do not train. Do not ask questions.
