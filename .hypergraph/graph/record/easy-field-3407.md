---
node_id: dbf494f3-e02a-50c1-bf7f-6601e98d367e
slug: easy-field-3407
title: 'Record and finish the ADR-317/318 project-root containment arc: full CLI suite, persistent service restarted onto the patched server, private-URL verification of ot5-lark-copy85'
created_at: '2026-09-13T09:13:38+00:00'
parents:
- spring-lake-5256
summary: ''
---
## What

Recorded and finished the project-root containment arc that iterations 89 and 90 landed without record nodes (commits `8a4bb4cd` and `7ab2a126`, ADR-317 and ADR-318). Iteration 89 anchored the reader at the project root: `read_run_record` returns an `unreadable` record with `run directory escapes the project directory` for a `runs/<name>` that resolves outside the project, opening nothing under it; `_retained_policies` resolves each run's `train/` and each file in it against the project root and additionally requires the file to sit inside that run's own `train/`; `_origin` reads telemetry only through `_retained_progress`, which applies both checks to `progress.json`. Iteration 90 anchored the dashboard's model and mesh routes the same way: `run_model` in `cli/cadex_cli/review_server.py` refuses an escaped run directory with `available: false` and the same reason before its retained training view or rollout is read, and `run_mesh` serves only a file that resolves inside the project root whatever the run directory's own links say. Four regressions: `test_a_run_directory_symlinked_outside_the_project_is_never_read` and `test_external_progress_and_policy_symlinks_inside_a_run_are_never_followed` (`cli/tests/test_review_record.py`), `test_a_run_directory_outside_the_project_has_no_model_and_serves_no_mesh` and the headless-browser `test_browser_lists_an_escaped_run_as_unreadable_and_draws_nothing_for_it` (`cli/tests/test_review_server.py`). Docs: `docs/CLI.md` (what the anchor refuses, on the reader and on the model/mesh routes) and `docs/probes/operator-review/README.md` (iterations 89 and 91). This iteration adds no product code: it ran the full CLI suite including the browser regressions, restarted the persistent operator service onto the patched server, verified the working project and run over the private-network URL, and wrote this record.

## Why

The critic's message, in order: add the missing causal record with real State Impact targets and actual verification results (ADR-318 cited a record node that did not exist, and ADR-317's did not exist either); finish the containment unit by running the full CLI suite including browser regressions, restarting the persistent dashboard with the patched server, and verifying the active project/run over the private-network URL. This does exactly that, as one record covering both unrecorded iterations because they are one defect class (a containment check anchored at an already-escaped run directory) fixed in the reader and then in the one route a browser draws from. It advances **D1** (the charter's "serve only the configured project's permitted artifacts, never arbitrary filesystem paths" now holds on the model and mesh routes, with a failing-on-old-server HTTP regression and a browser regression) and **D10** (the persistent URL was restarted deliberately, with no trainer active, and re-verified serving `ot5-lark-copy85` with `lark86-retry-video` selected on a fresh visit). No deviation from the message; the "choose the next demonstrated lifecycle defect" part is the next iteration's unit and is named as the bet below, because this dispatch's budget is one unit and the containment finish is it. The planned short-term bet on rollout-pose clearance is banned by the critic and is not taken.

## Method

Established that the port 8765 service (transient user unit `cadex-operator-review`, PID 4005107) started at 04:56:02 local, after the reader patch (`review_record.py` mtime 04:54:05) but before the server patch (`review_server.py` mtime 05:03:22), so ADR-318 was not live. Confirmed no trainer active (`pgrep` for `cadex_train`/`cadex walk`/mujoco empty; `nvidia-smi` compute apps empty). Started the full CLI suite in the background: `pixi run python -m pytest cli/tests -q -x` — **445 passed, 1 skipped in 417.59 s, exit 0**, browser regressions included (the ADR-316 record had 441 passed, so the four new regressions are in that count). Stopped the unit and restarted it with the documented `systemd-run --user --unit=cadex-operator-review … review --project ~/cadex-projects/ot5-lark-copy85 --host $(tailscale ip -4) --port 8765`; new PID 4042781 active since 05:06:57, listening on `100.104.232.88:8765`, `/api/project` naming `ot5-lark-copy85` with nine runs (`lark86-interrupt` still `failed`, `lark86-retry-video` `current`). Ran the identity-resolving checker `docs/probes/wren-fresh/check_video.py ~/cadex-projects/ot5-lark-copy85 lark86-retry-video http://100.104.232.88:8765/ --label anchor91` (exit 0): fresh visit selects `RUN lark86-retry-video`, which `default_run` expects; origin `lark86-retry` (final, `source_agrees` true); 81 decoded frames differing first to last, 8.1 s encoded for 8.0 s simulated; played through polls; download hash-equal `1f53d43d1c18…`; historical selection `lark2-final` then returned to current; receipt `evidence/lark86-retry-video-anchor91-check.json` and its PNG, new files only. Probed the patched routes on the live server: `/api/model/run/lark86-retry-video` `available: true` with eight components from the run's own `rollout/*.stl`; `/mesh/run/lark86-retry-video/torso.stl` 200 with 3,052 bytes; a missing run 404 on model and mesh; `/mesh/run/<run>/../../script.json` 404. No engine, shell, protocol, payload, page or dependency change, so no engine suite or packaged gate; the escaped-run behaviour itself is exercised by the regressions with throwaway fixtures, not by planting a symlink in the working copy.

## Result

The persistent private URL `http://100.104.232.88:8765/` runs the reader and server that anchor every run, policy, telemetry, model and mesh resolution at the project root, and still serves `ot5-lark-copy85` with `lark86-retry-video` as the fresh-visit selection; no trainer is active; nothing in the copy was overwritten. ADR-317 and ADR-318 now cite this record. The unreconciled tail is two records (`spring-lake-5256` and this one). Concern: the two unrecorded iterations show the loop can land and commit code without the mandatory record step; nothing mechanical prevents it, and this record is a backfill, not a substitute for the step. Assumption: one record for two commits is acceptable because the ADRs already separate them and both are this arc. **Next bet (a different criterion, as the critic asked — D4/D5, not clearance):** the page's checkpoint-provenance line still comes from the run record's `source_run` *name*, while `policy_lineage` resolves the training run from the retained bytes and reports `source_agrees`; the checker cross-checks them but the operator's page does not show the identity-resolved origin or a disagreement. The unit is to render `policy_lineage`'s origin (run, kind, checkpoint iteration) and an explicit `source_agrees: false` state in the run panel from the existing reader, with a browser regression using unrelated run names and one whose record names a different run than the bytes it carries, verified on the persistent Lark URL. No new dependency.

Dispatch closed: 1 unit — the ADR-317/318 project-root containment arc recorded with its suite, persistent-service restart onto the patched server and private-URL verification of `ot5-lark-copy85`/`lark86-retry-video`.

## Repo

- repo: git@github.com:theo-kirby/cadex.git
- branch: ouroboros/ot5
- commit: 7ab2a126172e1369e81a15a0484ac11ca988607e

## State Impact

- target: jolly-loom-0622 — the charter's permitted-paths rule now holds on the reader and on the dashboard's model and mesh routes (ADR-317, ADR-318): a runs/<name> symlinked out of the project is listed unreadable with 'run directory escapes the project directory', contributes no policy digest, has no telemetry read, answers /api/model/run/<name> with available: false and serves no mesh bytes; four regressions (two reader, one HTTP failing on the old server, one headless-browser) pin it; full CLI suite 445 passed, 1 skipped
- target: chilly-union-8972 — review_record anchors every run, train/ and policy-file resolution at the project root and reads telemetry only through _retained_progress (ADR-317); review_server.run_model refuses an escaped run directory before reading under it and run_mesh requires the served file to resolve inside the project (ADR-318); docs/CLI.md states both refusals
- target: deep-clover-6012 — iteration 91 restarted the persistent port 8765 unit (cadex-operator-review) onto the ADR-318 server with no trainer active; it still serves ot5-lark-copy85 with nine runs, a fresh visit selects RUN lark86-retry-video (origin lark86-retry, final, source_agrees true), the video played through polls and downloaded hash-equal (1f53d43d1c18…, 81 frames), receipt evidence/lark86-retry-video-anchor91-check.json; iterations 89 and 90 had landed the reader and server without record nodes and this record backfills both
